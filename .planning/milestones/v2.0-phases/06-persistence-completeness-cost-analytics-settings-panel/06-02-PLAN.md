---
phase: 06-persistence-completeness-cost-analytics-settings-panel
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/main.py
requirements:
  - PERS-01
  - PERS-02
autonomous: true
must_haves:
  truths:
    - "After sending a fresh-prompt Quality query and reloading the conversation, Stage 2 tabs show de-anonymized model names (not 'Quality (legacy)') and the aggregate rankings table renders fully."
    - "The saved `data/conversations/{id}.json` contains `metadata.label_to_model` and `metadata.aggregate_rankings` on the assistant message produced by the fresh-streaming branch."
    - "Existing v2 conversations written before this plan (no `label_to_model` in metadata) continue to load without TypeError — frontend already handles missing keys via optional chaining."
  artifacts:
    - path: "backend/main.py"
      provides: "Fresh-streaming branch metadata pack now includes `label_to_model` + `aggregate_rankings`"
      contains: "label_to_model"
  key_links:
    - from: "backend/main.py:303-316 fresh-streaming branch"
      to: "storage.add_assistant_message metadata kwarg"
      via: "Direct dict pack (Phase 5 critique branch analog at main.py:511-523)"
      pattern: "metadata\\[\"label_to_model\"\\]"
---

<objective>
Close the PERS-01 / PERS-02 gap in the fresh-prompt fast/quality streaming branch. The QR and critique branches already pack `label_to_model` + `aggregate_rankings` into the persisted `metadata` dict (verified in `main.py:257-264` and `main.py:511-523`). Only the fresh-streaming branch at `main.py:303-316` drops both keys — they are emitted in the `stage2_complete` SSE event (line 286) but never persisted, so reload shows the "Quality (legacy)" fallback.

Purpose: PERS-01 / PERS-02 are the smallest-blast-radius wins in Phase 6 — pure dict-pack delta, no signature change, no new file. Land them first so the rest of the phase (cost capture, settings) builds on a fully-hydrating reload.

Output: The fresh-streaming branch's `message_metadata` dict gains two existing-in-scope variables as keys. Reload of any new fresh-Quality conversation hydrates Stage 2 fully.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-CONTEXT.md
@.planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-PATTERNS.md
@.planning/phases/05-critique-mode-schema-migration-in-conversation-navigation/05-CONTEXT.md
@.planning/phases/05-critique-mode-schema-migration-in-conversation-navigation/05-VERIFICATION.md
@backend/main.py
@backend/storage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pack label_to_model + aggregate_rankings into fresh-streaming metadata</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py lines 277-323 (the entire fresh-streaming branch — Stage 1/2/3 fan-out, the `message_metadata` dict at line 303-307, the `add_assistant_message` call at 310-316, the `message_metadata` SSE emit at 320)
    - backend/main.py lines 511-523 (Phase 5 critique branch — the exact analog pattern; `label_to_model + aggregate_rankings + mode` are already merged into the persisted `metadata` dict)
    - backend/main.py lines 282-286 (where `label_to_model` and `aggregate_rankings` are computed in scope — local variables already available)
    - backend/storage.py lines 215-275 (the `add_assistant_message` opaque-`metadata`-dict contract — confirms no signature change is needed)
    - .planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-PATTERNS.md §"backend/main.py — fresh streaming metadata pack" lines 196-243 (the exact code transformation)
  </read_first>
  <action>
In `backend/main.py`, modify ONLY the dict literal at lines 303-307 (the fresh-streaming `message_metadata` declaration). Append two keys to the existing dict (after `chairman`):

- `label_to_model`: read from the local variable `label_to_model` already in scope (computed at line 284 by `stage2_collect_rankings`)
- `aggregate_rankings`: read from the local variable `aggregate_rankings` already in scope (computed at line 285 by `calculate_aggregate_rankings`)

Resulting dict (4 keys total, in this order to match Phase 5 critique branch ordering at line 511-516): `profile`, `models`, `chairman`, `label_to_model`, `aggregate_rankings`.

DO NOT change:
- The `storage.add_assistant_message(...)` call at lines 310-316 (signature unchanged; `metadata=message_metadata` keeps the same kwarg).
- The `yield ... message_metadata` SSE emit at line 320 (the same dict is sent down the wire — frontend reducer drains it without protocol change per RESEARCH §Pattern G).
- The leading comment block at lines 299-302 — update it to reflect that PERS-01 lands `label_to_model + aggregate_rankings` here too. Keep the QR / critique branch ownership references.
- Any other line in the file.

Per CLAUDE.md: 4-space Python, snake_case, no docstring change required (this is a literal pack — WHY is non-obvious is "Phase 6 PERS-01" which the existing comment already names).
  </action>
  <verify>
    <automated>grep -nE "\"label_to_model\":[[:space:]]*label_to_model" backend/main.py | head -1 && grep -nE "\"aggregate_rankings\":[[:space:]]*aggregate_rankings" backend/main.py | head -1 && grep -c "label_to_model" backend/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\"label_to_model\": label_to_model" backend/main.py` returns at least 1 (the new fresh-streaming pack).
    - `grep -c "\"aggregate_rankings\": aggregate_rankings" backend/main.py` returns at least 1.
    - `python -c "import ast, pathlib; ast.parse(pathlib.Path('backend/main.py').read_text())"` exits 0 (file still parses).
    - `add_assistant_message` is called with exactly the same kwargs as before this change — no new positional or keyword arguments.
  </acceptance_criteria>
  <done>The fresh-streaming `message_metadata` dict has 4 keys (profile, models, chairman, label_to_model, aggregate_rankings = 5 keys) and is identical-shape to the Phase 5 critique branch pack (minus `mode`).</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end hydration smoke test</name>
  <files>backend/main.py (no edits — read-only verification)</files>
  <read_first>
    - frontend/src/components/MessageHeader.jsx lines 26-28 (the legacy fallback guard — confirms the frontend handles missing metadata gracefully)
    - frontend/src/components/Stage2.jsx (de-anonymization reads `metadata.label_to_model` — the consumer side)
    - data/conversations/ directory (inspect a fresh-quality conversation written AFTER Task 1)
  </read_first>
  <action>
Manual smoke test (no code change in this task):

1. Start backend (`uv run python -m backend.main`) and frontend (`cd frontend && npm run dev`) per `start.sh`.
2. Create a new conversation in the UI.
3. Switch profile to `quality` (Quality dial mid-position).
4. Send a short prompt: `What is 2+2?` (cheap query — uses real models but minimal tokens).
5. Wait for Stage 3 to complete. The cost line will NOT appear yet (Plan 03 adds it).
6. Find the new conversation file in `data/conversations/` (latest by mtime). Open it as JSON.
7. Confirm `messages[-1].metadata.label_to_model` is a non-empty dict mapping `"Response A"` / `"Response B"` / ... to model IDs.
8. Confirm `messages[-1].metadata.aggregate_rankings` is a non-empty list.
9. Reload the conversation in the UI (refresh page, click the conversation in the sidebar).
10. Confirm Stage 2 tabs show de-anonymized model names (e.g. `openai/gpt-5.1`) — NOT the literal `"Response A"` labels and NOT the `"Quality (legacy)"` fallback.
11. Confirm the aggregate rankings table renders (sorted by average position).

If any of 7-11 fail, the dict pack in Task 1 is incomplete; revisit. If all pass, proceed.

DO NOT delete the test conversation — Plan 03 / Plan 04 / Plan 06 will need a representative fresh-quality conversation in `data/` to verify cost line hydration and cost-stats aggregation.

Per CLAUDE.md: do not commit any output of this test to the repo. The conversation JSON in `data/conversations/` is gitignored already.
  </action>
  <verify>
    <automated>ls data/conversations/*.json | head -1 | xargs -I{} python -c "import json,sys; d=json.load(open('{}')); m=d['messages'][-1]; meta=m.get('metadata',{}); assert 'label_to_model' in meta and 'aggregate_rankings' in meta, f'metadata missing PERS-01 keys: {list(meta.keys())}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - At least one conversation file in `data/conversations/` contains `messages[-1].metadata.label_to_model` (non-empty dict) AND `messages[-1].metadata.aggregate_rankings` (non-empty list).
    - Reloading that conversation in the UI shows de-anonymized model names in Stage 2 tabs.
    - Reloading that conversation does NOT show the "Quality (legacy)" fallback rendered by `MessageHeader.jsx:26-28`.
  </acceptance_criteria>
  <done>PERS-01 (persist) and PERS-02 (hydrate) are both observable end-to-end from a single round-trip.</done>
</task>

</tasks>

<verification>
- Fresh-streaming branch `message_metadata` dict packs `label_to_model` + `aggregate_rankings`.
- A representative conversation JSON file in `data/conversations/` proves the keys are persisted.
- UI reload shows de-anonymized Stage 2 tabs + complete aggregate rankings table.
</verification>

<success_criteria>
- `messages[-1].metadata` of a fresh-quality assistant message contains both `label_to_model` (dict) and `aggregate_rankings` (list).
- No `add_assistant_message` signature change; PERS-03 schema_version unchanged.
- No frontend code changed in this plan — hydration already works because Phase 5 wired the `stage2_complete` event consumer to read `metadata.label_to_model`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-02-SUMMARY.md` noting the conversation ID used for the smoke test (so Plan 03/04/06 can reuse it).
</output>
