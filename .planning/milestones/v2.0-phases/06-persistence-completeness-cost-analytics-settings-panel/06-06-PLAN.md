---
phase: 06-persistence-completeness-cost-analytics-settings-panel
plan: 06
type: execute
wave: 4
depends_on:
  - 06-05-PLAN.md
files_modified:
  - frontend/src/hooks/useSettings.js
  - frontend/src/components/SettingsPanel.jsx
  - frontend/src/components/SettingsPanel.css
  - frontend/src/components/Header.jsx
  - frontend/src/App.jsx
requirements:
  - SET-01
  - SET-02
autonomous: true
must_haves:
  truths:
    - "Clicking the gear icon in the Header opens a 380px slide-out panel from the right; clicking outside it, pressing ESC, or clicking the panel's X button closes it."
    - "The panel exposes 4 controls: theme toggle (delegating to existing useTheme), font-size S/M/L radio, density compact/comfortable radio, stage4_threshold slider 1-10."
    - "Each control change writes through to localStorage and applies to the DOM IMMEDIATELY (no Save button) — refreshing the page restores the same values."
    - "stage4_threshold slider initial value is 8 (mirroring `PROFILES[\"quality_research\"][\"stage4_threshold\"]` in `backend/config.py`); the microcopy below the slider reads `Higher = stricter; only refine when answer scores ≥{value}/10`."
    - "No scrim/overlay darkens the area outside the panel (per D-04); reading the deliberation behind is uninterrupted."
  artifacts:
    - path: "frontend/src/hooks/useSettings.js"
      provides: "New ~30 LOC hook — 1:1 mirror of useTheme — handles fontSize/density/stage4Threshold (theme stays in useTheme, composed not duplicated)"
      contains: "useSettings"
    - path: "frontend/src/components/SettingsPanel.jsx"
      provides: "Native `<dialog>` slide-out drawer with the 4 controls and dismiss handlers (ESC + backdrop click + X button)"
      contains: "showModal"
    - path: "frontend/src/components/SettingsPanel.css"
      provides: "Drawer geometry (380px fixed-right) + slide-in transform + transparent ::backdrop (no scrim) + section dividers"
      contains: "settings-panel"
    - path: "frontend/src/components/Header.jsx"
      provides: "Gear icon button mounted to the right of the existing theme toggle; props the `onSettingsOpen` callback"
      contains: "GearIcon"
    - path: "frontend/src/App.jsx"
      provides: "settingsOpen useState boolean lifted at the App level; Header passes onSettingsOpen; SettingsPanel mounted as a sibling of Sidebar + ChatInterface"
      contains: "settingsOpen"
  key_links:
    - from: "frontend/src/components/Header.jsx gear button onClick"
      to: "frontend/src/App.jsx settingsOpen state"
      via: "Lifted callback prop `onSettingsOpen` (App passes setter)"
      pattern: "onSettingsOpen"
    - from: "frontend/src/components/SettingsPanel.jsx controls"
      to: "frontend/src/hooks/useSettings.js setters"
      via: "Direct destructure + onChange wiring"
      pattern: "setFontSize|setDensity|setStage4Threshold"
    - from: "useSettings setters"
      to: "<html data-fontsize/data-density> attribute + localStorage"
      via: "Plan 05's CSS overrides activate immediately on attribute mutation"
      pattern: "setAttribute.*data-(fontsize|density)"
---

<objective>
Land SET-01 and SET-02 — the discoverable Settings UI surface and the 30 LOC `useSettings` hook backing it. Plan 05 already wired the FOUC blocker + density/font-size CSS overrides; this plan adds the React surface that mutates `data-density` / `data-fontsize` attributes via state + persists everything to localStorage. The panel is a native `<dialog>` + `showModal()` per CONTEXT.md D-04 — no scrim, ESC/backdrop/X dismiss, immediate-apply (D-03).

Purpose: Settings discoverability is a Phase 6 success criterion (#4 in ROADMAP §"Phase 6"). The panel is what makes `stage4_threshold` adjustable (the per-query knob with the highest cost impact) and what surfaces font-size + density to the user without requiring DevTools.

Output: A gear icon in the Header, a 380px slide-out drawer with 4 controls, and a tested localStorage round-trip via the new `useSettings` hook. SET-03 (Pydantic field + research_strategy threshold_override + request body wiring) is owned by Plan 07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-CONTEXT.md
@.planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-RESEARCH.md
@.planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-PATTERNS.md
@.planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-UI-SPEC.md
@frontend/src/hooks/useTheme.js
@frontend/src/components/Header.jsx
@frontend/src/components/Modal.jsx
@frontend/src/components/Sidebar.css
@frontend/src/App.jsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create useSettings hook + state lift in App.jsx</name>
  <files>frontend/src/hooks/useSettings.js (NEW), frontend/src/App.jsx</files>
  <read_first>
    - frontend/src/hooks/useTheme.js (entire file 1-94 — the 1:1 mirror; specifically `readInitialTheme` lines 23-41, `useState` initializer pattern lines 52-53, `useEffect` DOM sync lines 57-59, `useCallback` setter pattern lines 79-87, `try { } catch { }` private-browsing tolerance)
    - frontend/src/App.jsx (top-level useState declarations and existing prop-passing patterns; the `loadConversations` lifecycle)
    - backend/config.py (the `PROFILES["quality_research"]["stage4_threshold"]` value — confirm it is `8`)
    - .planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-PATTERNS.md §"frontend/src/hooks/useSettings.js (NEW ~30 LOC hook, 1:1 mirror)" lines 400-468 (the verbatim hook body)
    - .planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-RESEARCH.md §"Pattern 3: `useSettings()` as exact mirror of `useTheme`" lines 300-353
    - .planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-RESEARCH.md §Pitfall 4 lines 450-457 (useSettings must NOT import useTheme — circular dep risk; SettingsPanel composes both at the component level)
  </read_first>
  <action>
Edit (a) — create `frontend/src/hooks/useSettings.js` (~30 LOC, ESM module, default export NONE — named export only per the v1.0 hook convention at `useTheme.js`).

Imports: `import { useState, useEffect, useCallback } from 'react';` — exactly the same as useTheme.

Module-level constants: `KEY_FONT = 'fontSize'`, `KEY_DENSITY = 'density'`, `KEY_STAGE4 = 'stage4Threshold'`. Module-level defaults: `DEFAULT_FONT = 'm'`, `DEFAULT_DENSITY = 'comfortable'`, `DEFAULT_STAGE4 = 8`. Place a comment on the `DEFAULT_STAGE4 = 8` line stating "MIRROR: backend/config.py PROFILES[\"quality_research\"][\"stage4_threshold\"]" (this is the WHY-non-obvious case that justifies a comment per CLAUDE.md — without it, future maintainers won't know the value is duplicated).

A single private helper `readInitial(key, fallback, validate)` (~10 LOC): wraps `localStorage.getItem(key)` in `try { } catch { }` (private-browsing tolerance per useTheme.js:31-33), runs `validate(stored)` on a non-null read, returns the validated value or `fallback`. The validators are passed in by the consumer call sites — keeps the helper generic. The validator returns the parsed-valid-value OR `null`; the helper checks `!= null` and `=== null` is the "invalid" signal.

Exported `useSettings()` function (no parameters):
- Three `useState` initializers (one per setting), each calling `readInitial(KEY, DEFAULT, validator)`:
  - fontSize validator: `v => ['s','m','l'].includes(v) ? v : null`
  - density validator: `v => ['compact','comfortable'].includes(v) ? v : null`
  - stage4Threshold validator: `v => { const n = parseInt(v, 10); return Number.isInteger(n) && n >= 1 && n <= 10 ? n : null; }`
- Two `useEffect` hooks: sync `data-fontsize` and `data-density` attributes to `document.documentElement` whenever the respective state changes.
- Three `useCallback` setters: `setFontSize(v)`, `setDensity(v)`, `setStage4Threshold(v)`. Each setter calls the corresponding setState AND writes to localStorage inside `try { } catch { }`. The stage4Threshold setter must `String(v)` before writing because localStorage values are strings.
- Return value: `{ fontSize, density, stage4Threshold, setFontSize, setDensity, setStage4Threshold }`.

DO NOT:
- Import `useTheme` here. Theme stays in its own hook (Pitfall 4 — circular dep risk).
- Use a single namespaced localStorage object — individual keys mirror useTheme 1:1 per the locked deferred-rejection in CONTEXT.md.
- Add a `system-preference` listener (font-size and density have no system pref; theme is the only one that does, and theme stays in useTheme).

Edit (b) — `frontend/src/App.jsx`. Lift `[settingsOpen, setSettingsOpen]` `useState(false)` near the existing top-level state declarations. Pass `onSettingsOpen={() => setSettingsOpen(true)}` as a prop to `<Header />`. Mount `<SettingsPanel open={settingsOpen} onClose={() => setSettingsOpen(false)} />` as a SIBLING of `<Sidebar />` and the main content (NOT inside `<Sidebar />` — the panel is global). Import is `import SettingsPanel from './components/SettingsPanel';` at the top.

Per CLAUDE.md: 2-space JSX, camelCase, default export for components, named export for hooks. No comments unless WHY non-obvious; the `// MIRROR:` comment on `DEFAULT_STAGE4` is the single allowed exception in this plan.
  </action>
  <verify>
    <automated>node -e "const m = require('fs').readFileSync('frontend/src/hooks/useSettings.js', 'utf8'); if (!m.includes('export function useSettings')) throw new Error('missing named export'); if (m.includes('useTheme')) throw new Error('useSettings must not import useTheme'); if (!m.includes('MIRROR') || !m.includes('stage4_threshold')) throw new Error('missing MIRROR comment for DEFAULT_STAGE4'); console.log('OK');" && grep -E "settingsOpen" frontend/src/App.jsx && grep -E "SettingsPanel" frontend/src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/hooks/useSettings.js` exists, exports `useSettings` as a named export, does NOT import `useTheme`.
    - The file contains the MIRROR comment naming `backend/config.py PROFILES["quality_research"]["stage4_threshold"]`.
    - All three localStorage operations are wrapped in `try { } catch { }`.
    - `frontend/src/App.jsx` declares `settingsOpen` useState, passes `onSettingsOpen` to `<Header />`, and mounts `<SettingsPanel />` as a sibling.
    - DevTools test: opening the page WITHOUT any prior localStorage entries → `useSettings()` returns `{fontSize: 'm', density: 'comfortable', stage4Threshold: 8, ...}`.
    - DevTools test: setting `localStorage.setItem('fontSize', 'l')` + reload → `useSettings()` returns `fontSize: 'l'` AND `<html data-fontsize="l">` is set by Plan 05's FOUC blocker AND useSettings' effect (no flicker).
  </acceptance_criteria>
  <done>useSettings hook is a 1:1 mirror of useTheme; App.jsx is wired with settingsOpen state and the SettingsPanel mount point.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build SettingsPanel.jsx + SettingsPanel.css with native `<dialog>` slide-out</name>
  <files>frontend/src/components/SettingsPanel.jsx (NEW), frontend/src/components/SettingsPanel.css (NEW)</files>
  <read_first>
    - frontend/src/components/Modal.jsx entire file (the dialog idioms; specifically the click-outside guard `target === currentTarget` and the FOCUSABLE selector — DO NOT copy the hand-rolled focus trap at lines 43-80)
    - frontend/src/components/Header.jsx lines 50-58 (existing theme toggle button — analog for the inline-SVG icon pattern + aria-label/title style) and lines 63-98 (`SunIcon`/`MoonIcon` inline-SVG components — analog for the GearIcon + CloseIcon style)
    - frontend/src/components/Sidebar.css lines 6-49 (token-only styling discipline; the new-conversation-btn transition pattern at line 39 uses `var(--motion-duration-base) var(--motion-easing-out)`)
    - frontend/src/hooks/useTheme.js (for the `{ theme, toggle }` return shape used to wire the theme control in the panel)
    - .planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-PATTERNS.md §"frontend/src/components/SettingsPanel.jsx (NEW, dialog/presentation)" lines 472-568 (the verbatim component skeleton, dismiss handlers, fieldset structure)
    - .planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-PATTERNS.md §"frontend/src/components/SettingsPanel.css (NEW styling)" lines 572-637 (the verbatim CSS skeleton)
    - .planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-UI-SPEC.md §"Layout Specifications" lines 211-218 (380px desktop / full-width mobile / outer padding `var(--space-6) var(--space-5)` / inter-section gap `var(--space-5)`)
    - .planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-UI-SPEC.md slider microcopy verbatim "Higher = stricter; only refine when answer scores ≥{value}/10" and the locked control order Theme · Font size · Density · Stage 4 threshold
    - .planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-CONTEXT.md D-04 (panel dismiss: backdrop click + ESC + explicit X, no scrim) and D-05 (slider chrome: minimal range input + adjacent value + fixed microcopy, no tick marks / +-/buttons / dynamic copy)
  </read_first>
  <action>
Edit (a) — create `frontend/src/components/SettingsPanel.jsx`. Function component default export `SettingsPanel({ open, onClose })`.

Imports (camelCase / co-located CSS / hooks pattern per CLAUDE.md):
- `import { useEffect, useRef } from 'react';`
- `import { useTheme } from '../hooks/useTheme';`
- `import { useSettings } from '../hooks/useSettings';`
- `import './SettingsPanel.css';`

Compose both hooks at the top of the component: `const { theme, toggle } = useTheme();` and `const { fontSize, density, stage4Threshold, setFontSize, setDensity, setStage4Threshold } = useSettings();` (Pitfall 4 lock).

Refs + effects:
- `const dialogRef = useRef(null);`
- `useEffect` keyed on `[open]`: if open && !dialog.open call `dialog.showModal()`; if !open && dialog.open call `dialog.close()`.
- `useEffect` keyed on `[onClose]`: attach a `cancel` event listener to the dialog (native ESC behavior — preventDefault + call onClose). Cleanup on unmount.

Click-outside dismiss: a `handleClick` on the `<dialog>` element checks `e.target === e.currentTarget` and calls `onClose()`. The inner `<div className="settings-panel__inner">` calls `e.stopPropagation()` on its own onClick so clicks inside the drawer don't trigger dismiss.

Define two inline-SVG icon components inside the same file (mirror Header.jsx 63-98 conventions — 20×20 viewBox=24 stroke-2):
- `CloseIcon` — two crossed lines (`M18 6L6 18M6 6l12 12`)
- (Header.jsx already owns `GearIcon` in Task 3 below — do NOT re-export it from here.)

JSX structure (matches D-04 + D-05 + UI-SPEC):
- `<dialog ref={dialogRef} className="settings-panel" onClick={handleClick}>`
  - `<div className="settings-panel__inner" onClick={e => e.stopPropagation()}>`
    - Header: `<header className="settings-panel__header">` with `<h2 className="settings-panel__title">Settings</h2>` and a close `<button>` with aria-label "Close settings" containing `<CloseIcon />`.
    - Section 1 — Theme: `<section className="settings-panel__section">` with a label "Theme" and a button that calls `toggle` and shows the existing SunIcon/MoonIcon (REUSE — import from `./Header` if exported, otherwise duplicate the inline SVGs since Header doesn't export them; favor importing or co-locating cleanly).
    - Section 2 — Font size: `<fieldset>` with `<legend>Font size</legend>` and three radio inputs `name="fontSize"` value `s|m|l`, each with checked={fontSize === <v>}, onChange={() => setFontSize(<v>)}, and a label.
    - Section 3 — Density: `<fieldset>` same pattern with `name="density"` values `compact|comfortable`.
    - Section 4 — Stage 4 threshold: `<section>` with a `<label htmlFor="stage4-threshold">Stage 4 threshold</label>`, a native `<input id="stage4-threshold" type="range" min="1" max="10" step="1" value={stage4Threshold} onChange={e => setStage4Threshold(parseInt(e.target.value, 10))} aria-label="Stage 4 threshold" />`, a `<span className="settings-panel__slider-value" aria-live="polite">{stage4Threshold}</span>`, and the LOCKED microcopy `<p className="settings-panel__microcopy">Higher = stricter; only refine when answer scores ≥{stage4Threshold}/10</p>`.

Control order: Theme → Font size → Density → Stage 4 threshold (per CONTEXT.md "Specific Ideas" + UI-SPEC).

NO Save/Cancel buttons. NO scrim/overlay (D-04 — CSS sets `::backdrop` to transparent). NO tick marks on the slider. NO dynamic per-value microcopy beyond the `{stage4Threshold}` interpolation in the locked string.

Edit (b) — create `frontend/src/components/SettingsPanel.css`. Token-only (zero hex literals — Pattern E lock).

Selectors required:
- `.settings-panel` — `position: fixed; top: var(--layout-header-h); right: 0; width: 380px; height: calc(100vh - var(--layout-header-h)); margin: 0; padding: 0; background: var(--color-bg-elevated); border: none; border-left: 1px solid var(--color-border-subtle); box-shadow: var(--shadow-md); transform: translate3d(100%, 0, 0); transition: transform var(--motion-duration-base) var(--motion-easing-out);`
- `.settings-panel[open]` — `transform: translate3d(0, 0, 0);`
- `.settings-panel::backdrop` — `background: transparent;` (D-04 lock)
- `.settings-panel__inner` — `padding: var(--space-6) var(--space-5); display: flex; flex-direction: column; gap: var(--space-5); height: 100%; overflow-y: auto; box-sizing: border-box;`
- `.settings-panel__header` — `display: flex; align-items: center; justify-content: space-between; padding-bottom: var(--space-3); border-bottom: 1px solid var(--color-border-subtle);`
- `.settings-panel__title` — `font-size: var(--font-size-h2); margin: 0;` (or whatever the v1.0 H2 token is — confirm by reading index.css; if no `--font-size-h2`, fall back to inline `font-size: 18px` and surface the missing token to the user)
- `.settings-panel__close` — `background: transparent; border: none; cursor: pointer; padding: var(--space-1); color: var(--color-fg-secondary);`
- `.settings-panel__section` — `padding-bottom: var(--space-5); border-bottom: 1px solid var(--color-border-subtle);` and `.settings-panel__section:last-child { border-bottom: none; }`
- `.settings-panel__label` — `font-size: var(--font-size-label); font-weight: 600; color: var(--color-fg-primary); display: block; margin-bottom: var(--space-2);`
- `.settings-panel input[type="range"]` — `accent-color: var(--color-accent); width: 100%;`
- `.settings-panel__slider-value` — `font-variant-numeric: tabular-nums; font-feature-settings: "tnum" 1; font-weight: 600; margin-left: var(--space-2);` (outside the tabular-nums root, so apply explicitly per Pattern F)
- `.settings-panel__microcopy` — `font-size: var(--font-size-microcopy); color: var(--color-fg-muted); margin-top: var(--space-2);`
- Focus ring (copy from Sidebar.css:46-49): `.settings-panel button:focus-visible, .settings-panel input:focus-visible { outline: 2px solid var(--color-focus-ring); outline-offset: 2px; }`

If any of the consumed tokens do NOT exist in `frontend/src/index.css` (`--layout-header-h`, `--shadow-md`, `--color-bg-elevated`, `--color-border-subtle`, `--color-focus-ring`, `--color-accent`), halt and surface the gap. RESEARCH.md asserts these are v1.0 Phase-4 locked tokens; if any are missing, the planner over-stated availability.

DO NOT:
- Use any third-party slider, modal, or motion library.
- Hand-roll a focus trap (use native `<dialog>` + `showModal()`).
- Add an `inert` attribute to main content (D-04 diverges from MOBL-02; the scrim-less drawer leaves main content interactive deliberately).
- Add scrim/overlay darkening (D-04 transparent ::backdrop is locked).
  </action>
  <verify>
    <automated>grep -E "export default function SettingsPanel" frontend/src/components/SettingsPanel.jsx && grep -E "showModal" frontend/src/components/SettingsPanel.jsx && grep -E "Higher = stricter" frontend/src/components/SettingsPanel.jsx && grep -E "settings-panel__inner" frontend/src/components/SettingsPanel.css && grep -E "::backdrop" frontend/src/components/SettingsPanel.css && grep -E "transparent" frontend/src/components/SettingsPanel.css && ! grep -E "#[0-9a-fA-F]{3,6}" frontend/src/components/SettingsPanel.css | grep -v "^\s*\*"</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsPanel.jsx` default-exports a function component named `SettingsPanel` taking `{ open, onClose }`.
    - The component uses `<dialog ref={dialogRef}>` and calls `dialogRef.current.showModal()` / `.close()` based on `open`.
    - ESC dismiss is wired via the `cancel` event listener (NOT a manual document keydown listener).
    - Backdrop click dismiss is wired via the `target === currentTarget` idiom from Modal.jsx.
    - The 4 controls render in the locked order: Theme, Font size, Density, Stage 4 threshold.
    - The slider microcopy contains the literal string `Higher = stricter; only refine when answer scores ≥` and renders `{stage4Threshold}` interpolated.
    - `SettingsPanel.css` has zero hex color literals (verified by `grep "#[0-9a-fA-F]"` returning no matches in style declarations).
    - `SettingsPanel.css` declares `::backdrop { background: transparent }` (the D-04 no-scrim lock).
    - End-to-end smoke: open the panel via gear button → it slides in from the right; press ESC → closes; click outside → closes; click X → closes; toggle each control → DOM updates immediately (font scales, density tightens, theme flips); reload page → all 4 values restored from localStorage.
  </acceptance_criteria>
  <done>The slide-out Settings panel is fully wired with the 4 controls, native dialog semantics, immediate-apply persistence, and the locked D-01..D-06 chrome rules.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add gear icon button to Header</name>
  <files>frontend/src/components/Header.jsx</files>
  <read_first>
    - frontend/src/components/Header.jsx entire file (the existing theme-toggle button at lines 50-58 + the inline-SVG SunIcon/MoonIcon at lines 63-98 — these are the exact analogs)
    - .planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-PATTERNS.md §"frontend/src/components/Header.jsx" lines 641-684 (the verbatim button shape + GearIcon path)
    - frontend/src/App.jsx (confirm Task 1 passed `onSettingsOpen` to `<Header />`)
  </read_first>
  <action>
Edit `frontend/src/components/Header.jsx`:

(a) Accept `onSettingsOpen` in the function-component prop destructure (alongside whatever props Header already accepts).

(b) Add a SECOND inline-SVG icon component `GearIcon` next to the existing `SunIcon`/`MoonIcon` definitions. Match the existing inline-SVG convention exactly: `width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true"`. The geometry is a gear: a `<circle cx="12" cy="12" r="3" />` plus the outer cog path from PATTERNS.md line 678 — copy the verbatim `d=` attribute (it is the standard 8-tooth cog).

(c) Add a NEW `<button>` element as a sibling to the existing `app-header__theme-toggle` button. Place it AFTER the theme toggle (right side of the Header right group). Class `app-header__settings-toggle`. `type="button"`. `onClick={onSettingsOpen}`. `aria-label="Open settings"`. `title="Open settings"`. Children: `<GearIcon />`.

DO NOT:
- Restyle the existing theme toggle.
- Move the theme toggle to a different position.
- Add new CSS classes to `App.css` (or wherever Header styles live) in THIS task — the existing `.app-header__theme-toggle` rule should already provide enough geometry; if it doesn't and the gear button looks broken, add a one-line `.app-header__settings-toggle` rule inheriting the same padding/cursor/background-transparent from the theme toggle.
- Change Header.jsx's default export or signature beyond adding `onSettingsOpen` to the destructure.

Per CLAUDE.md: 2-space JSX, camelCase, default export. No comments unless WHY non-obvious.
  </action>
  <verify>
    <automated>grep -E "GearIcon" frontend/src/components/Header.jsx && grep -E "onSettingsOpen" frontend/src/components/Header.jsx && grep -E "app-header__settings-toggle" frontend/src/components/Header.jsx && grep -E "aria-label=\"Open settings\"" frontend/src/components/Header.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `Header.jsx` declares a `GearIcon` function component with the same inline-SVG convention as `SunIcon`/`MoonIcon`.
    - `Header.jsx` renders a `<button>` with class `app-header__settings-toggle`, `aria-label="Open settings"`, `onClick={onSettingsOpen}`.
    - The existing theme toggle button is unchanged (verified by reading the diff — only ADDITION, no MODIFICATION of theme toggle lines).
    - End-to-end smoke: clicking the gear icon opens the Settings panel built in Task 2; the panel's controls work as specified.
  </acceptance_criteria>
  <done>The Header has a gear icon next to the theme toggle that opens the Settings panel; the full SET-01 + SET-02 vertical slice is observable end-to-end.</done>
</task>

</tasks>

<verification>
- `useSettings` hook exists and is a 1:1 mirror of `useTheme` (no theme duplication, no useTheme import, MIRROR comment present).
- `SettingsPanel.jsx` uses native `<dialog>` + `showModal()`; ESC, backdrop click, X button all dismiss.
- `SettingsPanel.css` is token-only with `::backdrop { background: transparent }`.
- Header has a gear button wired to App.jsx's `settingsOpen` state.
- Reload restores all 4 settings from localStorage; FOUC blocker syncs density + fontSize on first paint per Plan 05.
</verification>

<success_criteria>
- The 4 controls (theme, font-size, density, stage4_threshold) all immediate-apply and persist.
- Stage4_threshold slider's initial value is 8 (matching `PROFILES["quality_research"]["stage4_threshold"]`).
- Slider microcopy reads exactly `Higher = stricter; only refine when answer scores ≥{value}/10` with `{value}` interpolated.
- No scrim darkens the area outside the 380px panel (D-04 lock).
- The panel slides in/out via CSS transform + transition tokens, no library.
</success_criteria>

<output>
After completion, create `.planning/phases/06-persistence-completeness-cost-analytics-settings-panel/06-06-SUMMARY.md` noting:
- Confirmation that all 4 controls round-trip through localStorage on page reload.
- Confirmation that the SettingsPanel does NOT import useTheme inside useSettings (Pitfall 4 dodge).
- A DevTools capture of the panel open state + the gear button position relative to the theme toggle.
</output>
