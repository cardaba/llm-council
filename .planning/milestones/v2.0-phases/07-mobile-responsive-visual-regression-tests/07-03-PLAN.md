---
phase: 07-mobile-responsive-visual-regression-tests
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - frontend/package.json
  - frontend/visual-tests/playwright.config.ts
  - frontend/visual-tests/_fixtures.ts
  - frontend/visual-tests/welcome-state.spec.ts
  - frontend/visual-tests/stage3-highlight.spec.ts
  - frontend/visual-tests/error-banner.spec.ts
  - frontend/visual-tests/sidebar-empty.spec.ts
  - frontend/visual-tests/theme-toggle.spec.ts
  - frontend/visual-tests/settings-panel.spec.ts
  - frontend/visual-tests/stage2-rankings.spec.ts
  - frontend/visual-tests/critique-loaded.spec.ts
  - frontend/visual-tests/fixtures/stage2-mock-sse.json
  - frontend/visual-tests/fixtures/critique-conversation.json
autonomous: true
requirements: [VRT-01, VRT-02, VRT-03]
user_setup: []  # D-03 revised 2026-05-11 — Playwright runs natively on Windows, no Docker required
must_haves:
  truths:
    - "8 spec files exist in frontend/visual-tests/, one per surface (welcome / Stage 3 / ErrorBanner / sidebar empty / theme toggle / Settings panel / Stage 2 rankings / Critique loaded)"
    - "Each surface produces 2 baselines (light + dark theme) → 16 baseline PNGs total committed to the repo"
    - "Every spec imports `test` from `_fixtures.ts` (NOT directly from `@playwright/test`) so anti-flake measures 4 + 5 apply structurally"
    - "Every `toHaveScreenshot` call passes `threshold: 0.2` and `maxDiffPixelRatio: 0.02` explicitly (anti-flake measures 2 + 3, kept at call site for review visibility)"
    - "Every spec calls `await settle(page)` before `toHaveScreenshot` (anti-flake measure 1 — fonts.ready + networkidle)"
    - "Baselines are generated natively on Windows via `npx playwright test --update-snapshots` (D-03 revised — Docker deferred to v2.1+ if CI is ever introduced)"
  artifacts:
    - path: "frontend/visual-tests/playwright.config.ts"
      provides: "Playwright config with 2 projects (chromium-light / chromium-dark), vite preview webServer, snapshotPathTemplate forced to linux-suffixed paths"
      contains: "chromium-dark"
    - path: "frontend/visual-tests/_fixtures.ts"
      provides: "Centralised anti-flake fixtures (reducedMotion + global animation override + settle helper)"
      contains: "addStyleTag"
    - path: "frontend/visual-tests/welcome-state.spec.ts"
      provides: "VRT spec for the welcome state surface"
      contains: "toHaveScreenshot"
    - path: "frontend/visual-tests/fixtures/stage2-mock-sse.json"
      provides: "Deterministic mock data for Stage 2 hero state (4 evaluations + aggregate rankings table)"
      contains: "aggregate_rankings"
    - path: "frontend/visual-tests/fixtures/critique-conversation.json"
      provides: "Deterministic mock conversation with 3 critique files loaded (filenames + chip pills + pre-submit textarea content)"
      contains: "external_research"
    - path: "frontend/package.json"
      provides: "Exact-pinned @playwright/test@1.59.1 in devDependencies + npm script `test:visual`"
      contains: "1.59.1"
  key_links:
    - from: "every spec file"
      to: "_fixtures.ts"
      via: "import { test, expect, settle } from './_fixtures'"
      pattern: "from './_fixtures'"
    - from: "frontend/visual-tests/playwright.config.ts"
      to: "the vite preview build"
      via: "webServer.command = 'npm run build && npm run preview -- --port 4173 --strictPort'"
      pattern: "webServer"
    - from: "spec files needing mocked backend"
      to: "frontend/visual-tests/fixtures/*.json"
      via: "page.route('**/api/conversations/**', route => route.fulfill({ body: fs.readFileSync('fixtures/...') }))"
      pattern: "page.route"
---

<objective>
Stand up the visual regression test suite per VRT-01 / VRT-02 / VRT-03. Eight spec files cover the eight surfaces locked in D-02 (welcome, Stage 3 highlight, ErrorBanner, sidebar empty, theme toggle, Settings panel, Stage 2 rankings, Critique loaded). Each surface is baselined at one "hero" state per D-02b. Two Playwright projects (`chromium-light` / `chromium-dark`) expand 8 specs × 2 themes = 16 baselines per D-02. All 5 anti-flake measures from VRT-02 apply to every spec without exception (D-02c) — measures 4+5 enforced structurally via `_fixtures.ts`, measures 1+2+3 visible at the call site.

Purpose: Freeze the Direction A skin so future development cannot drift visually without an explicit baseline update. Per D-03 (revised 2026-05-11), baselines are generated natively on Windows — the cross-OS reproducibility argument for Docker does not apply to a single-user, no-CI project. The executor runs `npx playwright test --config visual-tests/playwright.config.ts --update-snapshots` from `frontend/` for both initial baseline generation and any subsequent regeneration. If v2.1+ later introduces CI or external contributors, regenerate once inside `mcr.microsoft.com/playwright:v1.59.1-noble` and commit the Linux PNGs as the new canonical set — a one-time future cost, not a v2.0 dependency.

Output: Sixteen committed baseline PNGs (Windows-rendered Chromium) + the Playwright infrastructure to regenerate them deterministically on the user's machine. No CI configuration (forbidden by D-04b).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-CONTEXT.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-PATTERNS.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-01-SUMMARY.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-02-SUMMARY.md

# Existing infra (read to verify the build pipeline)
@frontend/package.json
@frontend/vite.config.js
@frontend/index.html

# Components mocked / driven by VRT
@frontend/src/App.jsx
@frontend/src/components/Stage2.jsx
@frontend/src/components/Stage3.jsx
@frontend/src/components/SettingsPanel.jsx

<interfaces>
<!-- Playwright API surface (1.59.1) — copy-paste-ready -->

From @playwright/test (1.59.1):
- `test.extend({ page: async ({ page }, use) => { await page.emulateMedia({ reducedMotion: 'reduce' }); await use(page); } })` — fixture extension pattern.
- `page.emulateMedia({ reducedMotion: 'reduce' | 'no-preference', colorScheme: 'light' | 'dark' })`.
- `page.addStyleTag({ content: '...' })` — injects a `<style>` tag into the page.
- `await expect(page).toHaveScreenshot(name, { threshold, maxDiffPixelRatio, animations: 'disabled' })`.
- `page.route(urlGlob, handler)` — intercepts requests; handler can `route.fulfill({ status, contentType, body })` to mock.
- `page.evaluate(fn)` / `page.evaluate(() => document.fonts.ready)` — runs in browser context.

From `defineConfig` / `devices`:
- `devices['Desktop Chrome']` — chromium preset with reasonable defaults.
- `projects: [{ name, use: { ...devices['Desktop Chrome'], colorScheme: 'light' | 'dark' } }]`.
- `webServer: { command, url, reuseExistingServer, timeout }` — auto-spawns the dev server during tests.

<!-- Frontend mock contract -->
<!-- Spec files use `page.route('**/api/conversations/**', ...)` to intercept BOTH the list endpoint
     and the per-conversation GET. SSE streaming is NOT mocked — the hero-state baselines capture
     POST-streaming state (Stage 3 highlight, Stage 2 rankings expanded, Critique pre-submit) so
     the SSE pipeline is bypassed by hydrating a "completed" conversation directly. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin @playwright/test@1.59.1 and add visual-tests scripts to frontend/package.json</name>
  <read_first>
    - frontend/package.json (existing devDependencies block — to confirm no prior Playwright entry, and to determine the exact location for the new dependency line and the script entries)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md (Recipe 1 — version pin discipline: exact pin, no `^` / `~`)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-CONTEXT.md (D-03 revised — Playwright runs natively on Windows)
  </read_first>
  <files>frontend/package.json</files>
  <action>
In `frontend/package.json`:

- In `devDependencies`, add an EXACT pin: `"@playwright/test": "1.59.1"` (no `^`, no `~`). Pinning still matters even without Docker — keeps Chromium binary version stable across `npm install` runs so baselines don't drift when dependencies refresh.
- In `scripts`, add: `"test:visual": "playwright test --config visual-tests/playwright.config.ts"`. This script is the canonical local invocation pattern; the executor runs it via `npx` per revised D-03.
- Do NOT add `playwright` (the bare CLI) to devDependencies — `@playwright/test` is the only required entry; the test runner ships with it.
- Do NOT bump or modify any existing dependency. Do NOT remove the existing scripts (`dev`, `build`, `preview`, `lint`).
- Run `npm install --prefix frontend` from this plan — per revised D-03 (2026-05-11) Playwright runs natively on Windows. The install downloads `@playwright/test@1.59.1` + matching Chromium binary into `frontend/node_modules`. First-run takes ~2 minutes (Chromium download); subsequent runs are fast. Verify `frontend/node_modules` stays in `.gitignore`.

Per RESEARCH Open Question 1 (resolved by the orchestrator as "keep Playwright in root `frontend/package.json`, not a sub-package"), this is the right home — no `frontend/visual-tests/package.json` is created.
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; grep -n '"@playwright/test": "1.59.1"' frontend/package.json &amp;&amp; grep -n 'test:visual' frontend/package.json</automated>
    <manual>Confirm the version string contains NO `^` or `~` prefix.</manual>
  </verify>
  <done>
    - `frontend/package.json` devDependencies contains `"@playwright/test": "1.59.1"` (exact pin).
    - `frontend/package.json` scripts contains `"test:visual": "playwright test --config visual-tests/playwright.config.ts"`.
    - No Windows-side `npm install` of Playwright was attempted.
    - All other entries unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create playwright.config.ts + _fixtures.ts (anti-flake substrate)</name>
  <read_first>
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md (Recipe 1 lines 39-64, Recipe 2 lines 66-155 — both files specified verbatim)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-PATTERNS.md (lines 408-444 — config + fixture pattern + the snapshotPathTemplate gotcha)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-CONTEXT.md (D-02c — all 5 anti-flake measures apply to every spec without exception)
  </read_first>
  <files>frontend/visual-tests/playwright.config.ts, frontend/visual-tests/_fixtures.ts</files>
  <action>
Create `frontend/visual-tests/playwright.config.ts` per RESEARCH Recipe 2 lines 125-151 verbatim. Key fields:

- `testDir: '.'` — specs sit next to the config.
- `snapshotPathTemplate: '{testDir}/{testFilePath}-snapshots/{arg}{ext}'` — forces a consistent path so accidental Windows runs do not pollute Linux baselines (PATTERNS.md "Critical config field" callout).
- `use.baseURL: 'http://localhost:4173'` — vite preview default port.
- `use.viewport: { width: 1280, height: 800 }` — single desktop viewport per ROADMAP scope (mobile baselines are out of scope per CONTEXT.md "Deferred Ideas" section).
- `projects: [{ name: 'chromium-light', use: { ...devices['Desktop Chrome'], colorScheme: 'light' } }, { name: 'chromium-dark', use: { ...devices['Desktop Chrome'], colorScheme: 'dark' } }]` — the theme matrix that automatically expands 8 specs × 2 themes → 16 baselines.
- `webServer: { command: 'npm run build && npm run preview -- --port 4173 --strictPort', url: 'http://localhost:4173', reuseExistingServer: !process.env.CI, timeout: 120_000 }`.
- `reporter: [['list'], ['html', { open: 'never' }]]`.

Create `frontend/visual-tests/_fixtures.ts` per RESEARCH Recipe 2 lines 72-103 verbatim. The fixture exports:

- `test` — extended `base.test` whose `page` fixture sets `reducedMotion: 'reduce'` and injects the global animation override style tag BEFORE every test (anti-flake measures 4 + 5, structurally enforced per D-02c).
- `settle(page)` — async helper that awaits `document.fonts.ready` (anti-flake 1) then `networkidle` state. Called by every spec after navigation, before `toHaveScreenshot`.
- `expect` — re-exported from `@playwright/test` so specs only import from `./_fixtures`.

The global animation override style is:

    *, *::before, *::after {
      animation: none !important;
      transition: none !important;
      caret-color: transparent !important;
    }

The `caret-color: transparent` line is part of the verbatim Recipe 2 block and prevents the blinking text cursor from flaking baselines on surfaces with focused inputs (Settings panel, Critique textarea).

TypeScript 2-space indent (matches `frontend/src/` 2-space convention per CLAUDE.md). No JSDoc unless WHY is non-obvious — Recipe 2 is well-commented; preserve those comments verbatim.

NOTE on D-02c compliance: by putting the `page` fixture override in `_fixtures.ts` and forcing every spec to `import { test } from './_fixtures'`, no spec can accidentally use a "raw" page without anti-flake measures 4+5. The plan-checker should flag any spec that imports from `@playwright/test` directly as a D-02c violation.
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; test -f frontend/visual-tests/playwright.config.ts &amp;&amp; test -f frontend/visual-tests/_fixtures.ts &amp;&amp; grep -n "chromium-dark" frontend/visual-tests/playwright.config.ts &amp;&amp; grep -n "reducedMotion: 'reduce'" frontend/visual-tests/_fixtures.ts &amp;&amp; grep -n "animation: none !important" frontend/visual-tests/_fixtures.ts &amp;&amp; grep -n "document.fonts.ready" frontend/visual-tests/_fixtures.ts &amp;&amp; grep -n "snapshotPathTemplate" frontend/visual-tests/playwright.config.ts</automated>
    <manual>None — config validity is exercised by Task 4 via the native `npx playwright test` invocation.</manual>
  </verify>
  <done>
    - `playwright.config.ts` exists with 2 projects (light + dark), forced `snapshotPathTemplate`, vite-preview webServer.
    - `_fixtures.ts` exists with `reducedMotion: 'reduce'` + animation/transition override + `settle` helper + re-exported `expect`.
    - Comments from RESEARCH Recipe 2 preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create 8 spec files + 2 JSON fixtures for the hero-state surfaces</name>
  <read_first>
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-CONTEXT.md (D-02b — specific hero state per new surface: Settings = open + all defaults; Stage 2 = all rankings expanded with realistic mock; Critique = 3 files loaded + instruction tipeada + pre-submit)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md (Recipe 2 sample spec lines 107-119; sample pattern reusable per-surface)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-PATTERNS.md (lines 423-444 — VRT pattern per spec + per-surface deltas)
    - frontend/src/App.jsx (to understand the welcome state surface routing)
    - frontend/src/components/Stage2.jsx (to understand the data shape needed for the Stage 2 rankings hero fixture)
    - frontend/src/components/Stage3.jsx (to understand the Stage 3 highlight container — likely needs a mocked completed conversation in `page.route()`)
    - frontend/src/components/SettingsPanel.jsx (to understand how to open it — gear button click; default values come from `useSettings.js`)
    - frontend/src/components/CritiqueWelcome.jsx (or whatever the critique entry-point component is — read to identify the "3 files loaded + instruction tipeada" pre-submit state surface)
    - frontend/src/components/ErrorBanner.jsx (or similar — locate the actual ErrorBanner component to know what triggers its visible state)
  </read_first>
  <files>frontend/visual-tests/welcome-state.spec.ts, frontend/visual-tests/stage3-highlight.spec.ts, frontend/visual-tests/error-banner.spec.ts, frontend/visual-tests/sidebar-empty.spec.ts, frontend/visual-tests/theme-toggle.spec.ts, frontend/visual-tests/settings-panel.spec.ts, frontend/visual-tests/stage2-rankings.spec.ts, frontend/visual-tests/critique-loaded.spec.ts, frontend/visual-tests/fixtures/stage2-mock-sse.json, frontend/visual-tests/fixtures/critique-conversation.json</files>
  <action>
Create 8 `.spec.ts` files in `frontend/visual-tests/`, one per locked surface (D-02). Every spec follows this skeleton (RESEARCH Recipe 2 sample lines 107-119, generalised):

    import { test, expect, settle } from './_fixtures';

    test('<surface-name>', async ({ page }) => {
      // (optional) page.route() to mock /api/* endpoints for surfaces that need backend data
      await page.goto('/');
      // (optional) user interactions to drive the surface into its hero state
      await settle(page);
      await expect(page).toHaveScreenshot('<surface-name>.png', {
        threshold: 0.2,
        maxDiffPixelRatio: 0.02,
      });
    });

The 2 projects in `playwright.config.ts` automatically expand each spec into light + dark variants → no per-theme code in the spec body. Playwright appends `-chromium-light` / `-chromium-dark` to the screenshot filename via the configured `snapshotPathTemplate`.

Per-surface specifics:

**welcome-state.spec.ts** — Navigate to `/` with NO conversations (`page.route('**/api/conversations', r => r.fulfill({ status: 200, body: '[]' }))`). The empty-list welcome state appears. No clicks. Settle. Screenshot.

**stage3-highlight.spec.ts** — Mock `/api/conversations` to return ONE completed conversation with a Stage 3 final-answer payload (use a minimal but realistic shape — read `backend/storage.py` for the canonical conversation dict shape if needed). `page.goto('/')`, click that conversation in the sidebar, wait for the Stage 3 panel to render (Stage 3 has the green-tinted background per CLAUDE.md). Settle. Screenshot.

**error-banner.spec.ts** — Identify the ErrorBanner trigger. Most likely: mock an API endpoint to return 500 and let the frontend surface the banner. Alternative: if ErrorBanner accepts a controllable prop in `App.jsx`, hit the path that sets it. Per RESEARCH Open Question, the executor decides at plan-time — the simplest is `page.route('**/api/conversations', r => r.fulfill({ status: 500, body: '{"detail":"mocked"}' }))` then `page.goto('/')`. Settle. Screenshot.

**sidebar-empty.spec.ts** — Same setup as welcome-state but specifically focus on the sidebar empty state (the spec may produce a different framing — full viewport vs sidebar-cropped). Use `page.goto('/')` after `page.route('**/api/conversations', r => r.fulfill({ status: 200, body: '[]' }))`. Settle. Screenshot the full page.

**theme-toggle.spec.ts** — `page.goto('/')`, locate the theme toggle button in the Header (or in the Settings panel — whichever surfaces it), hover it (or focus it) so its hover/focus styling is captured. Settle. Screenshot.

**settings-panel.spec.ts** — `page.goto('/')`, click the gear icon in the Header to open the Settings panel. Defaults per D-02b are: `theme = system`, `fontSize = M`, `density = comfortable`, `stage4Threshold = 8` (the 8 comes from `PROFILES["quality_research"]["stage4_threshold"]` in `backend/config.py` and is the default in `useSettings.js`). Do NOT mutate any control. Settle. Screenshot the open panel.

**stage2-rankings.spec.ts** — Use `page.route('**/api/conversations/*', r => r.fulfill({ status: 200, contentType: 'application/json', body: fs.readFileSync('frontend/visual-tests/fixtures/stage2-mock-sse.json', 'utf-8') }))` to inject a hydrated conversation whose Stage 2 has 4 evaluations + aggregate rankings table populated (D-02b "hero" state). After navigation, click the conversation, expand all 4 rankings tabs (or scroll Stage 2 into view if it auto-renders). Settle. Screenshot.

**critique-loaded.spec.ts** — Either (a) navigate to the critique mode entry-point and use `page.setInputFiles()` to inject 3 deterministic files (`model-a-notes.md`, `model-b-notes.md`, `model-c-notes.md` per CONTEXT specifics), then type a critique instruction in the textarea (e.g. `"Evaluate the depth of each research and flag missing primary sources."`); or (b) load a pre-built `frontend/visual-tests/fixtures/critique-conversation.json` via `page.route()` that already represents the pre-submit state. Choose (a) if the critique entry-point can be driven by `setInputFiles` (most likely — `DropZoneSlot` is a standard file picker); choose (b) only if file injection proves flaky. Settle. Screenshot the pre-submit surface.

Fixture files:

**frontend/visual-tests/fixtures/stage2-mock-sse.json** — A hydrated conversation matching the shape persisted by `backend/storage.py`, with:
- One assistant message with `stage1` containing 4 model responses (use the deterministic council from `backend/config.py PROFILES["quality"]`).
- `stage2` containing 4 anonymized evaluations with parseable rankings.
- `metadata.label_to_model` mapping `"Response A".."Response D"` to the 4 quality council models.
- `metadata.aggregate_rankings` containing 4 entries with deterministic average positions (e.g., 1.5, 2.0, 3.0, 3.5).
- `schema_version: 2`, `mode: 'fresh'`, fixed `created_at: '2026-05-11T00:00:00Z'`, fixed `id: '00000000-0000-0000-0000-000000000002'`.
- Realistic but non-changing prose for the model responses (lorem-ipsum-style 200-word chunks — deterministic, no timestamps, no random IDs).

**frontend/visual-tests/fixtures/critique-conversation.json** — Only needed if the executor picks path (b) above. If path (a) is chosen, this file is OMITTED and removed from the `files_modified` frontmatter at SUMMARY time. If created, the file represents a critique-mode conversation in the pre-submit state — 3 external_research entries with deterministic filenames + content snippets, NO Stage 1/2/3 yet (the user hasn't submitted).

Place fixture files in `frontend/visual-tests/fixtures/` (path locked by RESEARCH Open Question 3 — JSON format chosen over inline TS for diff readability). All 2-space indent. Pretty-print with 2 spaces for diff readability.

NOTE on D-02c structural enforcement: every spec MUST `import { test, expect, settle } from './_fixtures'`. ANY spec that imports directly from `@playwright/test` is a violation — the executor MUST audit all 8 spec imports before declaring this task done.
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; ls frontend/visual-tests/*.spec.ts | wc -l &amp;&amp; grep -L "from './_fixtures'" frontend/visual-tests/*.spec.ts &amp;&amp; grep -c "threshold: 0.2" frontend/visual-tests/*.spec.ts &amp;&amp; grep -c "maxDiffPixelRatio: 0.02" frontend/visual-tests/*.spec.ts &amp;&amp; grep -c "await settle(page)" frontend/visual-tests/*.spec.ts &amp;&amp; test -f frontend/visual-tests/fixtures/stage2-mock-sse.json</automated>
    <manual>None — the specs are executed in Task 4 via the native `npx playwright test` invocation; this task only validates structure.</manual>
  </verify>
  <done>
    - 8 spec files exist in `frontend/visual-tests/` (welcome / stage3 / error-banner / sidebar-empty / theme-toggle / settings-panel / stage2-rankings / critique-loaded).
    - Every spec imports `test`, `expect`, `settle` from `./_fixtures` — zero specs import from `@playwright/test` directly.
    - Every `toHaveScreenshot` call passes BOTH `threshold: 0.2` AND `maxDiffPixelRatio: 0.02`.
    - Every spec calls `await settle(page)` before `toHaveScreenshot`.
    - `fixtures/stage2-mock-sse.json` exists with the canonical Stage 2 hero shape (4 evaluations + aggregate rankings + deterministic IDs).
    - If path (b) chosen for critique-loaded: `fixtures/critique-conversation.json` exists. If path (a) chosen: only stage2-mock-sse.json exists and `files_modified` frontmatter is updated to reflect that at SUMMARY time.
  </done>
</task>

<task type="auto">
  <name>Task 4: Generate the 16 baseline PNGs natively on Windows and commit them</name>
  <read_first>
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md (Recipe 1 — Playwright invocation; ignore the Docker-specific bits, this plan uses native Windows execution per revised D-03)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-CONTEXT.md (D-03 revised 2026-05-11 — Playwright runs natively on Windows; Docker deferred to v2.1+)
  </read_first>
  <files>frontend/visual-tests/*-snapshots/*.png (generated)</files>
  <action>
This task generates the 16 baseline PNGs by running the Playwright suite with `--update-snapshots` natively on Windows. The exact invocation:

    cd frontend
    npm install   # downloads @playwright/test@1.59.1 + Chromium binary into frontend/node_modules
    npx playwright test --config visual-tests/playwright.config.ts --update-snapshots

Key points (the executor MUST honour these):

1. **Native Windows execution.** D-03 revised 2026-05-11 — Playwright runs natively via `npx playwright test`. Do NOT invoke Docker. Do NOT halt on missing Docker. The cross-OS reproducibility argument for Docker does not apply to a single-user no-CI project; if v2.1 introduces CI or external contributors, regenerate baselines once inside `mcr.microsoft.com/playwright:v1.59.1-noble` as a one-time future cost.
2. **Playwright binary download.** `npm install` from `frontend/` downloads the Chromium binary into `frontend/node_modules`. First-run takes ~2 minutes. Subsequent runs are fast. Verify `frontend/node_modules` is in `.gitignore` (it should be — standard Vite project layout).
3. **`--update-snapshots` on first run only.** This generates the 16 PNGs (`<spec-name>-snapshots/<screenshot-name>-chromium-light.png` and `-chromium-dark.png` for each of the 8 specs). Re-running without `--update-snapshots` validates the baselines.
4. **Snapshot path suffix.** The `playwright.config.ts` `snapshotPathTemplate` must force a `-win32` (or similar OS-explicit) suffix so PNG filenames carry the rendering platform in their name. If v2.1 ever adds Linux baselines, they coexist with `-linux` suffix without collision. The current spec ONLY commits the Windows-rendered set.
5. **Commit the PNGs to git.** After generation, `git add frontend/visual-tests/*-snapshots/` and verify 16 files are staged (`git diff --cached --stat | grep '\.png' | wc -l` should be 16). PNG bytes vary in size; just confirm count.
6. **Re-run WITHOUT `--update-snapshots`** to validate the baselines are stable across two runs. If any baseline fails on the second run, the surface is flaky and must be either (a) re-investigated for missing anti-flake measures, or (b) PULLED from VRT scope per D-02c (no exemption from anti-flake).

After the second run passes clean, the suite is locked. Subsequent regressions surface as snapshot diffs in the executor's `git status` after future Phase work.

If the executor encounters a baseline that fails on the second run, the failure mode is:
- First check: did `_fixtures.ts` actually apply? Inspect via `await page.evaluate(() => getComputedStyle(document.body).animation)` inside a debug spec — should be `none`.
- Second check: are fonts settled before screenshot? `settle(page)` does this; instrument with `await page.waitForTimeout(500)` ONLY as a diagnostic, never as a fix (timeout = flake).
- Third check: deterministic data? Inspect the fixture JSON for any non-stable values (timestamps, UUIDs that differ between runs).
- Final option: pull the surface from VRT per D-02c. Update SUMMARY documenting the removal.
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; ls frontend/visual-tests/*-snapshots/*.png 2&gt;/dev/null | wc -l</automated>
    <manual>
      From frontend/, run the full suite WITHOUT `--update-snapshots`:
        cd frontend &amp;&amp; npx playwright test --config visual-tests/playwright.config.ts
      All 16 baselines (8 specs × 2 projects) must pass. Confirm the HTML report (saved to `frontend/playwright-report/`) shows 16 passing tests, 0 failing, 0 flaky.
    </manual>
  </verify>
  <done>
    - 16 baseline PNGs exist under `frontend/visual-tests/*-snapshots/`, named with `-chromium-light` / `-chromium-dark` suffixes per the `snapshotPathTemplate`.
    - All 16 PNGs are committed to git.
    - The suite passes a second clean run without `--update-snapshots`.
    - HTML report shows 16/16 passing.
    - Native `npx playwright test` command documented in the plan SUMMARY (for the README in plan 07-05's TEST-03 task).
  </done>
</task>

</tasks>

<verification>
- `find frontend/visual-tests -name '*.png' | wc -l` returns exactly 16.
- `grep -L "from './_fixtures'" frontend/visual-tests/*.spec.ts` returns nothing (every spec uses the fixture).
- A clean second run (without `--update-snapshots`) passes 16/16.
- No `.github/workflows/` or `.gitlab-ci.yml` created (per D-04b).
- No Docker invocation in this plan (per revised D-03 — Docker is v2.1+ contingency only).
</verification>

<success_criteria>
- 8 spec files cover the 8 D-02 surfaces, one hero state per surface (D-02b).
- 16 baseline PNGs committed.
- All 5 anti-flake measures applied to every spec (4+5 structurally via `_fixtures.ts`; 1+2+3 visible at call site).
- `@playwright/test@1.59.1` pinned exactly (no `^`/`~`).
- Playwright runs natively on Windows (D-03 revised — no Docker required).
- Clean second-run validation passes (no flake).
- No CI configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mobile-responsive-visual-regression-tests/07-03-SUMMARY.md` summarising:
- The exact `npx playwright test` command used for baseline generation (for the README in 07-05).
- Which path was chosen for `critique-loaded.spec.ts` (path (a) `setInputFiles` or path (b) `page.route` with fixture JSON), and why.
- The exact path of each of the 16 baseline PNGs (file listing).
- Any surface pulled from VRT scope (with rationale tied to D-02c).
- Confirmation that the 8 specs collectively satisfy VRT-01, the 5 anti-flake measures satisfy VRT-02, and the Windows-native execution path satisfies VRT-03 (revised D-03).
</output>
