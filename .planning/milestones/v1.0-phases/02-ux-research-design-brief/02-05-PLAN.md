---
phase: 02-ux-research-design-brief
plan: 05
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - .planning/ux/04-mockups/sketch-notebook.html
  - .planning/ux/04-mockups/sketch-cockpit.html
  - .planning/ux/04-mockups/sketch-minimal.html
autonomous: true
requirements:
  - UXR-04
must_haves:
  truths:
    - "Existen 3 HTML throwaway sketches one-page bajo .planning/ux/04-mockups/, uno por cada dirección tonal."
    - "Cada sketch incluye light + dark con toggle visible en runtime (botón en esquina superior derecha)."
    - "Cada sketch materializa la paleta y tipografía del Plan 03 para esa dirección."
    - "Los sketches cubren al menos: branded header, sidebar con conversaciones, input + Quality toggle con coste visible, Stage 1 con disclosure de reasoning, Stage 2 con de-anonimización y aggregate rankings, Stage 3 con synthesis."
    - "Los sketches incluyen al menos 4 microinteracciones definidas (transitions/animations)."
    - "Los sketches están marcados explícitamente como throwaway en un comentario HTML — Phase 4 NO los lift verbatim."
  artifacts:
    - path: ".planning/ux/04-mockups/sketch-notebook.html"
      provides: "HTML throwaway one-page para Direction A: Research notebook (light + dark)."
    - path: ".planning/ux/04-mockups/sketch-cockpit.html"
      provides: "HTML throwaway one-page para Direction B: Tactical cockpit (light + dark)."
    - path: ".planning/ux/04-mockups/sketch-minimal.html"
      provides: "HTML throwaway one-page para Direction C: Claude-like minimal (light + dark)."
  key_links:
    - from: ".planning/ux/04-mockups/sketch-{direction}.html"
      to: ".planning/ux/03-redesign-proposal.md"
      via: "Cada sketch implementa la paleta y tipografía declaradas en su sección Direction X del redesign proposal."
      pattern: "Direction [ABC]"
---

<objective>
Producir los 3 HTML throwaway sketches one-page (uno por dirección tonal: notebook / cockpit / minimal), cada uno con toggle light/dark visible en runtime, materializando paleta + tipografía + microinteracciones del Plan 03 para esa dirección. Los sketches sirven como **contrato visual** para que el usuario elija una dirección en Plan 06; son deliberadamente desechables y Phase 4 NO debe lift CSS verbatim de ellos.

Purpose: Las 3 direcciones del Plan 03 son descripciones; los sketches las hacen tangibles. Decidir entre 3 direcciones leyendo descripciones es cognitivamente costoso; verlas materializadas con tipografía y paleta reales lado a lado es lo que permite la decisión informada que Plan 06 va a registrar.

Output: 3 archivos HTML autocontenidos en `.planning/ux/04-mockups/`.

Paralelización: Las 3 tareas pueden ejecutarse en paralelo si el orquestador lo soporta (no comparten archivos ni dependencias entre sí); todas dependen únicamente del Plan 03. Si el orquestador serializa, el orden no importa — son independientes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ux-research-design-brief/02-CONTEXT.md
@.planning/ux/03-redesign-proposal.md
@.planning/ux/04-mockups/wireframes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sketch HTML — Direction A: Research notebook (light + dark)</name>
  <files>.planning/ux/04-mockups/sketch-notebook.html</files>
  <read_first>
    - .planning/phases/02-ux-research-design-brief/02-CONTEXT.md (D-12 HTML throwaway uno por dirección via /gsd-sketch; D-14, D-16 light+dark ambos canónicos; D-15 deliberadamente desechables; CD-02 cost surfacing por dirección; CD-03 microinteracciones mínimas — al menos hover toggle y transición Stage 1→2→3).
    - .planning/ux/03-redesign-proposal.md §`## Direction A: Research notebook` (paleta light, paleta dark, typography, microinteractions, IA, density stance, cost surfacing — usar EXACTAMENTE los tokens y familias propuestos ahí; si la dirección propuso Inter + Newsreader o Söhne + Spectral, usar esos).
    - .planning/ux/04-mockups/wireframes.md (estructura de los wireframes W01..W23 para que el sketch refleje la misma estructura — el sketch es una "fotografía" estructural pintada con la paleta de Direction A).
    - .planning/REQUIREMENTS.md (UXR-04 mockups; QUAL-03 toggle 3-state; RSCH-05 reasoning disclosure).
  </read_first>
  <action>
    Invocar el slash command `/gsd-sketch` con el siguiente brief (o, si /gsd-sketch no está disponible en el entorno, producir directamente el HTML siguiendo el spec). Producir UN archivo HTML autocontenido (`<!doctype html>` + `<style>` inline + `<script>` inline para el toggle theme), sin dependencias externas (sin CDN, sin fuentes via Google Fonts en producción — usar `font-family` con fallbacks; si la dirección requiere Inter / IBM Plex / etc., declarar el `font-family` con fallback a `system-ui` y mencionar en un comentario que en Phase 4 se cargará la fuente real).

    **Spec del archivo `sketch-notebook.html`:**

    1. **Doctype + html con `data-theme="light"` por defecto en `<html>`.** El theme se cambia toggle-ando ese atributo via JS pequeño.
    2. **`<head>`** con `<title>LLM Council — Sketch: Research notebook</title>`, viewport meta, y `<style>` con:
       - Variables CSS bajo `:root[data-theme="light"]` y `:root[data-theme="dark"]` con TODOS los tokens declarados en `## Direction A` del redesign proposal (≥8 tokens por modo). Usar EXACTAMENTE los nombres y valores de Plan 03.
       - Reset mínimo (`*{box-sizing:border-box}`, body margin 0).
       - Reglas de tipografía aplicando las familias propuestas en Direction A.
       - Reglas para los componentes principales: `.app-shell`, `.app-header`, `.sidebar`, `.conversation-item`, `.conversation-item.active`, `.search-input`, `.three-dot`, `.menu-popover`, `.main-panel`, `.welcome-state`, `.input-area`, `.attachment-chip`, `.quality-toggle`, `.quality-option`, `.cost-hint`, `.stage`, `.stage-tabs`, `.stage-tab.active`, `.reasoning-disclosure`, `.aggregate-rankings`, `.synthesis`, `.download-btn`, `.error-state`, `.loading-stage-indicator`.
       - Microinteracciones obligatorias (CD-03): (a) hover del Quality toggle (transition 150ms ease-out); (b) transición visual entre `.stage` activos (CSS animation o transition del header con duración 250-400ms); (c) hover del three-dot trigger; (d) animación del `.reasoning-disclosure` al expandirse (max-height transition).
    3. **Comentario HTML al inicio del `<body>`** literal (la cadena `THROWAWAY SKETCH` y `throwaway` en minúsculas deben aparecer):
       - `THROWAWAY SKETCH — Direction A: Research notebook`
       - `Phase 2 / UX research. NOT production code. NOT to be lifted verbatim by Phase 4.`
       - `Source of truth for tokens: .planning/ux/03-redesign-proposal.md § Direction A.`
       - `Source of truth for structure: .planning/ux/04-mockups/wireframes.md.`
    4. **Toggle de tema visible en runtime:** un botón en `position: fixed; top: 12px; right: 12px;` con label `Toggle theme` (puede llevar un emoji decorativo) que invierte `data-theme` entre `light` y `dark` via un `<script>` inline pequeño (5-10 líneas).
    5. **Contenido del sketch (one-page, scrolleable, simulando la app):**
       - **Branded header** (W20) con nombre `LLM Council` + icono simple inline (puede ser un emoji o un SVG inline pequeño).
       - **Sidebar** (W02) con 5–6 conversation items mockeados (títulos plausibles: "Why does my httpx call timeout?", "Compare DAX vs M for…", "Plan estructura semantic model", etc.). Una activa.
       - **Search input** (W05) en el top del sidebar con placeholder.
       - **Three-dot menu state** — al menos un item con hover state activo mostrando el `⋮`. Opcional: un `.menu-popover` mockeado abierto sobre uno de los items con `Rename` y `Delete`.
       - **Main panel** mostrando una conversación con:
         - Pregunta del usuario al top.
         - **Stage 1** (W16) con tabs por modelo (4 mockeados), contenido markdown del activo, disclosure colapsado `Show reasoning` debajo. Bonus si se incluye también el estado expandido en una segunda muestra.
         - **Stage 2** (W18) con tabs de evaluadores, etiqueta `Response A` con su de-anonimización en bold (`**openai/gpt-5.1**`), aggregate rankings panel con avg position + vote count.
         - **Stage 3** (W19) con synthesis markdown y botón `Download .md`. SIN el background `#f0fff0` Bootstrap-flavored.
       - **Input area + Quality toggle** (W10 + W13/14/15) — input vacío con slot del toggle al lado mostrando los 3 estados Fast / Quality / Quality+Research. **El coste DEBE estar visible** y materializado de la forma que Direction A propuso (CD-02): para `Research notebook`, coste como nota al pie del label (`Quality+Research · ~$0.45 typical`).
    6. **Markup semántico**: usar `<header>`, `<aside>`, `<main>`, `<section>`, `<button>`. No tablas para layout. ARIA labels donde haya iconos sin texto.
    7. **Sin dependencias externas**: cero `<link rel="stylesheet">`, cero `<script src="...">`. El archivo debe abrirse en cualquier navegador con doble click y funcionar.

    Restricciones obligatorias:
    - Una sola página HTML autocontenida.
    - `data-theme="light"` y `data-theme="dark"` ambas funcionales, toggle visible.
    - Tokens de paleta de Direction A — usar los del Plan 03, no inventar.
    - Microinteracciones: ≥4 transitions/animations declaradas en el CSS.
    - Comentario `THROWAWAY SKETCH` presente.
    - Coste visible en el Quality toggle (state Quality+Research) con formato `~$0.XX` o equivalente.
    - Disclosure de reasoning visible (al menos colapsado).
    - Idioma: prosa visible en el sketch (placeholders, headings, copys) en español o inglés según lo que tenga sentido para la app real (si el copy del UI real está en inglés, mantenerlo en inglés en el sketch). Comentarios HTML/CSS en español.
  </action>
  <verify>
    <automated>test -f .planning/ux/04-mockups/sketch-notebook.html &amp;&amp; grep -qi "throwaway" .planning/ux/04-mockups/sketch-notebook.html &amp;&amp; grep -q 'data-theme="light"' .planning/ux/04-mockups/sketch-notebook.html &amp;&amp; grep -q 'data-theme="dark"' .planning/ux/04-mockups/sketch-notebook.html &amp;&amp; grep -q "Stage 1" .planning/ux/04-mockups/sketch-notebook.html &amp;&amp; grep -q "Stage 2" .planning/ux/04-mockups/sketch-notebook.html &amp;&amp; grep -q "Stage 3" .planning/ux/04-mockups/sketch-notebook.html &amp;&amp; grep -qE '~\$0\.[0-9]' .planning/ux/04-mockups/sketch-notebook.html &amp;&amp; grep -q "Show reasoning" .planning/ux/04-mockups/sketch-notebook.html &amp;&amp; [ $(grep -cE '(transition|animation):' .planning/ux/04-mockups/sketch-notebook.html) -ge 4 ]</automated>
  </verify>
  <acceptance_criteria>
    - Existe `.planning/ux/04-mockups/sketch-notebook.html`.
    - El archivo es autocontenido (sin `<link rel="stylesheet">` externo, sin `<script src="...">` externo).
    - Contiene literal `data-theme="light"` y `data-theme="dark"` con CSS variables definidas para ambos.
    - Contiene el comentario disclaimer con cadena `THROWAWAY SKETCH` (o `throwaway` case-insensitive).
    - Contiene `Stage 1`, `Stage 2`, `Stage 3` como texto.
    - Contiene un coste con formato `~$0.XX` visible en el Quality toggle.
    - Contiene `Show reasoning` (disclosure de reasoning_details).
    - Tiene un toggle de tema funcional visible en `position: fixed`.
    - Tiene al menos 4 declaraciones `transition:` o `animation:` en su CSS.
    - Las familias tipográficas declaradas con `font-family` corresponden a las propuestas en `## Direction A` del redesign proposal.
  </acceptance_criteria>
  <done>
    Sketch `sketch-notebook.html` listo, abre en navegador, light/dark toggle funciona, materializa Direction A.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sketch HTML — Direction B: Tactical cockpit (light + dark)</name>
  <files>.planning/ux/04-mockups/sketch-cockpit.html</files>
  <read_first>
    - .planning/phases/02-ux-research-design-brief/02-CONTEXT.md (mismas decisiones D-12, D-14, D-15, D-16, CD-02, CD-03; specifics — Cockpit usa coste como instrumento, chip cromado con número grande).
    - .planning/ux/03-redesign-proposal.md §`## Direction B: Tactical cockpit` (paleta light + dark, tipografía típicamente densa con variant mono, microinteracciones de tono terminal/Raycast, IA densa).
    - .planning/ux/04-mockups/wireframes.md (estructura común W01..W23).
  </read_first>
  <action>
    Mismo spec que Task 1 pero **para Direction B: Tactical cockpit**. Diferencias específicas obligatorias respecto a Task 1:

    - **Paleta:** oscuros saturados + acentos cromados/neón discretos (D-10). Modo light de Cockpit es alta densidad y sobrio (no es minimal); modo dark es el "natural habitat" — debe sentirse Raycast-like.
    - **Tipografía:** típicamente Inter / IBM Plex Sans + IBM Plex Mono o JetBrains Mono para datos. Los aggregate rankings y los costes deben usar la mono.
    - **Density stance:** alta. Sidebar más estrecha pero más items visibles; Stage 2 debe sentirse como un dashboard.
    - **Cost surfacing:** chip cromado con número grande (CD-02 + specifics). El coste de `Quality+Research` debe ser el primer elemento visualmente prominente del toggle, formato `~$0.XX`.
    - **Microinteracciones:** bordes finos, atajos visibles (`⌘K` ghost label en el search), hover states marcados.
    - **Comentario disclaimer ajustado:** `THROWAWAY SKETCH — Direction B: Tactical cockpit`.

    Mismas restricciones que Task 1 (autocontenido, light+dark toggle, ≥4 transitions, comentario throwaway, contenido cubriendo Stage 1/2/3 + Quality toggle con coste + reasoning disclosure).
  </action>
  <verify>
    <automated>test -f .planning/ux/04-mockups/sketch-cockpit.html &amp;&amp; grep -qi "throwaway" .planning/ux/04-mockups/sketch-cockpit.html &amp;&amp; grep -q 'data-theme="light"' .planning/ux/04-mockups/sketch-cockpit.html &amp;&amp; grep -q 'data-theme="dark"' .planning/ux/04-mockups/sketch-cockpit.html &amp;&amp; grep -q "Stage 1" .planning/ux/04-mockups/sketch-cockpit.html &amp;&amp; grep -q "Stage 2" .planning/ux/04-mockups/sketch-cockpit.html &amp;&amp; grep -q "Stage 3" .planning/ux/04-mockups/sketch-cockpit.html &amp;&amp; grep -qE '~\$0\.[0-9]' .planning/ux/04-mockups/sketch-cockpit.html &amp;&amp; grep -q "Show reasoning" .planning/ux/04-mockups/sketch-cockpit.html &amp;&amp; [ $(grep -cE '(transition|animation):' .planning/ux/04-mockups/sketch-cockpit.html) -ge 4 ]</automated>
  </verify>
  <acceptance_criteria>
    - Existe `.planning/ux/04-mockups/sketch-cockpit.html`.
    - Autocontenido, sin recursos externos.
    - Contiene `data-theme="light"` y `data-theme="dark"`.
    - Contiene el disclaimer `THROWAWAY SKETCH` (Direction B).
    - Contiene `Stage 1`, `Stage 2`, `Stage 3`, un coste `~$0.XX`, y `Show reasoning`.
    - Tiene toggle de tema funcional + ≥4 transitions/animations.
    - Familias tipográficas coinciden con `## Direction B` del redesign.
    - El cost surfacing es prominente (chip / número grande, no nota al pie) — verificable visualmente al abrir el sketch.
  </acceptance_criteria>
  <done>
    Sketch `sketch-cockpit.html` listo, materializa Direction B con tono cockpit/Raycast en ambos modos.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sketch HTML — Direction C: Claude-like minimal (light + dark)</name>
  <files>.planning/ux/04-mockups/sketch-minimal.html</files>
  <read_first>
    - .planning/phases/02-ux-research-design-brief/02-CONTEXT.md (mismas decisiones D-12, D-14, D-15, D-16, CD-02, CD-03; specifics — Minimal usa coste como subtle subtitle bajo el label).
    - .planning/ux/03-redesign-proposal.md §`## Direction C: Claude-like minimal` (paleta arenas + acento muy pulido, tipografía generosa, microinteracciones discretas).
    - .planning/ux/04-mockups/wireframes.md (estructura común W01..W23).
  </read_first>
  <action>
    Mismo spec que Task 1 pero **para Direction C: Claude-like minimal**. Diferencias específicas obligatorias respecto a Tasks 1 y 2:

    - **Paleta:** arenas + un acento muy pulido (D-10). Light dominante en arenas cálidas, dark en off-blacks suaves (no negro puro, no gris azulado).
    - **Tipografía:** generosa, una sola familia para texto (típicamente algo como Söhne / Inter / Geist) con tamaño de body ≥16px. Una mono opcional para code blocks pero sin dominar.
    - **Density stance:** baja. Whitespace abundante. Sidebar más simple (sin chip cromados ni shortcuts visibles). Una decisión visible a la vez.
    - **Cost surfacing:** subtle subtitle bajo el label (CD-02 + specifics). El coste no compite con la decisión principal; vive como contexto. Formato `~$0.XX` igualmente, pero presentación discreta.
    - **Microinteracciones:** discretas — duraciones más largas (200-300ms), curvas suaves, sin efectos llamativos.
    - **Comentario disclaimer ajustado:** `THROWAWAY SKETCH — Direction C: Claude-like minimal`.

    Mismas restricciones que Task 1 (autocontenido, light+dark toggle, ≥4 transitions, comentario throwaway, contenido cubriendo Stage 1/2/3 + Quality toggle con coste + reasoning disclosure).
  </action>
  <verify>
    <automated>test -f .planning/ux/04-mockups/sketch-minimal.html &amp;&amp; grep -qi "throwaway" .planning/ux/04-mockups/sketch-minimal.html &amp;&amp; grep -q 'data-theme="light"' .planning/ux/04-mockups/sketch-minimal.html &amp;&amp; grep -q 'data-theme="dark"' .planning/ux/04-mockups/sketch-minimal.html &amp;&amp; grep -q "Stage 1" .planning/ux/04-mockups/sketch-minimal.html &amp;&amp; grep -q "Stage 2" .planning/ux/04-mockups/sketch-minimal.html &amp;&amp; grep -q "Stage 3" .planning/ux/04-mockups/sketch-minimal.html &amp;&amp; grep -qE '~\$0\.[0-9]' .planning/ux/04-mockups/sketch-minimal.html &amp;&amp; grep -q "Show reasoning" .planning/ux/04-mockups/sketch-minimal.html &amp;&amp; [ $(grep -cE '(transition|animation):' .planning/ux/04-mockups/sketch-minimal.html) -ge 4 ]</automated>
  </verify>
  <acceptance_criteria>
    - Existe `.planning/ux/04-mockups/sketch-minimal.html`.
    - Autocontenido, sin recursos externos.
    - Contiene `data-theme="light"` y `data-theme="dark"`.
    - Contiene el disclaimer `THROWAWAY SKETCH` (Direction C).
    - Contiene `Stage 1`, `Stage 2`, `Stage 3`, un coste `~$0.XX`, y `Show reasoning`.
    - Tiene toggle de tema funcional + ≥4 transitions/animations.
    - Familias tipográficas coinciden con `## Direction C` del redesign.
    - El cost surfacing es discreto (subtitle bajo label, no chip prominente).
    - La densidad informacional visiblemente menor que sketch-cockpit.html — verificable al abrir lado a lado.
  </acceptance_criteria>
  <done>
    Sketch `sketch-minimal.html` listo, materializa Direction C con tono claude.ai/Vercel docs en ambos modos.
  </done>
</task>

</tasks>

<threat_model>
N/A — Plan 05 produce únicamente 3 archivos HTML estáticos throwaway bajo `.planning/ux/04-mockups/`. Estos sketches NO se sirven desde el backend de la app, NO se cargan en `frontend/src/`, NO contienen llamadas a APIs reales (todo es mock estático), NO procesan input de usuario de forma significativa (solo el toggle de tema mediante un atributo en `<html>`). Sin runtime de producción, sin endpoints, sin datos reales. La política `security_enforcement` se satisface por exclusión documentada.
</threat_model>

<verification>
- 3 archivos HTML existen en `.planning/ux/04-mockups/`.
- Cada uno tiene light + dark con toggle funcional.
- Cada uno tiene el disclaimer THROWAWAY presente.
- Cada uno cubre Stage 1/2/3 + Quality toggle con coste + reasoning disclosure.
- Cada uno tiene ≥4 transitions/animations.
- Cada uno usa la paleta y tipografía de su dirección correspondiente del Plan 03.
</verification>

<success_criteria>
El usuario puede abrir los 3 sketches lado a lado, alternar light/dark en cada uno, y formarse una opinión informada sobre cuál dirección elegir en Plan 06. Las 3 direcciones se ven contrastadas y diferenciadas — si dos sketches se ven iguales, eso es señal de que el Plan 03 falló en `## Convergence risk check` y hay que volver a Plan 03 antes de cerrar la fase.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/02-ux-research-design-brief/02-05-SUMMARY.md` con:
- Confirmación de los 3 archivos producidos.
- Resumen de las paletas (light + dark) usadas en cada uno.
- Resumen de las microinteracciones implementadas.
- Notas si alguna dirección presentó dificultades específicas durante la materialización (ej: si se descubrió un problema de contraste que obliga a ajustar el Plan 03).
</output>
