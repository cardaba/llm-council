---
phase: 02-ux-research-design-brief
plan: 06
type: execute
wave: 4
depends_on:
  - 02-04
  - 02-05
files_modified:
  - .planning/ux/03-redesign-proposal.md
autonomous: false
requirements:
  - UXR-03
  - UXR-04
must_haves:
  truths:
    - "El usuario ha revisado los 3 wireframes ASCII y los 3 HTML sketches en ambos modos light y dark."
    - "El usuario ha elegido UNA de las 3 direcciones (A: Research notebook, B: Tactical cockpit, o C: Claude-like minimal)."
    - "La sección '## Recommendation & decision' de .planning/ux/03-redesign-proposal.md está rellenada con la dirección elegida y rationale del usuario."
    - "Si durante la revisión emergen ajustes obligatorios para Phase 4 (e.g. swap de tipografía, cambio de un token concreto), quedan registrados en la misma sección como 'Adjustments before Phase 4 implementation'."
  artifacts:
    - path: ".planning/ux/03-redesign-proposal.md"
      provides: "Documento del Plan 03 con la sección final 'Recommendation & decision' rellenada (ya no vacía)."
      contains:
        - "## Recommendation & decision"
        - "**Selected direction:**"
        - "**Rationale:**"
        - "**Adjustments before Phase 4 implementation:**"
  key_links:
    - from: ".planning/ux/03-redesign-proposal.md § Recommendation & decision"
      to: "Phase 4 (consumidor)"
      via: "Phase 4 lee la dirección elegida + ajustes y los implementa sin volver a decidir."
      pattern: "Selected direction:"
---

<objective>
Cerrar la fase de UX Research & Design Brief: el usuario revisa los 3 wireframes ASCII (Plan 04) + los 3 HTML sketches en light y dark (Plan 05), elige UNA dirección tonal, y registra la decisión + rationale + ajustes obligatorios para Phase 4 en la sección `## Recommendation & decision` del documento `03-redesign-proposal.md` que el Plan 03 dejó vacía a propósito.

Purpose: Phase 4 está obligada a ejecutar SIN tomar decisiones visuales nuevas (constraint del proyecto: "no design decisions to make in Phase 4"). Esa garantía solo se cumple si Phase 2 termina con UNA dirección elegida y registrada por escrito. Sin Plan 06, Phase 4 no puede arrancar.

Output: Update in-place de `.planning/ux/03-redesign-proposal.md` — la sección `## Recommendation & decision` deja de estar vacía y contiene la decisión final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ux-research-design-brief/02-CONTEXT.md
@.planning/ux/01-cognitive-walkthrough.md
@.planning/ux/02-nielsen-audit.md
@.planning/ux/03-redesign-proposal.md
@.planning/ux/04-mockups/wireframes.md
@.planning/ux/04-mockups/sketch-notebook.html
@.planning/ux/04-mockups/sketch-cockpit.html
@.planning/ux/04-mockups/sketch-minimal.html
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1 (checkpoint): Usuario elige una de las 3 direcciones tonales</name>
  <decision>
    ¿Cuál de las 3 direcciones tonales propuestas en `.planning/ux/03-redesign-proposal.md` será la dirección visual oficial que Phase 4 implementará?
  </decision>
  <context>
    Phase 2 ha entregado:
    - **Cognitive walkthrough** con friction points concretos del UI actual.
    - **Nielsen audit** con findings ≥3 de severidad (priority-fix para Phase 4) y anticipatory findings sobre Quality toggle / reasoning_details.
    - **Redesign proposal** con 3 direcciones contrastadas (paleta + tipografía + microinteracciones + IA + density stance + cost surfacing + Phase 1 components restyling notes), cada una mapeada a friction points y findings concretos.
    - **Wireframes ASCII** neutros estructuralmente — el contrato estructural común a las 3 direcciones (cobertura D-13 al 100%).
    - **3 HTML sketches one-page** con light + dark, cada uno materializando UNA dirección tonal con paleta y tipografía reales.

    El usuario debe abrir los 3 archivos HTML y alternar light/dark en cada uno antes de decidir. La decisión NO se toma leyendo solo el redesign proposal — se toma viendo los sketches lado a lado.

    **Constraint dura:** Phase 4 NO debe tomar decisiones visuales nuevas. Toda decisión visible debe estar resuelta aquí. Si durante la revisión el usuario detecta que algo del sketch elegido no convence (ej: la tipografía es difícil de leer en dark mode), DEBE registrarse como "Adjustment before Phase 4 implementation" en la misma sección — no como TODO de Phase 4.

    **Reversibilidad:** La decisión es reversible solo si Phase 4 aún no ha implementado ningún token. Una vez Phase 4 arranca, cambiar de dirección implica replanificar Phase 4. Por eso esta decisión bloquea el cierre de Phase 2.
  </context>
  <how-to-verify>
    Pasos exactos para revisar antes de decidir:

    1. Abrir `.planning/ux/03-redesign-proposal.md` y leer las 3 secciones `## Direction A/B/C` completas (paleta, tipografía, density stance, cost surfacing, maps to friction).
    2. Abrir `.planning/ux/04-mockups/wireframes.md` y revisar al menos los wireframes W04 (three-dot menu), W09 (modal), W15 (Quality+Research toggle), W16/W17 (reasoning disclosure colapsado/expandido), W18 (Stage 2 densidad), W23 (loading state) — son los más diferenciadores entre direcciones.
    3. Abrir los 3 sketches HTML en el navegador (doble click sobre cada `.html`):
       - `.planning/ux/04-mockups/sketch-notebook.html`
       - `.planning/ux/04-mockups/sketch-cockpit.html`
       - `.planning/ux/04-mockups/sketch-minimal.html`
    4. En cada sketch, alternar el theme toggle (botón en esquina superior derecha) entre light y dark. Verificar que ambos modos se ven coherentes.
    5. Comparar lado a lado:
       - ¿Qué dirección hace mejor el surfacing del coste en `Quality+Research`?
       - ¿Qué dirección presenta Stage 2 (densidad informativa) de la forma que más te ayuda a leer los aggregate rankings?
       - ¿Qué dirección hace que el welcome state / cold start (W01) se sienta como una herramienta personal y no como producto comercial?
       - ¿Cuál de las dos paletas (light/dark) usarás más en tu día a día? La dirección elegida debe brillar especialmente en ese modo.
    6. Decidir UNA dirección. No es necesario justificar exhaustivamente — basta con 2–4 frases de rationale en el documento.
  </how-to-verify>
  <options>
    <option id="direction-a">
      <name>Direction A: Research notebook</name>
      <pros>Calmo, claro, generoso en whitespace. Mejor para lectura larga (Stage 2/3). Inspiración Linear / Notion / Obsidian / Are.na. Cost surfacing como nota al pie del label — discreto pero presente.</pros>
      <cons>Densidad media puede sentirse insuficiente en sesiones donde se comparan muchos rankings agregados de Stage 2. Si el usuario tiene perfil BI/data y le interesa ver costes prominentes en cada query, esta dirección los esconde más que las otras dos.</cons>
    </option>
    <option id="direction-b">
      <name>Direction B: Tactical cockpit</name>
      <pros>Denso, info-rico, terminal-flavored. Encaja con perfil BI/data (numbers-first). Cost surfacing como chip cromado con número grande — el coste es lo primero que ves en el toggle. Inspiración Raycast / Datadog. Aggregate rankings y costes con mono variant.</pros>
      <cons>Puede sentirse fatigante para sesiones largas de lectura de Stage 2/3 si el usuario está acostumbrado a uso prolongado. Light mode puede salir menos pulido que dark mode si la dirección está optimizada para "natural habitat dark".</cons>
    </option>
    <option id="direction-c">
      <name>Direction C: Claude-like minimal</name>
      <pros>Silencioso, ligero, una decisión a la vez. Inspiración claude.ai / Anthropic / Vercel docs. Tipografía generosa. Microinteracciones discretas. Excelente para concentración profunda en una respuesta sin distracciones laterales. Cost surfacing como subtle subtitle — presente pero no dominante.</pros>
      <cons>Cost surfacing discreto puede ser un problema si el usuario quiere coste muy visible para decisiones conscientes en `Quality+Research` (constraint PROJECT.md). Densidad baja puede esconder los aggregate rankings de Stage 2 — si el flujo de comparación de modelos es central, esta dirección lo subutiliza.</cons>
    </option>
    <option id="adjust-and-pick">
      <name>Híbrido: elijo una pero con ajustes obligatorios</name>
      <pros>Resuelve el caso de "me gusta dirección X pero el cost surfacing de Y es mejor" o "me gusta la paleta light de A pero la dark de C". Permite cerrar Phase 2 sin volver a Plan 03.</pros>
      <cons>Los ajustes deben estar bien delimitados (token concreto, microinteracción concreta) — si requieren re-arquitectura tonal, mejor volver a Plan 03 con `/gsd-plan-phase --gaps`.</cons>
    </option>
    <option id="re-explore">
      <name>Ninguna convence — re-explorar Plan 03</name>
      <pros>Honesto si las 3 direcciones convergieron involuntariamente o ninguna captura el tono deseado.</pros>
      <cons>Bloquea Phase 4. Implica replanificar Plan 03 con `/gsd-plan-phase 02 --gaps`. Solo elegir si las 3 direcciones genuinamente NO funcionan; no como reflejo de "más opciones es mejor".</cons>
    </option>
  </options>
  <resume-signal>
    Indicar al ejecutor:
    - **Si elige direction-a / direction-b / direction-c:** confirmar el ID + 2-4 frases de rationale + lista (puede ser vacía) de "Adjustments before Phase 4 implementation".
    - **Si elige adjust-and-pick:** indicar la dirección base + lista numerada de ajustes específicos (e.g. "1. Cambiar la tipografía body de Direction A a la de Direction C porque rinde mejor en mi pantalla", "2. Adoptar el cost chip de Direction B en el toggle Quality+Research").
    - **Si elige re-explore:** indicar qué dimensión faltó (paleta? tipografía? IA?) para que el orchestrator pueda invocar `/gsd-plan-phase 02 --gaps` con scope claro.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrar la decisión en .planning/ux/03-redesign-proposal.md § Recommendation & decision</name>
  <files>.planning/ux/03-redesign-proposal.md</files>
  <read_first>
    - .planning/ux/03-redesign-proposal.md (leer la sección actual `## Recommendation & decision` que el Plan 03 dejó vacía con el marcador `_(empty — to be filled by Plan 06 after the user reviews wireframes and HTML sketches)_`).
    - El input del usuario del checkpoint anterior (Task 1) — direction ID + rationale + adjustments.
  </read_first>
  <action>
    Sobrescribir la sección `## Recommendation & decision` del documento `.planning/ux/03-redesign-proposal.md` con el siguiente template, rellenado con los inputs del usuario:

    ```markdown
    ## Recommendation & decision

    **Decision date:** YYYY-MM-DD (fecha real del momento de ejecución del plan)
    **Decision recorded by:** Plan 02-06 (Phase 2 closure)

    **Selected direction:** Direction X — <Nombre completo, ej. "Research notebook">

    **Rationale:**

    <2-4 frases del usuario explicando por qué elige esta dirección. Si el usuario apunta a un sketch HTML específico o a un wireframe específico como evidencia de la decisión, citarlo>.

    **Adjustments before Phase 4 implementation:**

    <Lista numerada de ajustes obligatorios. Si el usuario no aporta ajustes, escribir literal: "_None — Phase 4 implementa la dirección elegida tal como queda definida en `## Direction X` arriba._">

    1. <Ajuste 1, descripción concreta y testeable. Ej: "Cambiar `--color-accent-primary` en light mode de #B85C38 a #A04E2F para mejorar contraste sobre `--color-bg-secondary`.">
    2. <Ajuste 2 si aplica>
    ...

    **Phase 4 entry contract:**

    Phase 4 lee:
    - Esta sección (`## Recommendation & decision`).
    - La sección `## Direction X` correspondiente (paleta, tipografía, microinteracciones, IA, density stance, cost surfacing, Phase 1 components restyling notes).
    - El documento `.planning/ux/04-mockups/wireframes.md` (estructura común).
    - El sketch `.planning/ux/04-mockups/sketch-<dirección>.html` SOLO como referencia visual — NO como código a copiar (recordatorio del `## Throwaway HTML disclaimer`).

    Phase 4 NO necesita re-decidir nada visual. Si durante Phase 4 emerge una decisión visual no resuelta aquí, ese es señal de gap y se cierra con `/gsd-plan-phase 04 --gaps` referenciando este documento.
    ```

    Restricciones obligatorias:
    - **NO sobrescribir el resto del documento.** Solo la sección `## Recommendation & decision`. Las secciones `## Direction A/B/C`, `## Inputs`, `## Convergence risk check`, `## Throwaway HTML disclaimer` deben quedar intactas.
    - **El marcador literal `_(empty — to be filled by Plan 06 after the user reviews wireframes and HTML sketches)_` debe desaparecer** del documento — ya no aplica.
    - **Si el usuario eligió `adjust-and-pick`:** el campo `**Selected direction:**` lleva la dirección base, y los ajustes en `**Adjustments before Phase 4 implementation:**` listan los cambios específicos.
    - **Si el usuario eligió `re-explore`:** este task NO se ejecuta — el orchestrator debe redirigir a `/gsd-plan-phase 02 --gaps` antes de cerrar Phase 2. Documentar en SUMMARY que Phase 2 queda en estado "re-exploration requested".
    - Idioma: español para la prosa, headings en inglés (críticos para grep).
  </action>
  <verify>
    <automated>test -f .planning/ux/03-redesign-proposal.md &amp;&amp; grep -q "^## Recommendation &amp; decision" .planning/ux/03-redesign-proposal.md &amp;&amp; grep -q "\*\*Selected direction:\*\*" .planning/ux/03-redesign-proposal.md &amp;&amp; grep -q "\*\*Rationale:\*\*" .planning/ux/03-redesign-proposal.md &amp;&amp; grep -q "\*\*Adjustments before Phase 4 implementation:\*\*" .planning/ux/03-redesign-proposal.md &amp;&amp; ! grep -q "empty — to be filled by Plan 06" .planning/ux/03-redesign-proposal.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `.planning/ux/03-redesign-proposal.md` sigue existiendo (no se eliminó).
    - La sección `## Recommendation & decision` existe y contiene los 3 sub-campos en bold: `**Selected direction:**`, `**Rationale:**`, `**Adjustments before Phase 4 implementation:**`.
    - La cadena marcador `empty — to be filled by Plan 06` ha sido eliminada del documento.
    - El campo `**Selected direction:**` referencia exactamente una de las 3 direcciones por nombre completo: `Research notebook`, `Tactical cockpit`, o `Claude-like minimal`.
    - Existe la sub-sección `**Phase 4 entry contract:**` con las referencias a los archivos que Phase 4 debe leer.
    - Las secciones `## Direction A`, `## Direction B`, `## Direction C` siguen presentes en el documento (no fueron borradas accidentalmente) — verificable con `grep -c "^## Direction [ABC]:"` ≥ 3.
    - Si el usuario eligió `re-explore`, este task fue saltado y SUMMARY lo documenta.
  </acceptance_criteria>
  <done>
    `.planning/ux/03-redesign-proposal.md § Recommendation & decision` rellenada con la dirección elegida, rationale, ajustes (si aplica), y el contrato de entrada de Phase 4. Phase 2 cerrada y Phase 4 desbloqueada.
  </done>
</task>

</tasks>

<threat_model>
N/A — Plan 06 modifica únicamente una sección de un documento markdown bajo `.planning/ux/`. El task 1 es un checkpoint humano (decisión, no código). Sin runtime, sin endpoints, sin código nuevo. La política `security_enforcement` se satisface por exclusión documentada.
</threat_model>

<verification>
- Existe la sección `## Recommendation & decision` rellenada (no vacía).
- Una de las 3 direcciones está seleccionada por nombre.
- El documento mantiene las 3 secciones `## Direction A/B/C` intactas.
- Si hay ajustes, están listados de forma testeable.
</verification>

<success_criteria>
Phase 2 cierra con UNA dirección elegida + rationale + ajustes opcionales en escrito. Phase 4 puede arrancar leyendo `## Direction X` + `## Recommendation & decision` + `wireframes.md` sin tomar decisiones visuales nuevas. ROADMAP success criteria #3 y #4 de Phase 2 cierran al 100%.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/02-ux-research-design-brief/02-06-SUMMARY.md` con:
- Dirección elegida + 2-4 frases de rationale.
- Lista de ajustes obligatorios (puede ser vacía).
- Confirmación de que el marcador `empty — to be filled by Plan 06` ha desaparecido del documento.
- Si el usuario eligió `re-explore`: documentar el motivo y que la fase queda en estado "re-exploration requested" antes de cerrar.
- Próximo paso para el milestone: ejecutar `/gsd-verify-phase 2` para cerrar Phase 2 oficialmente, luego `/gsd-execute-phase 3`.
</output>
