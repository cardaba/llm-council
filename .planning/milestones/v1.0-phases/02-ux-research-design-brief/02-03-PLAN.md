---
phase: 02-ux-research-design-brief
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - .planning/ux/03-redesign-proposal.md
autonomous: true
requirements:
  - UXR-03
must_haves:
  truths:
    - "Existe una redesign proposal bajo .planning/ux/ que explora 3 direcciones tonales contrastadas (Research notebook, Tactical cockpit, Claude-like minimal)."
    - "Cada dirección incluye paleta light+dark, sistema tipográfico (1–2 familias con rationale), microinteracciones básicas, IA principal y stance sobre densidad."
    - "Cada dirección tiene filosofía de paleta propia (D-10) — no es el mismo sistema con distintos hues."
    - "El rationale de cada dirección referencia friction points del walkthrough y findings del audit."
    - "La sección final 'Recommendation & decision' queda vacía (la rellena Plan 06)."
  artifacts:
    - path: ".planning/ux/03-redesign-proposal.md"
      provides: "Propuesta de redesign con 3 direcciones tonales en paralelo y rationale anclado en walkthrough+audit."
      contains:
        - "## Direction A: Research notebook"
        - "## Direction B: Tactical cockpit"
        - "## Direction C: Claude-like minimal"
        - "### Palette (light)"
        - "### Palette (dark)"
        - "### Typography"
        - "### Microinteractions"
        - "### Information architecture"
        - "### Density stance"
        - "### Cost surfacing in Quality toggle"
        - "## Recommendation & decision"
        - "(empty until Plan 06)"
  key_links:
    - from: ".planning/ux/03-redesign-proposal.md"
      to: ".planning/ux/01-cognitive-walkthrough.md"
      via: "Cada dirección referencia al menos 3 friction IDs (F-XX) que resuelve."
      pattern: "F-[0-9]+"
    - from: ".planning/ux/03-redesign-proposal.md"
      to: ".planning/ux/02-nielsen-audit.md"
      via: "Cada dirección referencia al menos 3 findings Nielsen (HN-XX) que mejora."
      pattern: "H[0-9]+-[0-9]+"
---

<objective>
Producir la propuesta de redesign con 3 direcciones tonales contrastadas — `Research notebook`, `Tactical cockpit`, `Claude-like minimal` — explorándolas en paralelo, cada una con paleta (light + dark), tipografía con rationale, microinteracciones, IA, stance sobre densidad, y tratamiento explícito del cost surfacing en el Quality toggle. La sección final `Recommendation & decision` se deja vacía y la rellena Plan 06 cuando el usuario revise wireframes + sketches HTML.

Purpose: Es el contrato visual sobre el que se construyen Plan 04 (wireframes) y Plan 05 (HTML sketches). Sin el redesign no hay forma de saber qué pintar en los mockups.

Output: `.planning/ux/03-redesign-proposal.md` — un único documento markdown con tres sub-secciones paralelas + sección de recomendación vacía.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ux-research-design-brief/02-CONTEXT.md
@.planning/ux/01-cognitive-walkthrough.md
@.planning/ux/02-nielsen-audit.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@frontend/src/components/Sidebar.jsx
@frontend/src/components/ChatInterface.jsx
@frontend/src/components/Stage2.jsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign proposal con 3 direcciones tonales paralelas (notebook / cockpit / minimal)</name>
  <files>.planning/ux/03-redesign-proposal.md</files>
  <read_first>
    - .planning/phases/02-ux-research-design-brief/02-CONTEXT.md (D-06 explorar 3 direcciones; D-07 nombres exactos de las direcciones; D-08 brutalist editorial está RECHAZADO; D-09 contenido por dirección; D-10 cada dirección tiene filosofía de paleta propia; D-11 Recommendation & decision queda vacía aquí; D-16 light+dark ambos canónicos desde Phase 2; D-17 referencias por dirección; D-18 tipografía/paleta delegada al ui-ux-designer agent con criterios; D-19 re-stylar componentes Phase 1; D-20 single-shot input preservado; CD-02 cómo se renderiza el coste estimado por dirección; specifics — los 3 ejemplos de cómo cada dirección surface el coste).
    - .planning/REQUIREMENTS.md (UXR-03 skill ui-ux-designer; QUAL-01..04 contexto Quality toggle; RSCH-05 reasoning_details; VIS-01..04 son los CONSUMIDORES — la fidelidad debe ser suficiente para que VIS-* se implementen sin volver a decidir).
    - .planning/ux/01-cognitive-walkthrough.md (Friction Index — la propuesta debe declarar cuáles friction resuelve cada dirección).
    - .planning/ux/02-nielsen-audit.md (Severity Summary — los priority-fix ≥3 deben quedar mitigados por las 3 direcciones; las anticipatory findings de QUAL-03 y RSCH-05 deben estar tratadas explícitamente).
    - .planning/codebase/CONVENTIONS.md (CSS co-located + .markdown-content global + kebab-case — los tokens propuestos deben encajar con este patrón, no proponer Tailwind/MUI/Bootstrap).
    - frontend/src/components/Sidebar.jsx (la superficie con más estados a mockear; las 3 direcciones deben proponer su tratamiento).
    - frontend/src/components/Stage2.jsx (la surface con mayor densidad informacional — clave para diferenciar cockpit vs minimal).
  </read_first>
  <action>
    Invocar la skill global `ui-ux-designer` para producir un único documento `.planning/ux/03-redesign-proposal.md` con la siguiente estructura:

    1. **Encabezado:**
       - Título: "Redesign Proposal — LLM Council Visual Direction (Phase 2)".
       - Resumen ejecutivo de 5–8 líneas: por qué 3 direcciones, por qué esas 3 (D-07), por qué brutalist editorial está descartado (D-08), y cómo se va a tomar la decisión final (Plan 06 — usuario revisa wireframes + sketches HTML).

    2. **Sección `## Inputs`:**
       - Subsección `### Friction points addressed (from walkthrough)` — lista de los friction F-XX más críticos del Plan 01.
       - Subsección `### Priority fixes (from Nielsen audit)` — lista de findings HN-XX con severidad ≥3 del Plan 02.
       - Subsección `### Anticipatory surfaces (Phase 3)` — Quality toggle (cost surfacing per CD-02), reasoning_details disclosure colapsado.
       - Subsección `### Constraints` — single-user local app, light+dark ambos canónicos, sin licencia tipográfica preexistente, CSS vanilla / CSS variables / co-located (no Tailwind, no MUI, no Bootstrap), single-shot input preservado, header branded sin logo corporativo (tono producto personal).

    3. **Tres secciones de dirección, en este orden y con headings exactos** (críticos para grep — incluye letra `A/B/C` para que el grep distinga cuál es cuál):
       - `## Direction A: Research notebook`
       - `## Direction B: Tactical cockpit`
       - `## Direction C: Claude-like minimal`

    4. **Estructura idéntica DENTRO de cada dirección**, con estos sub-headings (todos obligatorios y todos en cada dirección):
       - `### Tone & references` — referencias D-17 (Notebook → Linear, Notion, Obsidian, Are.na | Cockpit → Raycast, Datadog, herramientas SRE | Minimal → claude.ai, Anthropic.com, Vercel docs). Una frase explicando qué se toma de cada referencia.
       - `### Palette (light)` — paleta light en formato tabla con: token name (kebab-case, ej `--color-bg-primary`), valor hex/oklch, rol semántico, contrast ratio sobre el bg principal cuando aplique. Mínimo 8 tokens (bg-primary, bg-secondary, fg-primary, fg-secondary, fg-muted, accent, border, focus-ring). La filosofía de paleta debe ser propia per D-10 (Notebook = neutros calmos + 1 acento cálido | Cockpit = oscuros saturados + acentos cromados/neón discretos | Minimal = arenas + un acento muy pulido).
       - `### Palette (dark)` — espejo de la light con mismos tokens y la misma filosofía aplicada al modo oscuro. NO es la light invertida; es un sistema dark coherente con la dirección.
       - `### Typography` — 1 o 2 familias propuestas con rationale para cada una: legibilidad para lectura larga (Stage 2/3 son 90% texto), peso del archivo, soporte de glifos (acentos español + bullets/tablas markdown), licencia (open-source preferido). Tabla de roles tipográficos: display, h1..h3, body, body-small, code, label. Referencias concretas a familias reales (Inter, IBM Plex Sans/Mono, Söhne, JetBrains Mono, Newsreader, Spectral, etc.).
       - `### Microinteractions` — al menos 4 microinteracciones definidas: (a) hover de botones / sidebar items, (b) transición Stage 1 → 2 → 3 (los gaps de 15-30s entre stages), (c) hover del Quality toggle, (d) expansión del reasoning_details disclosure. Cada una con duración (ms) y curva (ease-in-out, cubic-bezier(...)).
       - `### Information architecture` — descripción de la estructura del shell: ancho del sidebar, posición/altura del header branded, layout del main panel cuando hay conversación activa, layout del welcome state, comportamiento responsive básico (la app es desktop-first; mobile no es priority). Incluir un ASCII rápido del layout de una página típica para anclar la idea.
       - `### Density stance` — declaración explícita de cuán densa es la información en esta dirección. Cockpit = alta densidad, Notebook = densidad media (whitespace generoso pero información completa), Minimal = baja densidad (una decisión a la vez).
       - `### Cost surfacing in Quality toggle` — cómo esta dirección hace visible el coste estimado en el toggle de Quality (CD-02 + specifics). Notebook ≈ nota al pie (`Quality+Research · ~$0.45 typical`); Cockpit ≈ chip cromado con número grande; Minimal ≈ subtitle sutil bajo el label. Cada dirección debe ser FIEL a su tono — no se puede copiar el patrón de otra.
       - `### Phase 1 components restyling notes` — D-19: cómo esta dirección re-estiliza Modal, Menu, three-dot icon (`⋮`), inline rename input, search input, affordance "Search inside content (N conversations)". Una línea por componente con la decisión visual clave.
       - `### Maps to friction & audit findings` — bullet list que conecta esta dirección con: (a) ≥3 friction IDs F-XX del walkthrough, (b) ≥3 findings Nielsen HN-XX del audit. Esto es lo que justifica que la dirección es research-backed y no estética.

    5. **Sección `## Convergence risk check` (D-06 + CD-05):**
       - Tres bullets cortos respondiendo: ¿la dirección A converge visualmente con B? ¿B con C? ¿A con C? Si en algún caso la respuesta es "sí parcialmente", el agent debe re-orientar la dirección AHORA o registrar por escrito que se reduce el set a 2 con rationale (CD-05). El default esperado es que las 3 contrastan; si convergen, ese es un problema a resolver en el propio Plan 03, no a postergar.

    6. **Sección `## Recommendation & decision` (D-11):**
       Heading exacto `## Recommendation & decision`. Cuerpo: una sola línea literal — `_(empty — to be filled by Plan 06 after the user reviews wireframes and HTML sketches)_`. Esa cadena debe aparecer literal para que el grep de Plan 06 pueda detectar el slot vacío.

    7. **Sección `## Throwaway HTML disclaimer` (D-15):**
       Una sub-sección breve aclarando que los HTML sketches del Plan 05 son DELIBERADAMENTE desechables — Phase 4 NO debe lift CSS verbatim de los sketches; solo extrae paleta, tipografía y microinteracciones del documento canónico (este redesign + los wireframes). Esto previene el anti-patrón "se copia el sketch 1:1 a producción".

    Restricciones obligatorias:
    - **3 direcciones, no 1** (D-06). Las 3 deben tener TODA la estructura (sub-headings completos), no solo la primera.
    - **Filosofía de paleta propia por dirección** (D-10) — el agent debe explicitar la filosofía en `### Palette (light)` antes de la tabla, en una línea.
    - **Light + Dark ambos canónicos** (D-14, D-16) — no es válido entregar solo light.
    - **Cada dirección debe mapear a friction y findings reales**: ≥3 F-XX y ≥3 HN-XX en `### Maps to friction & audit findings`. Si Plan 01 o Plan 02 no produjeron suficientes IDs, registrar el gap aquí en lugar de inventar.
    - **El single-shot input se preserva** (D-20). Ninguna dirección debe proponer multi-turn.
    - **Brutalist editorial está descartado** (D-08) — no aparecer como dirección ni como referencia.
    - **No proponer Tailwind / MUI / Bootstrap.** Los tokens propuestos asumen CSS variables vanilla en `frontend/src/index.css` y CSS co-located por componente (CONVENTIONS.md).
    - **`## Recommendation & decision` queda vacía** con la cadena literal `_(empty — to be filled by Plan 06 after the user reviews wireframes and HTML sketches)_` para que Plan 06 detecte el slot.
    - **Idioma:** español para la prosa, headings exactos en inglés (críticos para grep), nombres de tokens CSS en kebab-case inglés.
  </action>
  <verify>
    <automated>test -f .planning/ux/03-redesign-proposal.md &amp;&amp; grep -q "^## Direction A: Research notebook" .planning/ux/03-redesign-proposal.md &amp;&amp; grep -q "^## Direction B: Tactical cockpit" .planning/ux/03-redesign-proposal.md &amp;&amp; grep -q "^## Direction C: Claude-like minimal" .planning/ux/03-redesign-proposal.md &amp;&amp; grep -c "^### Palette (light)" .planning/ux/03-redesign-proposal.md | grep -q "^3$" &amp;&amp; grep -c "^### Palette (dark)" .planning/ux/03-redesign-proposal.md | grep -q "^3$" &amp;&amp; grep -c "^### Typography" .planning/ux/03-redesign-proposal.md | grep -q "^3$" &amp;&amp; grep -c "^### Cost surfacing in Quality toggle" .planning/ux/03-redesign-proposal.md | grep -q "^3$" &amp;&amp; grep -q "^## Recommendation &amp; decision" .planning/ux/03-redesign-proposal.md &amp;&amp; grep -q "empty — to be filled by Plan 06" .planning/ux/03-redesign-proposal.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `.planning/ux/03-redesign-proposal.md` existe.
    - Contiene los 3 headings de dirección con texto literal exacto: `## Direction A: Research notebook`, `## Direction B: Tactical cockpit`, `## Direction C: Claude-like minimal`.
    - Cada uno de los sub-headings `### Palette (light)`, `### Palette (dark)`, `### Typography`, `### Microinteractions`, `### Information architecture`, `### Density stance`, `### Cost surfacing in Quality toggle`, `### Phase 1 components restyling notes`, `### Maps to friction & audit findings` aparece exactamente 3 veces (uno por dirección).
    - Cada paleta light/dark tiene al menos 8 tokens con valores hex/oklch concretos.
    - Cada dirección menciona al menos un nombre de familia tipográfica real (regex `Inter|IBM Plex|Söhne|Newsreader|Spectral|JetBrains|DM Sans|Geist|Space Grotesk` o similar).
    - Cada dirección menciona al menos 3 friction IDs (`F-XX`) y 3 findings Nielsen (`HN-XX`) en `### Maps to friction & audit findings`.
    - Existe la sección `## Convergence risk check` con análisis A↔B / B↔C / A↔C.
    - La sección `## Recommendation & decision` existe y contiene literalmente la cadena `empty — to be filled by Plan 06 after the user reviews wireframes and HTML sketches`.
    - La cadena literal `Brutalist editorial` NO aparece como heading (verificable: el doc puede mencionarla en una nota explicando por qué se descartó, pero no debe estar como `## Direction X: Brutalist editorial`).
    - El doc NO propone Tailwind, MUI ni Bootstrap como sistema (verificable con `grep -i "tailwind\\|mui\\|bootstrap"` — si aparece, debe ser solo en contexto negativo "no usamos Tailwind/MUI/Bootstrap").
    - El doc menciona explícitamente `single-shot` o `single shot` para confirmar que el patrón se preserva (D-20).
    - Existe la sección `## Throwaway HTML disclaimer`.
  </acceptance_criteria>
  <done>
    Documento `.planning/ux/03-redesign-proposal.md` commiteado con las 3 direcciones completas, ambos modos light+dark, rationale anclado en walkthrough y audit, sección Recommendation & decision vacía esperando a Plan 06, y disclaimer del throwaway escrito.
  </done>
</task>

</tasks>

<threat_model>
N/A — Plan 03 produce únicamente un documento markdown bajo `.planning/ux/`. Sin runtime, sin endpoints, sin código. La política `security_enforcement` se satisface por exclusión documentada.
</threat_model>

<verification>
- Existe `.planning/ux/03-redesign-proposal.md` con las 3 direcciones completas.
- Cada dirección tiene los 9 sub-headings obligatorios.
- Light + Dark presentes en cada una.
- Maps to friction & findings con IDs reales (≥3 F-XX y ≥3 HN-XX por dirección).
- Recommendation & decision vacía con marcador detectable por Plan 06.
</verification>

<success_criteria>
Plan 04 (wireframes) y Plan 05 (HTML sketches) tienen un contrato visual claro sobre las 3 direcciones, con paleta y tipografía decididas por dirección. ROADMAP success criterion #3 de Phase 2 satisfecho. La sección `Recommendation & decision` queda como hook explícito para Plan 06.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/02-ux-research-design-brief/02-03-SUMMARY.md` con:
- Resumen de las 3 direcciones (1 línea por dirección).
- Conteo de tokens de paleta por dirección × modo.
- Familias tipográficas finales propuestas por dirección.
- Lista de friction IDs y finding IDs cubiertos por cada dirección.
- Notas sobre convergence risk check (¿alguna dirección quedó re-orientada?).
</output>
