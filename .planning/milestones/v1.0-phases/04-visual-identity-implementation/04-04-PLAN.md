---
phase: 04-visual-identity-implementation
plan: 04
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - frontend/src/components/Sidebar.css
  - frontend/src/components/Sidebar.jsx
  - frontend/src/components/Modal.css
  - frontend/src/components/Menu.css
  - frontend/src/components/ErrorBanner.jsx
  - frontend/src/components/ErrorBanner.css
  - frontend/src/App.jsx
  - frontend/src/App.css
  - frontend/public/favicon-ampersand.svg
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
must_haves:
  truths:
    - "El usuario abre la app por primera vez (sin conversaciones) y ve un sidebar empty state con un ampersand serif gigante (≥96px) + el body 'No conversations yet. Start one to see it here.'"
    - "El usuario renombra una conversación inline y ve el hint microcopy `Enter para guardar · Esc para cancelar` debajo del input."
    - "El usuario abre el menú de tres puntos en una conversación activa y ve los items 'Rename' (con shortcut hint `R`) y 'Delete' (con shortcut hint `⌫`)."
    - "El usuario abre el modal de delete y ve el body con metadata: `\"{title}\" · N messages · {time_ago}`."
    - "Si la deliberación falla en mid-stream, el usuario ve un error banner persistente arriba del main panel con copy `La deliberación se interrumpió en Stage {N}. ¿Quieres reintentar con la misma pregunta?` + botones Retry y Dismiss."
    - "El favicon del browser tab muestra el ampersand serif (no el de Vite)."
  artifacts:
    - path: "frontend/src/components/Sidebar.css"
      provides: "Sidebar layout (280px column 2 row) + items + active row indicator + search input + empty state ampersand mark — tokens only"
    - path: "frontend/src/components/Modal.css"
      provides: "Modal styles (backdrop + card with shadow-md + radius-md) — tokens"
    - path: "frontend/src/components/Menu.css"
      provides: "Menu popover (radius-sm + shadow-sm + accent-soft hover) — tokens"
    - path: "frontend/src/components/ErrorBanner.jsx"
      provides: "Persistent error banner with retry + dismiss (resolves H9-01)"
      exports: ["default ErrorBanner"]
      min_lines: 25
    - path: "frontend/src/components/ErrorBanner.css"
      provides: "Banner styles (color-error tint background + border-top + retry button) — tokens"
    - path: "frontend/public/favicon-ampersand.svg"
      provides: "Favicon = SVG ampersand serif (used by index.html link rel=icon)"
  key_links:
    - from: "Sidebar.jsx empty-state branch"
      to: "ampersand SVG mark ≥96px"
      via: "sidebar__empty-mark"
      pattern: "sidebar__empty"
    - from: "App.jsx error state"
      to: "ErrorBanner component"
      via: "rendered above ChatInterface when streamError state truthy"
      pattern: "<ErrorBanner"
    - from: "frontend/index.html link rel=icon"
      to: "frontend/public/favicon-ampersand.svg"
      via: "static asset served by Vite from /public"
      pattern: "favicon-ampersand"
---

<objective>
Wave 4 — Conversations & polish. Cierra el 20% restante del Phase 4: sidebar (con empty state ampersand gigante + active-row indicator + inline rename hint microcopy), modal + menu (consumiendo tokens, con shortcut hints en menu items), `ErrorBanner` componente nuevo persistente para H9-01 (catastrophic interruption), y el favicon ampersand SVG.

Tras esta wave: TODOS los componentes consumen tokens, la app está completamente alineada con Direction A en light + dark, y los 23 wireframes (W01-W23) están materializados.

Purpose: Cerrar VIS-01 + VIS-02 + VIS-03 + VIS-04 al 100% — todos los grep gates Wave D del UI-SPEC pasan, todos los wireframes coverage commitments del UI-SPEC §IA satisfechos.
Output: 3 .css migrados (Sidebar, Modal, Menu) + 1 .jsx editado (Sidebar — empty state + rename hint) + 2 archivos nuevos (ErrorBanner.jsx + .css) + App.jsx wiring del banner + 1 binario nuevo (favicon-ampersand.svg).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-visual-identity-implementation/04-CONTEXT.md
@.planning/phases/04-visual-identity-implementation/04-RESEARCH.md
@.planning/phases/04-visual-identity-implementation/04-UI-SPEC.md
@.planning/phases/04-visual-identity-implementation/04-01-PLAN.md
@.planning/phases/04-visual-identity-implementation/04-02-PLAN.md
@.planning/phases/04-visual-identity-implementation/04-03-PLAN.md
@.planning/codebase/CONVENTIONS.md
@./CLAUDE.md
@frontend/src/components/Sidebar.jsx
@frontend/src/components/Sidebar.css
@frontend/src/components/Modal.jsx
@frontend/src/components/Modal.css
@frontend/src/components/Menu.jsx
@frontend/src/components/Menu.css
@frontend/src/App.jsx

<interfaces>
<!-- Locked specs for Wave 4. -->

**Sidebar empty state (UI-SPEC §Copywriting Contract line 228-229):**
- No heading.
- Big `&` ampersand mark in `var(--font-serif)` weight 400 color `var(--color-fg-secondary)` size ≥96px (D-15: "96-128px").
- Body copy: `No conversations yet. Start one to see it here.` in `var(--font-sans)` `var(--font-size-body-small)` color `var(--color-fg-muted)`.

**Sidebar active row (UI-SPEC §Color Allowlist item 3):**
- `border-left: 3px solid var(--color-accent)` indicator
- Slight `var(--color-accent-soft)` background tint on the active row.

**Inline rename hint (UI-SPEC §Copywriting Contract line 232):**
- Below the rename input (which is part of `Sidebar.jsx` ConversationItem already): show microcopy `Enter para guardar · Esc para cancelar` in `var(--font-size-microcopy)` color `var(--color-fg-muted)`.

**Menu items copy (UI-SPEC §Copywriting Contract lines 261-262):**
- Item 1: label "Rename", right-aligned shortcut hint "R" in `var(--color-fg-muted)`.
- Item 2: label "Delete", right-aligned shortcut hint "⌫" (U+232B) in `var(--color-fg-muted)`.

**Modal copy (UI-SPEC §Copywriting Contract lines 257-260):**
- Title: `Delete this conversation?`
- Body: `"{conversation_title}" · {N} messages · {time_ago}` (italic with U+201C/U+201D quote marks).
- Cancel button: `Cancel`.
- Destructive confirm button: `Delete conversation` with bg `var(--color-error)` text white.

**ErrorBanner component contract:**

```jsx
// frontend/src/components/ErrorBanner.jsx
import './ErrorBanner.css';

export default function ErrorBanner({ stageNumber, onRetry, onDismiss, retryAttempted = false }) {
  return (
    <div className="error-banner" role="alert" aria-live="assertive">
      <p className="error-banner__copy">
        La deliberación se interrumpió en Stage {stageNumber}. ¿Quieres reintentar con la misma pregunta?
      </p>
      <div className="error-banner__actions">
        <button type="button" className="error-banner__retry" onClick={onRetry}>Retry</button>
        {retryAttempted && (
          <button type="button" className="error-banner__dismiss" onClick={onDismiss}>Dismiss</button>
        )}
      </div>
    </div>
  );
}
```

Per UI-SPEC §Copywriting Contract line 256: "Dismiss only after retry attempted at least once" → control via `retryAttempted` prop.

**Banner CSS** (UI-SPEC §Color: destructive reserved for "Persistent ErrorBanner background tint + border-top + retry-button border"):
```css
.error-banner {
  background: rgba(160, 56, 40, 0.10);   /* color-error at 10% alpha — soft tint, inline literal NOT token (per cross-wave ownership rule) */
  border-top: 3px solid var(--color-error);
  padding: var(--space-3) var(--space-5);
  display: flex; align-items: center; justify-content: space-between;
  gap: var(--space-4);
  font-family: var(--font-sans);
  color: var(--color-fg-primary);
}
.error-banner__copy { font-size: var(--font-size-body-small); }
.error-banner__retry {
  background: transparent;
  border: 1px solid var(--color-error);
  border-radius: var(--radius-sm);
  color: var(--color-error);
  padding: var(--space-2) var(--space-4);
  font-family: var(--font-sans);
  font-size: var(--font-size-label);
  cursor: pointer;
  transition: background-color var(--motion-duration-base) var(--motion-easing-out);
}
.error-banner__retry:hover { background: rgba(160, 56, 40, 0.12); }
.error-banner__retry:focus-visible { outline: 2px solid var(--color-focus-ring); outline-offset: 2px; }
.error-banner__dismiss {
  background: transparent;
  border: 1px solid var(--color-border-subtle);
  border-radius: var(--radius-sm);
  color: var(--color-fg-secondary);
  padding: var(--space-2) var(--space-4);
  font-family: var(--font-sans);
  font-size: var(--font-size-label);
  cursor: pointer;
}
```

**NO añadir nuevos tokens a `index.css` en esta wave** — el set de tokens de Plan 04-01 está cerrado al final de Wave 1 para evitar cross-wave file ownership confusion (Plan 04-01 owns index.css; Plan 04-04 NO lo modifica). Por tanto el soft-tint del banner se mantiene como literal inline en `ErrorBanner.css`: `background: rgba(160, 56, 40, 0.10);` (light) — un único valor que funciona en ambos themes con suficiente contraste. Si el contraste en dark mode resulta insuficiente al visual review, aceptable subir el alpha a `0.14` — sigue siendo literal inline, NO token nuevo.

**Favicon ampersand SVG** (D-15 + D-16 + UI-SPEC §Design System "Brand mark"):

Create `frontend/public/favicon-ampersand.svg` with:

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <rect width="32" height="32" fill="#FAF8F4"/>
  <text x="16" y="24" text-anchor="middle"
        font-family="Georgia, 'Times New Roman', serif"
        font-size="28" font-weight="400"
        fill="#6B635A">&amp;</text>
</svg>
```

NOTE on the favicon font: ideally we'd use `'Source Serif 4 Variable'`, but favicons are rendered before the `@font-face` loads. Georgia is a near-perfect serif fallback installed on every OS — visually consistent with the in-app brand mark which DOES use Source Serif 4 (with Georgia as fallback).

The SVG fill colors are LITERAL hex (NOT tokens) because SVG files outside the React tree do not consume CSS variables. Light-mode-only colors are acceptable for the favicon — D-16 explicitly mentions a single 32×32 SVG/PNG, not a dual-theme version. Document this scope decision in the SUMMARY.

The `index.html` `<link rel="icon" type="image/png" href="/favicon-ampersand.png">` from Plan 04-01 must be UPDATED in this plan to point at the SVG: `<link rel="icon" type="image/svg+xml" href="/favicon-ampersand.svg">`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate Sidebar.css + Modal.css + Menu.css to tokens; restructure Sidebar empty state with big ampersand</name>
  <files>
    frontend/src/components/Sidebar.css
    frontend/src/components/Sidebar.jsx
    frontend/src/components/Modal.css
    frontend/src/components/Menu.css
  </files>
  <read_first>
    frontend/src/components/Sidebar.jsx (current state — locate empty state branch and rename input area)
    frontend/src/components/Sidebar.css (current state — Phase 1 styles, hex hardcoded)
    frontend/src/components/Modal.css (current state)
    frontend/src/components/Menu.css (current state)
    .planning/phases/04-visual-identity-implementation/04-UI-SPEC.md §Copywriting Contract (sidebar empty state, rename hint, menu items, modal — lines 224-263)
    .planning/phases/04-visual-identity-implementation/04-UI-SPEC.md §Color Allowlist item 3 (active sidebar row pattern, line 181)
  </read_first>
  <action>
    **`Sidebar.css`** — full token migration:

    Apply the same hex → token swap pattern as Plan 04-03 Task 1. Specific requirements:

    - `.sidebar` container: `background: var(--color-bg-secondary); border-right: 1px solid var(--color-border-subtle); width: var(--layout-sidebar-w); display: flex; flex-direction: column; overflow: hidden;` (the grid placement comes from App.css; Sidebar.css owns its internal layout).
    - `.sidebar__header` (or wherever the "+ New Conversation" button lives): padding `var(--space-4) var(--space-3)`. The button: `font-family: var(--font-sans); font-size: var(--font-size-label); font-weight: 500; background: var(--color-bg-elevated); border: 1px solid var(--color-border-subtle); color: var(--color-fg-primary); border-radius: var(--radius-sm); padding: var(--space-2) var(--space-3); cursor: pointer; transition: background-color var(--motion-duration-base) var(--motion-easing-out);` hover `var(--color-bg-primary)`.
    - `.sidebar__search` input: `var(--font-sans)`, size `var(--font-size-body-small)`, border `1px solid var(--color-border-subtle)`, radius `var(--radius-sm)`, padding `var(--space-2) var(--space-3)`, bg `var(--color-bg-elevated)`. Focus: `border-color: var(--color-accent); outline: none; box-shadow: 0 0 0 3px var(--color-focus-ring);`.
    - `.sidebar__list` items (`.conversation-item` or whatever the existing class is): padding `var(--space-3)`, font `var(--font-sans)` size `var(--font-size-body-small)` weight 400 color `var(--color-fg-primary)`. Hover: `background: var(--color-bg-elevated)` (NEUTRAL, not accent — accent reserved for active per UI-SPEC).
    - **Active row indicator** (per UI-SPEC line 181). **PASO OBLIGATORIO PRIMERO: leer `frontend/src/components/Sidebar.jsx`** para identificar la convención actual. La inspección al planificar reveló que Sidebar.jsx usa la clase **`.conversation-item.active`** (línea ~96: `className={\`conversation-item ${isActive ? 'active' : ''}\`}`). Por tanto el selector CSS DEBE ser:
      ```css
      .conversation-item.active {
        border-left: 3px solid var(--color-accent);
        background: var(--color-accent-soft);
        padding-left: calc(var(--space-3) - 3px);
      }
      ```
      **Verificación al ejecutar:** confirmar que Sidebar.jsx no ha cambiado de convención desde el planning. Si la inspección revela una convención DISTINTA (p.ej. ahora usa `aria-current="page"` o `.conversation-item--active` BEM, o un `data-active` attribute), usar esa convención EN VEZ DE `.active`. Si Sidebar.jsx no marca el row activo de ninguna manera, AÑADIR `data-active={isActive ? 'true' : undefined}` al JSX del row Y usar selector `.conversation-item[data-active="true"]` en CSS — los DOS cambios juntos en el mismo task. NUNCA dejar el CSS sin un selector que matchee el JSX real.
    - `.sidebar__item-meta` (the "{N} messages" line): `font-size: var(--font-size-microcopy); color: var(--color-fg-muted); font-variant-numeric: tabular-nums; font-feature-settings: "tnum" 1;` (UI-SPEC line 130).
    - **Empty state**:
      ```css
      .sidebar__empty {
        flex: 1;
        display: flex; flex-direction: column;
        align-items: center; justify-content: center;
        padding: var(--space-7) var(--space-5);
        gap: var(--space-4);
      }
      .sidebar__empty-mark {
        font-family: var(--font-serif);
        font-weight: 400;
        font-size: 96px;        /* D-15: "96-128px"; lower bound is fine */
        line-height: 1;
        color: var(--color-fg-secondary);
        opacity: 0.55;          /* slightly muted — it's a mark, not a heading */
        user-select: none;
      }
      .sidebar__empty-body {
        font-family: var(--font-sans);
        font-size: var(--font-size-body-small);
        color: var(--color-fg-muted);
        text-align: center;
        max-width: 24ch;
      }
      ```
    - **Inline rename hint** (UI-SPEC line 232):
      ```css
      .sidebar__rename-hint {
        font-family: var(--font-sans);
        font-size: var(--font-size-microcopy);
        color: var(--color-fg-muted);
        margin-top: var(--space-1);
        padding: 0 var(--space-3);
      }
      ```
    - Three-dot menu trigger (`.sidebar__menu-trigger` or similar): visible permanente en la fila active (UI-SPEC line 344 — "deliberate divergence from ChatGPT, justified by F-16/H6-02"). Hover en filas no-active = visible. Resto = `opacity: 0; visibility: hidden;` con transition `opacity var(--motion-duration-base) var(--motion-easing-out)`. Active row OR row-hover = `opacity: 1; visibility: visible;`.

    **`Sidebar.jsx`** — surgical edits (NO API changes, NO new state hooks beyond what's needed for empty-state rendering):

    1. **Empty state branch**: localizar dónde el componente decide qué renderizar cuando `conversations.length === 0`. Reemplazar/expandir el render por:
       ```jsx
       {conversations.length === 0 ? (
         <div className="sidebar__empty">
           <span className="sidebar__empty-mark" aria-hidden="true">&amp;</span>
           <p className="sidebar__empty-body">No conversations yet. Start one to see it here.</p>
         </div>
       ) : (
         /* existing list rendering */
       )}
       ```
       Si la condicional ya existe pero con copy diferente, sustituir el copy por el literal exacto.
    2. **Inline rename hint**: localizar el `RenameInput` o equivalente (Phase 1 / Plan 03 lo introdujo con el intentRef pattern). Justo debajo del `<input>` rendererarlo cuando `isEditing === true`:
       ```jsx
       {isEditing && (
         <p className="sidebar__rename-hint">Enter para guardar · Esc para cancelar</p>
       )}
       ```
       Sin cambiar la API ni el flujo de commit/cancel — solo añadir la línea de hint.
    3. NO renombrar class names existentes — añadir las nuevas (sidebar__empty, sidebar__empty-mark, sidebar__empty-body, sidebar__rename-hint) sin tocar las que ya estaban.

    **`Modal.css`** — full token migration:

    - `.modal-backdrop`: `background: rgba(28, 26, 23, 0.55)` (a literal — backdrop opacity is theme-agnostic-ish; or define `--color-backdrop` token if needed). For dark mode the same alpha works.
    - `.modal`: `background: var(--color-bg-elevated); border-radius: var(--radius-md); box-shadow: var(--shadow-md); padding: var(--space-5); max-width: 480px; width: 90%; font-family: var(--font-serif);`.
    - `.modal__title`: `font-size: var(--font-size-h2); font-weight: 600; color: var(--color-fg-primary); margin-bottom: var(--space-3);`.
    - `.modal__body`: `font-size: var(--font-size-body); color: var(--color-fg-primary); margin-bottom: var(--space-5);`.
    - `.modal__cancel-btn` (the `Cancel` button): ghost — `background: transparent; border: 1px solid var(--color-border-strong); border-radius: var(--radius-sm); padding: var(--space-2) var(--space-4); color: var(--color-fg-primary); font-family: var(--font-sans); font-size: var(--font-size-label); cursor: pointer;`. Hover: `background: var(--color-bg-secondary)`.
    - `.modal__confirm-btn` (the destructive `Delete conversation` button): `background: var(--color-error); color: #FFFFFF; border: none; border-radius: var(--radius-sm); padding: var(--space-2) var(--space-4); font-family: var(--font-sans); font-size: var(--font-size-label); font-weight: 500; cursor: pointer; transition: filter var(--motion-duration-base) var(--motion-easing-out);`. Hover: `filter: brightness(0.92)`.

    NOTE: the Modal copy is in `Modal.jsx` (Phase 1 / Plan 02). Verify the title/body/buttons strings match UI-SPEC §Copywriting Contract verbatim. If they differ ("Delete?" vs "Delete this conversation?", etc.), update them in Modal.jsx as a small surgical edit. The body's metadata interpolation `"{title}" · N messages · {time_ago}` should already be there from Phase 1; if missing, that's out of scope (a Phase 1 follow-up) and should be flagged in the SUMMARY.

    **`Menu.css`** — full token migration:

    - `.menu` (the popover root): `background: var(--color-bg-elevated); border-radius: var(--radius-sm); box-shadow: var(--shadow-sm); border: 1px solid var(--color-border-subtle); padding: var(--space-1) 0; min-width: 180px; font-family: var(--font-sans);`.
    - `.menu__item`: `display: flex; align-items: center; justify-content: space-between; padding: var(--space-2) var(--space-3); font-size: var(--font-size-label); color: var(--color-fg-primary); cursor: pointer; transition: background-color var(--motion-duration-fast) var(--motion-easing-out); background: transparent; border: none; width: 100%; text-align: left;`. Hover: `background: var(--color-accent-soft)` (UI-SPEC accent allowlist item 10 — "Three-dot menu hover/active item: background var(--color-accent-soft)").
    - `.menu__item--destructive` (the Delete entry): `color: var(--color-error)`. Hover: still `var(--color-accent-soft)` (per the allowlist), but the text stays error-red. If the visual contrast feels off, an alternate is `background: rgba(160, 56, 40, 0.08); color: var(--color-error);` — document the choice in SUMMARY.
    - `.menu__shortcut`: `color: var(--color-fg-muted); font-size: var(--font-size-microcopy); font-family: var(--font-sans);` aligned right (parent uses `justify-content: space-between`).

    Verify the `Menu.jsx` items have the correct copy: "Rename" + shortcut "R", "Delete" + shortcut "⌫". If missing, add the shortcut spans:
    ```jsx
    <button className="menu__item" onClick={onRename}>
      Rename <span className="menu__shortcut">R</span>
    </button>
    <button className="menu__item menu__item--destructive" onClick={onDelete}>
      Delete <span className="menu__shortcut">⌫</span>
    </button>
    ```

    NO añadir keyboard handlers para los shortcuts — son hints visuales (la implementación de los shortcuts de teclado está fuera de scope; no aparece en el roadmap ni en CONTEXT/UI-SPEC como must-have, solo el HINT visible).
  </action>
  <verify>
    <automated>powershell -Command "$ok = $true; $files = @('frontend/src/components/Sidebar.css', 'frontend/src/components/Modal.css', 'frontend/src/components/Menu.css'); foreach ($f in $files) { $c = Get-Content $f -Raw; if ($c -match '#4a90e2|#357abd|#f0fff0|#f5f5f5|#f0f0f0') { Write-Host ('FORBIDDEN HEX in ' + $f); $ok = $false }; if ($c -notmatch 'var\(--color-') { Write-Host ('NO TOKEN CONSUMPTION in ' + $f); $ok = $false } }; $sidebarCss = Get-Content frontend/src/components/Sidebar.css -Raw; if ($sidebarCss -notmatch 'sidebar__empty-mark') { Write-Host 'Sidebar.css MISSING __empty-mark class'; $ok = $false }; if ($sidebarCss -notmatch 'font-size: 96px' -and $sidebarCss -notmatch 'font-size:\s*9[6-9]px' -and $sidebarCss -notmatch 'font-size:\s*1[0-2][0-9]px') { Write-Host 'Sidebar.css ampersand mark size <96px'; $ok = $false }; if ($sidebarCss -notmatch 'border-left:.*var\(--color-accent\)') { Write-Host 'Sidebar.css MISSING active row border-left accent'; $ok = $false }; $sidebarJsx = Get-Content frontend/src/components/Sidebar.jsx -Raw; if ($sidebarJsx -notmatch 'No conversations yet. Start one to see it here.') { Write-Host 'Sidebar.jsx MISSING empty body copy'; $ok = $false }; if ($sidebarJsx -notmatch 'Enter para guardar · Esc para cancelar') { Write-Host 'Sidebar.jsx MISSING rename hint copy'; $ok = $false }; if (-not ($sidebarJsx -match '(data-active|aria-current|conversation-item--active|isActive\s*\?\s*''active''|isActive\s*\?\s*"active")')) { Write-Host 'Sidebar.jsx no tiene un marcador reconocible de active row — el CSS no podrá aplicar el border-left'; $ok = $false }; $modalCss = Get-Content frontend/src/components/Modal.css -Raw; if ($modalCss -notmatch 'var\(--shadow-md\)') { Write-Host 'Modal.css MISSING --shadow-md'; $ok = $false }; $menuCss = Get-Content frontend/src/components/Menu.css -Raw; if ($menuCss -notmatch 'var\(--color-accent-soft\)') { Write-Host 'Menu.css MISSING --color-accent-soft hover'; $ok = $false }; if (-not $ok) { exit 1 } else { Write-Host 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar/Modal/Menu CSS migrated: 0 matches of Bootstrap hex; ≥3 token consumptions per file.
    - Sidebar empty state visible with `&` ampersand mark ≥96px in `var(--font-serif)` weight 400 + body copy "No conversations yet. Start one to see it here.".
    - Active row in sidebar uses `border-left: 3px solid var(--color-accent)` + `--color-accent-soft` background tint.
    - Rename hint copy `Enter para guardar · Esc para cancelar` rendered when isEditing.
    - Modal: title `Delete this conversation?`, confirm button `Delete conversation` with `var(--color-error)` background.
    - Menu: items "Rename" + shortcut "R", "Delete" + shortcut "⌫"; hover bg `var(--color-accent-soft)`.
    - `npm --prefix frontend run build` passes.
  </acceptance_criteria>
  <done>Sidebar/Modal/Menu fully Direction A — light + dark consistent. Empty state branded with the ampersand. Phase 4's accent allowlist enforced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ErrorBanner component + wire into App.jsx</name>
  <files>
    frontend/src/components/ErrorBanner.jsx
    frontend/src/components/ErrorBanner.css
    frontend/src/App.jsx
    frontend/src/App.css
  </files>
  <read_first>
    frontend/src/App.jsx (current state — locate the SSE error handler in handleSendMessage)
    .planning/phases/04-visual-identity-implementation/04-UI-SPEC.md §Copywriting Contract (error banner copy + retry/dismiss, lines 254-256)
    .planning/phases/04-visual-identity-implementation/04-UI-SPEC.md §Color (destructive reserved for, lines 196-202)
    .planning/ux/02-nielsen-audit.md (H9-01 catastrophic interruption — what should the recovery flow look like)
    .planning/phases/04-visual-identity-implementation/04-CONTEXT.md D-14 (error banner persistente con retry)
  </read_first>
  <action>
    **`frontend/src/components/ErrorBanner.jsx`** (NEW file):

    Implement EXACTLY per `<interfaces>` block above. Three props: `stageNumber`, `onRetry`, `onDismiss`, `retryAttempted` (default false). Renders a div with `role="alert"` and `aria-live="assertive"`. Copy LITERAL: `La deliberación se interrumpió en Stage {stageNumber}. ¿Quieres reintentar con la misma pregunta?` (UI-SPEC §Copywriting Contract line 254). Buttons: "Retry" always; "Dismiss" only when `retryAttempted === true` (UI-SPEC §Copywriting Contract line 256).

    **`frontend/src/components/ErrorBanner.css`** (NEW file):

    Implement EXACTLY per `<interfaces>` block above. Use `--color-error` for border-top + retry button border + retry button text color. Background = soft tint of error color: literal inline `rgba(160, 56, 40, 0.10)` — **NO añadir nuevo token a index.css** (set cerrado al final de Wave 1).

    Add a `prefers-reduced-motion` overlap check: the global override from Plan 04-01 takes care of the transitions; no extra rules needed.

    **`frontend/src/App.jsx`** — wire the banner:

    1. Add state: `const [streamError, setStreamError] = useState(null);` and `const [retryAttempted, setRetryAttempted] = useState(false);`.
    2. In the `case 'error':` branch of `handleSendMessage` (currently just logs to console + setIsLoading(false)), set:
       ```jsx
       case 'error': {
         // Determine which stage was active when the error occurred.
         const lastMsg = currentConversation?.messages?.[currentConversation.messages.length - 1];
         let stageNumber = 1;
         if (lastMsg?.stage1) stageNumber = 2;
         if (lastMsg?.stage2) stageNumber = 3;
         setStreamError({ stageNumber, originalContent: content, originalProfile: profile });
         setIsLoading(false);
         break;
       }
       ```
       (`content` and `profile` are the args of the enclosing `handleSendMessage` — captured by closure.)
    3. Add a `handleRetryError` function:
       ```jsx
       const handleRetryError = useCallback(() => {
         if (!streamError) return;
         setRetryAttempted(true);
         const { originalContent, originalProfile } = streamError;
         setStreamError(null);
         // Re-send the same query
         handleSendMessage(originalContent, originalProfile);
       }, [streamError]);

       const handleDismissError = useCallback(() => {
         setStreamError(null);
         setRetryAttempted(false);
       }, []);
       ```
       Note: `useCallback` dep on `streamError` is required because the function reads its current value. If `useCallback` deps complicate things, use a plain function — the optimization is not critical here.

       Also import `useCallback` from React if not already imported.

    4. In the JSX, render the banner above ChatInterface:
       ```jsx
       <div className="app">
         <Header />
         <Sidebar ... />
         <div className="app__main-with-banner">
           {streamError && (
             <ErrorBanner
               stageNumber={streamError.stageNumber}
               onRetry={handleRetryError}
               onDismiss={handleDismissError}
               retryAttempted={retryAttempted}
             />
           )}
           <ChatInterface ... />
         </div>
       </div>
       ```

       **CROSS-WAVE OWNERSHIP NOTE — App.css:** Plan 04-02 (Wave 2) is the canonical owner del grid layout en App.css. Plan 04-04 (Wave 4) **solo añade** la regla `.app__main-with-banner` Y **migra la posición grid de `.chat-interface` desde la regla directa a la nueva regla anidada** (operación delta, no rewrite). La ejecución secuencial de waves (2 → 4) previene merge conflicts: Wave 2 commitea la regla directa, Wave 4 la sustituye por el wrapper. **Documentar este delta en el SUMMARY.md** ("Plan 04-04 Task 2 modified App.css adding .app__main-with-banner and removing the prior .app > .chat-interface direct grid placement rule.").

       **GRID PLACEMENT WARNING:** App.css (Plan 04-02) places `.chat-interface` at `grid-column: 2; grid-row: 2`. Wrapping it in `.app__main-with-banner` would break that selector. Two options:

       **Option A (simpler):** Add `.app__main-with-banner` to App.css with `grid-column: 2; grid-row: 2; display: flex; flex-direction: column; overflow: hidden;` and update `.app > .chat-interface` selector to NOT match (since chat-interface is now nested). The banner sits above ChatInterface inside this column. This is the recommended approach.

       Update `App.css` (DELTA — añadir + remover, NO rewrite del archivo):
       ```css
       /* ADD: */
       .app > .app__main-with-banner {
         grid-column: 2;
         grid-row: 2;
         display: flex;
         flex-direction: column;
         overflow: hidden;
       }
       .app__main-with-banner > .chat-interface {
         flex: 1;
         overflow: hidden;
       }
       /* REMOVE: the existing `.app > .chat-interface { grid-column: 2; grid-row: 2; ... }` rule
          (set by Plan 04-02). The new wrapper owns esa posición ahora.
          Si Plan 04-02 también puso media-query mobile rules como `.app > .chat-interface { grid-column: 1; }`,
          renombrar el selector a `.app > .app__main-with-banner { grid-column: 1; }` para preservar el comportamiento mobile. */
       ```

       **Option B (mobile drawer compatibility):** Mobile rule `.app > .chat-interface { grid-column: 1; }` becomes `.app > .app__main-with-banner { grid-column: 1; }`. Adjust accordingly.

       Add `import ErrorBanner from './components/ErrorBanner';`.

    5. Import `useCallback` from 'react' in the hooks import line if not already imported.

    Document the banner trigger condition in the SUMMARY: only fires on `'error'` SSE event, NOT on individual model failures (those are graceful per Phase 1 / RESEARCH §Error Handling — Stage 1 just renders failed tabs with strikethrough).
  </action>
  <verify>
    <automated>powershell -Command "$ok = $true; if (-not (Test-Path frontend/src/components/ErrorBanner.jsx)) { Write-Host 'MISSING ErrorBanner.jsx'; $ok = $false }; if (-not (Test-Path frontend/src/components/ErrorBanner.css)) { Write-Host 'MISSING ErrorBanner.css'; $ok = $false }; $banJsx = Get-Content frontend/src/components/ErrorBanner.jsx -Raw -ErrorAction SilentlyContinue; $banCss = Get-Content frontend/src/components/ErrorBanner.css -Raw -ErrorAction SilentlyContinue; $jsxChecks = @('La deliberación se interrumpió en Stage', 'Retry', 'role=\"alert\"', 'aria-live=\"assertive\"'); foreach ($c in $jsxChecks) { if ($banJsx -notmatch [regex]::Escape($c)) { Write-Host ('banner jsx MISSING: ' + $c); $ok = $false } }; $cssChecks = @('var(--color-error)', 'border-top'); foreach ($c in $cssChecks) { if ($banCss -notmatch [regex]::Escape($c)) { Write-Host ('banner css MISSING: ' + $c); $ok = $false } }; $appJsx = Get-Content frontend/src/App.jsx -Raw; $appChecks = @('import ErrorBanner', '<ErrorBanner', 'streamError', 'setStreamError'); foreach ($c in $appChecks) { if ($appJsx -notmatch [regex]::Escape($c)) { Write-Host ('App.jsx MISSING: ' + $c); $ok = $false } }; cd frontend; $build = & npm run build 2>&1; if ($LASTEXITCODE -ne 0) { Write-Host 'Build failed'; Write-Host $build; $ok = $false }; cd ..; if (-not $ok) { exit 1 } else { Write-Host 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - ErrorBanner.jsx + .css created.
    - ErrorBanner copy literal: `La deliberación se interrumpió en Stage {N}. ¿Quieres reintentar con la misma pregunta?`.
    - role="alert" + aria-live="assertive" present.
    - Dismiss button conditional on `retryAttempted === true`.
    - Banner CSS uses `var(--color-error)` border-top + retry button border.
    - App.jsx imports + renders ErrorBanner + manages streamError/retryAttempted state + wires retry to re-send.
    - App.css actualizada (delta: añade `.app__main-with-banner` wrapper grid placement Y elimina la regla directa anterior `.app > .chat-interface` de Plan 04-02 que ahora es redundante).
    - `npm --prefix frontend run build` passes.
  </acceptance_criteria>
  <done>ErrorBanner persistent recovery for H9-01 wired. User can retry the same query from the banner; dismiss appears only after first retry.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create favicon-ampersand.svg and update index.html link</name>
  <files>
    frontend/public/favicon-ampersand.svg
    frontend/index.html
  </files>
  <read_first>
    frontend/index.html (current state — link rel=icon set in Plan 04-01 to /favicon-ampersand.png)
    .planning/phases/04-visual-identity-implementation/04-CONTEXT.md D-15, D-16 (ampersand mark + favicon)
    .planning/phases/04-visual-identity-implementation/04-UI-SPEC.md §Design System (brand mark line 31)
  </read_first>
  <action>
    **Create `frontend/public/favicon-ampersand.svg`** with the following content:

    ```xml
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
      <rect width="32" height="32" fill="#FAF8F4"/>
      <text x="16" y="24" text-anchor="middle"
            font-family="Georgia, 'Times New Roman', serif"
            font-size="28" font-weight="400"
            fill="#6B635A">&amp;</text>
    </svg>
    ```

    Notes:
    - Hex literals are intentional in this file (SVGs cannot consume CSS variables since the file lives outside the React tree). Document this exception in the SUMMARY.
    - Font: Georgia (universally available serif). The in-app brand mark uses Source Serif 4 with Georgia as fallback; the favicon stays consistent because Source Serif 4 isn't loaded yet at favicon-render time.
    - **Single-theme (light only) — trade-off ACEPTADO Y DOCUMENTADO en backlog.** Los navegadores no soportan per-theme favicons consistentemente en 2026: algunas versiones de Chrome aceptan `<link rel="icon" media="(prefers-color-scheme: dark)">` pero la compatibilidad es patchy en Firefox/Safari. La paleta usada en el SVG (bg `#FAF8F4`, glyph `#6B635A`) son los valores LIGHT de los tokens `--color-bg-primary` y `--color-fg-secondary`. En dark theme el favicon se verá ligeramente fuera de palette, pero seguirá siendo legible (cream sobre dark navigator chrome). Documentar en `SUMMARY.md` Y añadir entrada al backlog para revisar cuando la cobertura per-theme mejore. Per CONTEXT D-16: single 32×32 SVG/PNG es lo planeado.

    **Update `frontend/index.html`**:

    Plan 04-01 added `<link rel="icon" type="image/png" href="/favicon-ampersand.png" />` as a placeholder. Change to:

    ```html
    <link rel="icon" type="image/svg+xml" href="/favicon-ampersand.svg" />
    ```

    Verify the link is in the `<head>` and uses the correct MIME type and path. Vite serves `/public/*` directly at the root path, so `/favicon-ampersand.svg` resolves correctly.
  </action>
  <verify>
    <automated>powershell -Command "$ok = $true; if (-not (Test-Path frontend/public/favicon-ampersand.svg)) { Write-Host 'MISSING favicon-ampersand.svg'; $ok = $false }; $svg = Get-Content frontend/public/favicon-ampersand.svg -Raw -ErrorAction SilentlyContinue; if ($svg -notmatch '<svg' -or $svg -notmatch '&amp;' -or $svg -notmatch 'viewBox=\"0 0 32 32\"') { Write-Host 'favicon SVG malformed'; $ok = $false }; $html = Get-Content frontend/index.html -Raw; if ($html -notmatch 'rel=\"icon\"\s+type=\"image/svg\+xml\"\s+href=\"/favicon-ampersand.svg\"') { Write-Host 'index.html link rel=icon not updated to SVG'; $ok = $false }; if (-not $ok) { exit 1 } else { Write-Host 'OK'; exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/public/favicon-ampersand.svg` exists with correct viewBox + ampersand text.
    - `frontend/index.html` `<link rel="icon">` updated to SVG path with correct MIME type.
    - Browser tab muestra el ampersand (smoke test manual: `npm run dev` → ver favicon en tab).
  </acceptance_criteria>
  <done>Favicon ampersand sustituye al placeholder Vite. Branding completo desde el browser tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidebar conversation list | Already validated in Phase 1 (UUIDs canonicalized at storage layer). Phase 4 doesn't change the data flow. |
| ErrorBanner retry → handleSendMessage | Re-uses the existing send flow; no new server endpoint. The captured `originalContent + originalProfile` are the same as the failed call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | ErrorBanner retry replay | accept | The user is intentionally re-sending the same query. The backend has no idempotency requirement; a duplicate assistant message would only cost the user money (BYOK). The visible Retry button is the deliberate UI-mediated consent. |
| T-04-12 | Information Disclosure | Sidebar empty state ampersand exposing brand mark | accept | The brand mark is public by design (it's the favicon and header logo). No sensitive data. |
| T-04-13 | Denial of Service | ErrorBanner repeated retry creating zombie streams | mitigate | `setIsLoading(false)` is set in the error handler before the banner shows. The user cannot retry while a stream is mid-flight; the new send re-enters `handleSendMessage` which already manages `setIsLoading(true)`. The previous stream's reader is already aborted (since `'error'` event terminates the SSE generator on the backend per Phase 1 / Plan 05). |
| T-04-14 | Tampering | Favicon SVG content modified at runtime | accept | Static file served by Vite from `/public`. No runtime injection vector. |
</threat_model>

<verification>
**Wave 4 grep gates (UI-SPEC §Acceptance gates Wave D):**

```bash
# Token consumption in conversations files
grep -c "var(--color-" frontend/src/components/Sidebar.css   # >= 5
grep -c "var(--color-" frontend/src/components/Modal.css     # >= 4
grep -c "var(--color-" frontend/src/components/Menu.css      # >= 4

# No Bootstrap hex
grep -iE "#(4a90e2|357abd|f0fff0|f5f5f5|f0f0f0)" \
  frontend/src/components/Sidebar.css \
  frontend/src/components/Modal.css \
  frontend/src/components/Menu.css | wc -l
# Expected: 0

# Sidebar empty state ampersand mark ≥96px
grep -c "sidebar__empty-mark" frontend/src/components/Sidebar.css   # >= 1
grep -E "font-size:\s*(96|10[0-9]|11[0-9]|12[0-9])px" frontend/src/components/Sidebar.css | wc -l
# Expected: >= 1 (the ampersand mark)

# Sidebar empty body copy literal
grep -c "No conversations yet. Start one to see it here." frontend/src/components/Sidebar.jsx  # >= 1

# Inline rename hint copy literal
grep -c "Enter para guardar · Esc para cancelar" frontend/src/components/Sidebar.jsx  # >= 1

# ErrorBanner present and wired
test -f frontend/src/components/ErrorBanner.jsx && echo OK
test -f frontend/src/components/ErrorBanner.css && echo OK
grep -c "<ErrorBanner" frontend/src/App.jsx  # >= 1

# Favicon
test -f frontend/public/favicon-ampersand.svg && echo OK
grep -c "favicon-ampersand.svg" frontend/index.html  # >= 1
```

**Final phase-wide grep gates** (all 4 waves combined — verify the entire phase is clean):

```bash
# No Bootstrap-flavored hex anywhere in frontend/src/
grep -riE "#(4a90e2|357abd|f0fff0|f5f5f5|f0f0f0)" frontend/src/ | wc -l
# Expected: 0  (UI-SPEC line 207-211)

# No system-ui except as fallback inside var(--font-sans) in index.css
grep -rn "system-ui" frontend/src/ --include="*.css" --include="*.jsx" --include="*.js" | grep -v "index.css" | wc -l
# Expected: 0  (UI-SPEC Wave B gate)

# No Phase 3 placeholder hex word-bounded in MessageHeader / QualityToggle / ReasoningDisclosure / Stage4
grep -iE "#(666|333|999)\b" \
  frontend/src/components/MessageHeader.css \
  frontend/src/components/QualityToggle.css \
  frontend/src/components/ReasoningDisclosure.css \
  frontend/src/components/Stage4.css | wc -l
# Expected: 0  (UI-SPEC line 407)
```

**Smoke test (manual):**

1. `npm --prefix frontend run dev` (with empty `data/conversations/` if possible).
2. Sidebar empty state: gigantic `&` mark + body copy.
3. Click "+ New Conversation" → list populates; selected row shows accent border-left + soft bg.
4. Right-click / three-dot menu on the active row → "Rename" + "R" hint, "Delete" + "⌫" hint, hover shows accent-soft.
5. Click "Rename" → inline rename input appears + hint `Enter para guardar · Esc para cancelar`.
6. Click "Delete" → modal: title `Delete this conversation?`, body with metadata `"{title}" · N messages · {time_ago}`, buttons Cancel / Delete conversation (red).
7. Force an error (e.g., disconnect network mid-stream) → ErrorBanner aparece arriba con copy + Retry button. Click Retry → new send fires; banner desaparece.
8. Browser tab favicon = ampersand (no Vite logo).
9. Toggle theme light/dark → todo se mantiene coherente; Stage 3 sigue terracota-soft (no green); empty state ampersand visible en ambos modos.
10. `npm --prefix frontend run build` pasa.
11. `npm --prefix frontend run lint` pasa.
</verification>

<success_criteria>
1. Sidebar/Modal/Menu CSS migrated to tokens; 0 matches of Bootstrap hex.
2. Sidebar empty state with `&` ≥96px serif mark + locked body copy.
3. Sidebar active row: `border-left: 3px solid var(--color-accent)` + `--color-accent-soft` bg tint.
4. Inline rename hint copy literal `Enter para guardar · Esc para cancelar` rendered when isEditing.
5. Modal title `Delete this conversation?` + destructive button `Delete conversation` with `--color-error` bg.
6. Menu items "Rename" + "R" / "Delete" + "⌫" with `--color-accent-soft` hover.
7. ErrorBanner persistent component implemented + wired to App.jsx error handling; resolves H9-01.
8. Favicon SVG = ampersand serif; index.html link updated.
9. ALL Wave A/B/C/D grep gates pass cumulatively.
10. ALL 23 wireframes (W01-W23) materializados under Direction A skin.
11. VIS-01 + VIS-02 + VIS-03 + VIS-04 closed.
12. `npm run build` y `npm run lint` pasan.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-identity-implementation/04-04-SUMMARY.md` documenting:
- Sidebar/Modal/Menu migration: hex eliminations + token consumption counts.
- Empty state ampersand: visual confirmation (size + color in light + dark).
- Rename hint: render confirmation.
- Menu shortcut hints: confirmation that they are visual-only (no keyboard handlers — out of scope by design).
- ErrorBanner: trigger condition, retry behavior, dismiss conditional.
- Favicon: SVG content + index.html update. **Trade-off documented:** single-theme (light palette only) por inconsistent `prefers-color-scheme` favicon support across 2026 browsers. Backlog item: revisitar cuando la cobertura mejore.
- Phase 4 closing checklist:
  - [ ] All Wave A/B/C/D grep gates pass cumulatively (provide command outputs).
  - [ ] VIS-01 closed (Bootstrap palette gone, terracota+neutrals applied).
  - [ ] VIS-02 closed (system-ui gone, Source Serif 4 + Inter + JetBrains Mono Variable applied).
  - [ ] VIS-03 closed (Header + Sidebar + ChatInterface intentional empty states).
  - [ ] VIS-04 closed (microinteractions: progress dots, accordion, Stage 3 fade, hover fades, prefers-reduced-motion respected).
  - [ ] All 23 wireframes (W01-W23) materialised — list each with implementation note.
- Markdown export from a deliberation produces unchanged output (Phase 3 / Plan 03-05 contract preserved — Phase 4 is CSS-only).
- Recommended next session: update `.planning/STATE.md` to mark Phase 4 complete + 25/21 v1 requirements satisfied (or however the requirements counter is structured).
</output>
