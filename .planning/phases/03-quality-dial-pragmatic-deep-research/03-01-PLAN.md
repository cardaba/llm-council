---
phase: 03-quality-dial-pragmatic-deep-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - backend/openrouter.py
autonomous: true
requirements: [QUAL-02, RSCH-01]
must_haves:
  truths:
    - "El módulo backend/config.py expone un dict PROFILES con 3 entries: fast, quality, quality_research."
    - "Cada profile declara council_models + chairman_model + typical_cost_usd; quality_research añade critic_model y stage4_threshold."
    - "query_model en backend/openrouter.py acepta un kwarg opcional reasoning (default False) que inyecta {\"reasoning\": {\"enabled\": true}} en el payload cuando se pasa True."
    - "El BYOK routing existente (provider.only via PUBLISHER_TO_PROVIDER) sigue funcionando para todos los nuevos IDs incluyendo los suffix :online."
  artifacts:
    - path: "backend/config.py"
      provides: "PROFILES dict (Fast/Quality/Quality+Research) + critic config + cost defaults"
      contains: "PROFILES = {"
    - path: "backend/openrouter.py"
      provides: "query_model con soporte opt-in para reasoning param"
      contains: "reasoning: bool = False"
  key_links:
    - from: "backend/openrouter.py"
      to: "OpenRouter API payload"
      via: "payload['reasoning'] = {'enabled': True} cuando reasoning=True"
      pattern: "reasoning.*enabled.*True"
    - from: "backend/config.py"
      to: "consumidores del PROFILES dict (council.py + research_strategy.py)"
      via: "import PROFILES from .config"
      pattern: "from .config import.*PROFILES"
---

<objective>
Sentar la **base de configuración y transporte** del Quality Dial: definir el dict `PROFILES` en `backend/config.py` con los tres profiles (`fast`, `quality`, `quality_research`) y extender `query_model` en `backend/openrouter.py` para aceptar un parámetro opcional `reasoning` que se inyecte en el payload de OpenRouter como `{"reasoning": {"enabled": true}}`.

Este plan es la **fundación arquitectónica** consumida por todos los planes posteriores: Plan 03-02 lee `PROFILES[profile]["council_models"]` para enrutar `fast` y `quality`, Plan 03-04 lee `PROFILES["quality_research"]` (incluyendo `critic_model` y `stage4_threshold`) para orquestar el pipeline de research, y Plan 03-05 expone `typical_cost_usd` en el toggle (footnote-style per Direction A).

Purpose: Centralizar todas las decisiones de modelo/coste/threshold en un único dict para que ningún plan downstream tenga que hardcodear strings dispersos en otros archivos. La señal architectural es: "los profiles son configuración, no lógica".

Output:
- `backend/config.py` con `PROFILES` dict + constantes legacy (`COUNCIL_MODELS`, `CHAIRMAN_MODEL`) preservadas como aliases hacia `PROFILES["fast"]` para no romper el flujo actual antes de que Plan 03-02 reroute el código.
- `backend/openrouter.py` con `query_model(model, messages, timeout, reasoning=False)` y la inyección condicional del payload param `reasoning`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-quality-dial-pragmatic-deep-research/03-CONTEXT.md
@.planning/phases/03-quality-dial-pragmatic-deep-research/03-RESEARCH.md
@.planning/codebase/INTEGRATIONS.md

@backend/config.py
@backend/openrouter.py

<interfaces>
<!-- Contratos que este plan crea y que serán consumidos por planes posteriores. -->

Estado actual (a preservar como aliases):

```python
# backend/config.py (estado actual)
COUNCIL_MODELS = [
    "openai/gpt-5-mini",
    "openai/gpt-4.1-nano",
    "anthropic/claude-haiku-4.5",
    "google/gemini-2.5-flash",
]
CHAIRMAN_MODEL = "anthropic/claude-haiku-4.5"
```

Shape objetivo (CONTEXT.md §specifics + RESEARCH.md §OpenRouter model availability — IDs canónicos verificados):

```python
PROFILES = {
    "fast": {
        "council_models": [
            "openai/gpt-5-mini",
            "openai/gpt-4.1-nano",
            "anthropic/claude-haiku-4.5",
            "google/gemini-2.5-flash",
        ],
        "chairman_model": "anthropic/claude-haiku-4.5",
        "typical_cost_usd": 0.001,
    },
    "quality": {
        "council_models": [
            "openai/gpt-5.5",
            "anthropic/claude-opus-4.7",
            "google/gemini-3.1-pro-preview",
        ],
        "chairman_model": "anthropic/claude-opus-4.7",
        "typical_cost_usd": 0.05,
    },
    "quality_research": {
        # 4 reasoning models, todos con :online (D-10 + D-11 corregido por RESEARCH.md)
        "council_models": [
            "openai/o4-mini:online",
            "anthropic/claude-opus-4.7:online",
            "google/gemini-3.1-pro-preview:online",
            "openai/gpt-5.5:online",
        ],
        "chairman_model": "anthropic/claude-opus-4.7",
        "critic_model": "anthropic/claude-opus-4.7",
        "stage4_threshold": 8,
        "typical_cost_usd": 0.45,
    },
}

# Aliases legacy para minimizar diff hasta que Plan 03-02 cablee PROFILES en main.py/council.py.
# council.py los seguirá importando hasta entonces.
COUNCIL_MODELS = PROFILES["fast"]["council_models"]
CHAIRMAN_MODEL = PROFILES["fast"]["chairman_model"]
```

Firma extendida de `query_model`:

```python
async def query_model(
    model: str,
    messages: List[Dict[str, str]],
    timeout: float = 120.0,
    reasoning: bool = False,  # NUEVO — opt-in per llamada
) -> Optional[Dict[str, Any]]:
    ...
```

Inyección en payload (RESEARCH.md §OpenRouter model availability — `:thinking` NO existe como suffix; thinking se enables via payload):

```python
if reasoning:
    payload["reasoning"] = {"enabled": True}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender backend/config.py con PROFILES dict + aliases legacy</name>
  <files>backend/config.py</files>
  <read_first>
    - backend/config.py completo (44 líneas — leer una vez).
    - .planning/phases/03-quality-dial-pragmatic-deep-research/03-RESEARCH.md §"OpenRouter model availability" (líneas 81-122) — IDs canónicos verificados, especialmente la corrección `gemini-3.1-pro` → `gemini-3.1-pro-preview` y la NO existencia de `:thinking:online`.
    - .planning/phases/03-quality-dial-pragmatic-deep-research/03-CONTEXT.md §"Hardcoded cost numbers (D-21)" (CONTEXT líneas 220-241) — shape exacta del dict.
  </read_first>
  <action>
    Añade el dict `PROFILES` y los aliases al final de `backend/config.py`, **manteniendo intactas** las líneas 1-43 actuales (env load, `OPENROUTER_API_KEY`, `OPENROUTER_API_URL`, `DATA_DIR`, `PUBLISHER_TO_PROVIDER`, `get_provider_for_model`).

    1. **Conserva** `COUNCIL_MODELS` y `CHAIRMAN_MODEL` como están **temporalmente** (líneas 14-22), pero **reasignalos al final** del archivo como aliases derivados de `PROFILES["fast"]` para evitar drift entre las dos fuentes de verdad. Plan 03-02 los retirará cuando reescriba `council.py`.

    2. **Inserta** el dict `PROFILES` después de `get_provider_for_model` (al final del archivo), con la shape exacta del bloque `<interfaces>`. Comenta cada profile indicando:
       - `fast`: el cheap mix actual (per QUAL-02 ROADMAP — "fast using the current cheap mix").
       - `quality`: tier premium 3-model (gpt-5.5, opus-4.7, gemini-3.1-pro-preview) per D-11 + sustitución `gemini-3.1-pro-preview` per CD-05 + RESEARCH.md.
       - `quality_research`: 4 reasoning models todos con `:online` per D-10. Critic = Opus per D-06. Threshold = 8 per D-06. typical_cost = $0.45 per D-14.

    3. **Reasigna** los aliases al final:
       ```python
       COUNCIL_MODELS = PROFILES["fast"]["council_models"]
       CHAIRMAN_MODEL = PROFILES["fast"]["chairman_model"]
       ```
       Borra las definiciones inline iniciales (líneas 14-22) — ahora viven dentro del dict, los aliases las re-exportan.

    4. **NO usar** `:thinking` ni `:thinking:online` en ningún model ID (RESEARCH.md confirma que no existen). El reasoning se enables via payload param en Task 2.

    5. **NO** añadir Perplexity Sonar ni ningún provider fuera de `PUBLISHER_TO_PROVIDER` (D-12 — BYOK allowlist preservado). Verifica mentalmente que cada model ID arranca con `openai/`, `anthropic/`, o `google/`.

    6. Agrega un comment al final del bloque PROFILES referenciando la trazabilidad: `# Source of truth for QUAL-02. Decisions: D-06, D-10, D-11, D-12, D-14, D-21. Substitutions: CD-05 (gemini-3.1-pro → gemini-3.1-pro-preview).`
  </action>
  <verify>
    <automated>
      python -c "from backend.config import PROFILES, COUNCIL_MODELS, CHAIRMAN_MODEL, get_provider_for_model; assert set(PROFILES.keys()) == {'fast', 'quality', 'quality_research'}, 'profile keys'; assert all('council_models' in PROFILES[p] and 'chairman_model' in PROFILES[p] and 'typical_cost_usd' in PROFILES[p] for p in PROFILES), 'common keys'; assert PROFILES['quality_research']['critic_model'] == 'anthropic/claude-opus-4.7', 'critic'; assert PROFILES['quality_research']['stage4_threshold'] == 8, 'threshold'; assert all(m.endswith(':online') for m in PROFILES['quality_research']['council_models']), 'online suffix'; assert len(PROFILES['quality_research']['council_models']) == 4, '4 QR models'; assert COUNCIL_MODELS is PROFILES['fast']['council_models'], 'alias preserved'; assert all(get_provider_for_model(m) in {'openai', 'anthropic', 'google-ai-studio'} for p in PROFILES.values() for m in p['council_models']), 'BYOK allowlist'; print('OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^PROFILES = {" backend/config.py` retorna una línea.
    - `grep -c '":online"' backend/config.py` == 4 (los 4 models de quality_research).
    - `grep -n '":thinking"' backend/config.py` retorna **vacío** (no debe haber `:thinking` per RESEARCH).
    - `grep -n 'gemini-3.1-pro-preview' backend/config.py` retorna al menos 2 ocurrencias (quality + quality_research).
    - `grep -n 'critic_model' backend/config.py` retorna 1 línea dentro del bloque `quality_research`.
    - `grep -n 'stage4_threshold' backend/config.py` retorna 1 línea con valor 8.
    - El test python inline anterior imprime `OK`.
    - Funcional: una llamada existente a `query_model("anthropic/claude-haiku-4.5", [...])` desde el flujo `fast` actual sigue funcionando idéntica (los aliases preservan la API).
  </acceptance_criteria>
  <done>
    PROFILES dict definido con los 3 profiles, comentado, aliases legacy preservados, BYOK allowlist respetado, model IDs alineados con RESEARCH.md (sin `:thinking`, con `gemini-3.1-pro-preview`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Extender query_model en backend/openrouter.py con kwarg reasoning opcional</name>
  <files>backend/openrouter.py</files>
  <read_first>
    - backend/openrouter.py completo (86 líneas — leer una vez).
    - .planning/phases/03-quality-dial-pragmatic-deep-research/03-RESEARCH.md §"OpenRouter model availability" parágrafo "Suffix `:thinking:online` — composabilidad NO confirmada" (líneas 94-122) — confirma que reasoning se enables via payload `reasoning: { enabled: true }`, no via suffix.
    - .planning/phases/03-quality-dial-pragmatic-deep-research/03-RESEARCH.md §"reasoning_details renderable shapes" (líneas 187-281) — shape de la response cuando reasoning está activo (NO modificar el unpacking de `reasoning_details` actual; ya es correcto).
  </read_first>
  <action>
    Modifica únicamente la función `query_model` (líneas 8-59 actuales). NO toques `query_models_parallel` (Plan 03-04 lo llamará vía un wrapper si necesita pasar `reasoning`).

    1. **Añade** el kwarg `reasoning: bool = False` a la firma, justo después de `timeout: float = 120.0`. Default `False` preserva comportamiento existente.

    2. **Actualiza** la docstring para mencionar el nuevo parámetro:
       ```
       reasoning: If True, enable model reasoning via the OpenRouter `reasoning` payload
                  parameter (`{"enabled": true}`). Required for o4-mini / gpt-5.5 /
                  claude-opus-4.7 / gemini-3.1-pro-preview to expose `reasoning_details`
                  in the response. Per RESEARCH.md: `:thinking` suffix does NOT exist —
                  thinking is opt-in via payload, not via model ID.
       ```

    3. **Inyecta** el reasoning param en el payload **después** de la inyección actual de `provider.only` (línea 38) y **antes** del bloque `try` (línea 40):
       ```python
       if reasoning:
           payload["reasoning"] = {"enabled": True}
       ```

    4. **NO modifiques** el unpacking de la response (líneas 49-55). El campo `reasoning_details` ya se extrae correctamente — Plan 03-05 lo renderiza, no este plan.

    5. **NO añadas** lógica de filtrado de `reasoning.encrypted` aquí — eso es responsabilidad del frontend per RESEARCH.md §"Pitfall: array filter para encrypted-only".

    6. Añade un comment encima del bloque `if reasoning:` indicando: `# Per RESEARCH.md: reasoning is opt-in via payload, not via :thinking suffix. Anthropic Opus uses adaptive thinking; Gemini/o4-mini/gpt-5.5 expose reasoning_details when this is set.`
  </action>
  <verify>
    <automated>
      python -c "import inspect; from backend.openrouter import query_model; sig = inspect.signature(query_model); assert 'reasoning' in sig.parameters, 'reasoning kwarg missing'; assert sig.parameters['reasoning'].default is False, 'default must be False'; assert sig.parameters['reasoning'].annotation is bool, 'annotation must be bool'; print('OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reasoning: bool = False" backend/openrouter.py` retorna 1 línea (la firma).
    - `grep -n 'payload\["reasoning"\]' backend/openrouter.py` retorna 1 línea con `{"enabled": True}`.
    - `grep -c "if reasoning:" backend/openrouter.py` == 1.
    - El test python inline imprime `OK` (firma + default + annotation correctos).
    - Funcional: una llamada existente `await query_model("anthropic/claude-haiku-4.5", [...])` (sin pasar `reasoning`) produce el mismo payload que antes — el campo `reasoning` NO debe aparecer en el payload cuando no se pasa el kwarg.
    - Funcional: una llamada `await query_model("openai/o4-mini:online", [...], reasoning=True)` produce un payload con `{"model": "openai/o4-mini:online", "messages": [...], "provider": {"only": ["openai"]}, "reasoning": {"enabled": True}}`.
  </acceptance_criteria>
  <done>
    `query_model` acepta `reasoning` opcional, lo inyecta solo cuando es True, no rompe el flujo existente, BYOK routing intacto.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend ↔ OpenRouter API | Payload con nuevo campo `reasoning` cruza esta frontera. Token sigue siendo el `OPENROUTER_API_KEY` del usuario; routing BYOK preservado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `backend/openrouter.py` payload construction | accept | El nuevo payload field es controlado server-side (booleano hardcoded a `True`); ningún input de usuario fluye a él. Ningún usuario puede inyectar otros sub-keys del `reasoning` object porque la dimensión es un single boolean. |
| T-03-01-02 | Information Disclosure | `PROFILES` dict (model identifiers) | accept | Los model IDs no son secretos; aparecen en docs públicas de OpenRouter. No hay credenciales en el dict. `OPENROUTER_API_KEY` sigue cargado de env via `python-dotenv` como antes. |

Phase 3 no introduce nueva superficie de ataque novel: el cambio es additivo (un kwarg opcional + un dict de configuración). El BYOK routing existente sigue cubriendo a todos los model IDs incluyendo `:online` (split por `/` en `get_provider_for_model` ignora suffixes — verificado en RESEARCH.md).
</threat_model>

<verification>
- `python -c "from backend.config import PROFILES; print(list(PROFILES.keys()))"` → `['fast', 'quality', 'quality_research']`
- `python -c "from backend.openrouter import query_model; import inspect; print(list(inspect.signature(query_model).parameters.keys()))"` → incluye `reasoning`
- Smoke test manual (con servidor corriendo): el flujo actual (sin pasar `profile`) sigue ejecutando una deliberación end-to-end usando `PROFILES["fast"]["council_models"]` via los aliases — UI no debe romperse antes de Plan 03-02.
</verification>

<success_criteria>
- PROFILES dict con shape exacta del bloque `<interfaces>` definido en `backend/config.py`.
- `query_model` con `reasoning: bool = False` opt-in funcionando.
- BYOK allowlist preservado (`get_provider_for_model` cubre todos los new IDs).
- Aliases legacy `COUNCIL_MODELS` y `CHAIRMAN_MODEL` siguen exportados — Plan 03-02 los retirará.
- Cero cambios en otros archivos del repo.
</success_criteria>

<output>
Crear `.planning/phases/03-quality-dial-pragmatic-deep-research/03-01-SUMMARY.md` documentando:
- Shape final del dict `PROFILES`.
- Substitutions aplicadas (CD-05: `gemini-3.1-pro` → `gemini-3.1-pro-preview`).
- Confirmación de que NO se usa `:thinking` (RESEARCH override).
- Firma final de `query_model`.
</output>
