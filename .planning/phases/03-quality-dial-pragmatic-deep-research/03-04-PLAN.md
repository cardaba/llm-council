---
phase: 03-quality-dial-pragmatic-deep-research
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - backend/research_strategy.py
  - backend/council.py
  - backend/main.py
autonomous: true
requirements: [QUAL-02, RSCH-01, RSCH-02, RSCH-03, RSCH-04]
must_haves:
  truths:
    - "Existe un módulo backend/research_strategy.py que orquesta el pipeline completo de quality_research (Stages 1, 2, 3, critic, Stage 4)."
    - "council.py NO contiene branches if profile == 'quality_research' más allá de un único delegate single-line al strategy module — verificable via grep."
    - "council.py NO accede a PROFILES['quality_research'] ni a critic_model ni a stage4_threshold — verificable via grep."
    - "Stage 1 del pipeline QR llama a query_model con reasoning=True para los 4 council models (todos con :online suffix)."
    - "El critic invoca anthropic/claude-opus-4.7 con CRITIC_RUBRIC y parsea el output extrayendo CRITIC SCORE entero 1-10 + PRIMARY CONCERN."
    - "Cuando critic_score >= 8, Stage 4 NO dispara y metadata.stage4_triggered es false. Cuando critic_score < 8, Stage 4 dispara como una llamada al chairman con feedback embedded y metadata.stage4_triggered es true."
    - "Cuando critic falla en parsearse (parser retorna None), el pipeline procede sin disparar Stage 4 (graceful degradation conservadora — RESEARCH §Critic prompt design)."
    - "Los SSE events extendidos (critic_complete, stage4_start, stage4_complete) se emiten desde main.py event_generator solo cuando profile == 'quality_research'."
    - "El message persistido para QR incluye metadata.critic + metadata.stage4_triggered y, cuando aplica, un campo stage4 con la respuesta refinada + critic_score + primary_concern."
  artifacts:
    - path: "backend/research_strategy.py"
      provides: "Async generator run() que emite stage events; CRITIC_RUBRIC constante; parse_critic_score helper"
      contains: "async def run("
      min_lines: 150
    - path: "backend/council.py"
      provides: "run_full_council con delegate single-line a research_strategy"
      contains: "from . import research_strategy"
    - path: "backend/main.py"
      provides: "event_generator branch que consume research_strategy.run() y propaga events"
      contains: "research_strategy.run("
  key_links:
    - from: "backend/main.py event_generator"
      to: "research_strategy.run async generator"
      via: "async for event in research_strategy.run(content, PROFILES['quality_research']):"
      pattern: "async for event in research_strategy"
    - from: "backend/council.py run_full_council"
      to: "research_strategy.run"
      via: "delegate cuando profile == 'quality_research'"
      pattern: "research_strategy"
    - from: "research_strategy.py Stage 1"
      to: "openrouter.query_model con reasoning=True"
      via: "kwarg reasoning explícito por cada model"
      pattern: "reasoning=True"
---

<objective>
Implementar el **`research_strategy` module aislado** que orquesta el pipeline completo de `quality_research`: Stage 1 (4 reasoning models con `:online`), Stage 2 (rankings reusando helpers de council.py), Stage 3 (chairman synthesis), critic invocation con CRITIC_RUBRIC y parser, y Stage 4 condicional refinement. El strategy expone un async generator de stage events compatible con el SSE per-stage existente del frontend.

Este plan cierra la inversión arquitectónica de RSCH-04: cuando RSCH-V2-01 (fully-agentic deep research) llegue, reemplaza el cuerpo de `research_strategy.run()` sin tocar `council.py`. El delegate en council.py es UNA línea (un `async for ... yield`).

Purpose: El profile `quality_research` deja de ser un placeholder y empieza a producir deliberaciones reales con web search + reasoning + critic-gated refinement. Cierra QUAL-02 (extensión QR) + RSCH-01 + RSCH-02 + RSCH-03 + RSCH-04.

Output:
- `backend/research_strategy.py` (NUEVO, ~200 líneas) con:
  - `CRITIC_RUBRIC` constante (CD-03 — Claude propone, user calibra).
  - `parse_critic_score(text) -> tuple[int|None, str|None]` helper.
  - `async def run(user_query, profile_config)` async generator que emite stage events idénticos en shape a los actuales (`stage1_start`, `stage1_complete`, `stage2_*`, `stage3_*`) más `critic_complete`, `stage4_start`, `stage4_complete`.
- `backend/council.py` con el placeholder `raise NotImplementedError` REEMPLAZADO por un delegate single-line (`from . import research_strategy` import + `return await research_strategy.run(...)` o equivalente para non-streaming, depending on shape).
- `backend/main.py event_generator` consume el async generator del strategy en el branch QR y propaga events al frontend.
- `backend/main.py send_message` (no-streaming endpoint) también soporta QR consumiendo el strategy en modo "collect-all-events-then-return-tuple" o invocando una variante async-no-generator del strategy.
- Persistencia en JSON: para QR, metadata incluye `critic` + `stage4_triggered`; cuando Stage 4 dispara, el message dict gana un campo `stage4 = {response, critic_score, primary_concern}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-quality-dial-pragmatic-deep-research/03-CONTEXT.md
@.planning/phases/03-quality-dial-pragmatic-deep-research/03-RESEARCH.md
@.planning/phases/03-quality-dial-pragmatic-deep-research/03-01-PLAN.md
@.planning/phases/03-quality-dial-pragmatic-deep-research/03-02-PLAN.md
@.planning/phases/03-quality-dial-pragmatic-deep-research/03-03-PLAN.md

@backend/council.py
@backend/main.py
@backend/openrouter.py
@backend/config.py

<interfaces>
<!-- Contratos del módulo nuevo + delegate. Plan 03-05 consumirá los SSE events nuevos. -->

`backend/research_strategy.py` API (D-01 Thick + D-03 streaming-compatible):

```python
"""Research strategy for the `quality_research` profile.

Owns the full orchestration of Stage 1 + Stage 2 + Stage 3 + critic + Stage 4
for the `quality_research` profile. Council.py delegates to this module via a
single line so that future agentic deep-research (RSCH-V2-01) can replace
`run()` without touching council.py.

Per RESEARCH.md:
- Stage 1 council uses 4 reasoning models all with `:online` suffix; reasoning
  is enabled via payload param `reasoning={"enabled": true}`, NOT via :thinking
  suffix (which does not exist).
- `:online` plugin is deprecated in favour of `openrouter:web_search` server
  tool; v1 keeps `:online`. Migration is RSCH-V2-XX.
- Critic uses LLM-as-judge with anchored "CRITIC SCORE:" parser, conservative
  fallback (skip Stage 4 on parse failure).
- Stage 1 prompts: each model receives the user_query verbatim + reasoning enabled.
- Stage 2 reuses council.parse_ranking_from_text + calculate_aggregate_rankings.
- Stage 3 reuses the existing chairman_prompt shape from council.py.
"""

from typing import AsyncGenerator, Dict, Any, Optional, Tuple
import json
import re

from .openrouter import query_model, query_models_parallel
from .council import (
    parse_ranking_from_text,
    calculate_aggregate_rankings,
)


CRITIC_RUBRIC = """You are an independent quality reviewer evaluating a synthesized answer from an LLM council.

Score the answer 1-10 on these four dimensions, then output a SINGLE overall score
that reflects whether this answer is ready to ship to the user, or whether it
should be refined.

Dimensions:
1. **Groundedness** — Does the answer rely on the council's research / web sources, or does it speculate? Citations or specific facts strengthen this dimension.
2. **Completeness** — Does it address the full scope of the user's question, or does it skip aspects?
3. **Clarity** — Is it free of unnecessary hedging ("I'm not sure", "it depends") that the question doesn't require?
4. **Consistency** — Does it align with the council's aggregate ranking — i.e., does it incorporate the strongest insights, not the weakest?

Score scale anchors:
- 9-10: Ship as-is. Strong on all dimensions.
- 7-8: Ship as-is. Minor weakness in 1-2 dimensions but overall solid.
- 5-6: Borderline. Refinement would meaningfully improve it.
- 1-4: Refinement required. Material gap in 2+ dimensions.

User question:
{question}

Council aggregate ranking (best to worst):
{aggregate_ranking_summary}

Synthesized answer to evaluate:
{chairman_synthesis}

Output your evaluation in EXACTLY this format:

CRITIC SCORE: <integer 1-10>
GROUNDEDNESS: <integer 1-10> — <one-sentence justification>
COMPLETENESS: <integer 1-10> — <one-sentence justification>
CLARITY: <integer 1-10> — <one-sentence justification>
CONSISTENCY: <integer 1-10> — <one-sentence justification>
PRIMARY CONCERN: <one sentence — the single most important reason for the overall score>

Output nothing else after this block.
"""


STAGE4_PROMPT = """The chairman synthesized this answer:

{synthesis}

An independent critic flagged these issues:

{critic_reason}

Score: {critic_score}/10. Threshold for refinement is <8.

Produce a refined answer addressing the critic's points. Keep what works; revise what was flagged. Do NOT mention the critic in the output.
"""


def parse_critic_score(text: str) -> Tuple[Optional[int], Optional[str]]:
    """
    Returns (overall_score, primary_concern) or (None, None) if parse fails.
    Defensive: handles models that add prose before/after, capitalization variation.
    """
    score_matches = list(re.finditer(r'CRITIC SCORE:\s*(\d+)', text, re.IGNORECASE))
    if not score_matches:
        return None, None
    score = int(score_matches[-1].group(1))
    score = max(1, min(10, score))

    concern_match = re.search(
        r'PRIMARY CONCERN:\s*(.+?)(?:\n\n|\Z)',
        text, re.IGNORECASE | re.DOTALL,
    )
    concern = concern_match.group(1).strip() if concern_match else None
    return score, concern


async def run(user_query: str, profile_config: Dict[str, Any]) -> AsyncGenerator[Dict[str, Any], None]:
    """
    Async generator that yields stage event dicts for the quality_research pipeline.

    Each yielded dict has shape compatible with main.py event_generator:
        {"type": "stage1_start"} | {"type": "stage1_complete", "data": [...]}
        {"type": "stage2_start"} | {"type": "stage2_complete", "data": [...], "metadata": {...}}
        {"type": "stage3_start"} | {"type": "stage3_complete", "data": {...}}
        {"type": "critic_complete", "data": {"score": int, "concern": str|None}}
        {"type": "stage4_start"}    # only when score < threshold
        {"type": "stage4_complete", "data": {"response": str, "critic_score": int, "primary_concern": str}}

    A final yield carries the consolidated tuple-like shape used by storage:
        {"type": "_final", "stage1": [...], "stage2": [...], "stage3": {...},
         "stage4": {...} | None, "message_metadata": {...}}

    main.py consumes events of type starting with "stage"/"critic" and emits them as SSE,
    swallows the "_final" event and uses its payload to call storage.add_assistant_message.
    """
    council_models = profile_config["council_models"]
    chairman_model = profile_config["chairman_model"]
    critic_model = profile_config["critic_model"]
    threshold = profile_config["stage4_threshold"]

    # --- Stage 1: 4 reasoning models with :online + reasoning=True ---
    yield {"type": "stage1_start"}
    messages = [{"role": "user", "content": user_query}]
    # query_models_parallel does not yet accept reasoning kwarg; use asyncio.gather directly.
    import asyncio
    tasks = [query_model(m, messages, reasoning=True) for m in council_models]
    raw_responses = await asyncio.gather(*tasks)
    stage1_results = []
    for model, response in zip(council_models, raw_responses):
        if response is not None:
            stage1_results.append({
                "model": model,
                "response": response.get("content", ""),
                "reasoning_details": response.get("reasoning_details"),
            })
    yield {"type": "stage1_complete", "data": stage1_results}

    if not stage1_results:
        # All models failed; abort with a stage3-like error result for storage compat
        yield {
            "type": "_final",
            "stage1": [],
            "stage2": [],
            "stage3": {"model": "error", "response": "All research models failed."},
            "stage4": None,
            "message_metadata": {
                "profile": "quality_research",
                "models": council_models,
                "chairman": chairman_model,
                "critic": critic_model,
                "stage4_triggered": False,
            },
        }
        return

    # --- Stage 2: rankings (reuse council.py helpers) ---
    yield {"type": "stage2_start"}
    labels = [chr(65 + i) for i in range(len(stage1_results))]
    label_to_model = {f"Response {label}": r["model"] for label, r in zip(labels, stage1_results)}
    responses_text = "\n\n".join(
        f"Response {label}:\n{r['response']}" for label, r in zip(labels, stage1_results)
    )
    ranking_prompt = (
        "You are evaluating different responses to the following question:\n\n"
        f"Question: {user_query}\n\n"
        f"Here are the responses from different models (anonymized):\n\n{responses_text}\n\n"
        "Your task:\n"
        "1. First, evaluate each response individually.\n"
        "2. Then, at the very end, provide a final ranking.\n\n"
        "IMPORTANT: Your final ranking MUST be formatted EXACTLY as follows:\n"
        "- Start with the line \"FINAL RANKING:\" (all caps, with colon)\n"
        "- Then list the responses from best to worst as a numbered list\n"
        "- Each line should be: number, period, space, then ONLY the response label (e.g., \"1. Response A\")\n"
        "- Do not add any other text or explanations in the ranking section\n"
    )
    ranking_messages = [{"role": "user", "content": ranking_prompt}]
    ranking_tasks = [query_model(m, ranking_messages, reasoning=True) for m in council_models]
    ranking_raw = await asyncio.gather(*ranking_tasks)
    stage2_results = []
    for model, response in zip(council_models, ranking_raw):
        if response is not None:
            full_text = response.get("content", "")
            stage2_results.append({
                "model": model,
                "ranking": full_text,
                "parsed_ranking": parse_ranking_from_text(full_text),
            })
    aggregate_rankings = calculate_aggregate_rankings(stage2_results, label_to_model)
    yield {
        "type": "stage2_complete",
        "data": stage2_results,
        "metadata": {"label_to_model": label_to_model, "aggregate_rankings": aggregate_rankings},
    }

    # --- Stage 3: chairman synthesis ---
    yield {"type": "stage3_start"}
    stage1_text = "\n\n".join(f"Model: {r['model']}\nResponse: {r['response']}" for r in stage1_results)
    stage2_text = "\n\n".join(f"Model: {r['model']}\nRanking: {r['ranking']}" for r in stage2_results)
    chairman_prompt = (
        "You are the Chairman of an LLM Council. Multiple AI models have provided responses to a user's question, and then ranked each other's responses.\n\n"
        f"Original Question: {user_query}\n\n"
        f"STAGE 1 - Individual Responses:\n{stage1_text}\n\n"
        f"STAGE 2 - Peer Rankings:\n{stage2_text}\n\n"
        "Your task as Chairman is to synthesize all of this information into a single, comprehensive, accurate answer to the user's original question.\n"
    )
    chairman_response = await query_model(chairman_model, [{"role": "user", "content": chairman_prompt}], reasoning=True)
    if chairman_response is None:
        stage3_result = {"model": chairman_model, "response": "Error: Unable to generate final synthesis."}
    else:
        stage3_result = {
            "model": chairman_model,
            "response": chairman_response.get("content", ""),
            "reasoning_details": chairman_response.get("reasoning_details"),
        }
    yield {"type": "stage3_complete", "data": stage3_result}

    # --- Critic invocation ---
    aggregate_summary = "\n".join(
        f"{i+1}. {row['model']} (avg rank {row['average_rank']})"
        for i, row in enumerate(aggregate_rankings)
    )
    critic_prompt = CRITIC_RUBRIC.format(
        question=user_query,
        aggregate_ranking_summary=aggregate_summary,
        chairman_synthesis=stage3_result["response"],
    )
    critic_response = await query_model(critic_model, [{"role": "user", "content": critic_prompt}])
    critic_score, primary_concern = (None, None)
    if critic_response is not None:
        critic_score, primary_concern = parse_critic_score(critic_response.get("content", ""))
    yield {"type": "critic_complete", "data": {"score": critic_score, "concern": primary_concern}}

    # --- Stage 4 conditional ---
    stage4_result = None
    stage4_triggered = False
    # Conservative fallback: skip Stage 4 if parse failed (per RESEARCH.md "conservative skip")
    if critic_score is not None and critic_score < threshold:
        yield {"type": "stage4_start"}
        stage4_prompt = STAGE4_PROMPT.format(
            synthesis=stage3_result["response"],
            critic_reason=primary_concern or "(no specific concern parsed)",
            critic_score=critic_score,
        )
        stage4_raw = await query_model(chairman_model, [{"role": "user", "content": stage4_prompt}], reasoning=True)
        if stage4_raw is not None:
            stage4_result = {
                "model": chairman_model,
                "response": stage4_raw.get("content", ""),
                "reasoning_details": stage4_raw.get("reasoning_details"),
                "critic_score": critic_score,
                "primary_concern": primary_concern,
            }
            stage4_triggered = True
            yield {"type": "stage4_complete", "data": stage4_result}
        else:
            # Stage 4 failed — graceful degradation per CONTEXT.md "Si Stage 4 falla → fallback"
            stage4_triggered = False

    # --- Final consolidation event for storage ---
    yield {
        "type": "_final",
        "stage1": stage1_results,
        "stage2": stage2_results,
        "stage3": stage3_result,
        "stage4": stage4_result,
        "message_metadata": {
            "profile": "quality_research",
            "models": council_models,
            "chairman": chairman_model,
            "critic": critic_model,
            "stage4_triggered": stage4_triggered,
        },
    }
```

Council.py delegate (replaces the placeholder from Plan 03-02):

```python
# backend/council.py — single-line delegate, no QR-specific config access
from . import research_strategy

async def run_full_council(user_query: str, profile: str = "fast") -> Tuple[List, List, Dict, Dict]:
    config = PROFILES[profile]
    if profile == "quality_research":
        # Collect events into the legacy tuple shape for the non-streaming endpoint.
        # The streaming endpoint in main.py consumes research_strategy.run() directly.
        stage1_results = stage2_results = []
        stage3_result = {}
        message_metadata = {}
        async for event in research_strategy.run(user_query, config):
            if event["type"] == "_final":
                stage1_results = event["stage1"]
                stage2_results = event["stage2"]
                stage3_result = event["stage3"]
                message_metadata = event["message_metadata"]
        return stage1_results, stage2_results, stage3_result, {"message_metadata": message_metadata}
    # ... rest unchanged from Plan 03-02 ...
```

main.py event_generator branch for QR (replaces the placeholder error event from Plan 03-02):

```python
if request.profile == "quality_research":
    stage4_data = None
    final_metadata = None
    final_stage1 = final_stage2 = []
    final_stage3 = {}
    async for event in research_strategy.run(request.content, PROFILES["quality_research"]):
        if event["type"] == "_final":
            final_stage1 = event["stage1"]
            final_stage2 = event["stage2"]
            final_stage3 = event["stage3"]
            stage4_data = event["stage4"]
            final_metadata = event["message_metadata"]
        else:
            yield f"data: {json.dumps(event)}\n\n"
    # title generation block (existente) ...
    # Persist with stage4 (if any)
    storage.add_assistant_message(
        conversation_id, final_stage1, final_stage2, final_stage3,
        metadata=final_metadata, stage4=stage4_data,
    )
    yield f"data: {json.dumps({'type': 'message_metadata', 'data': final_metadata})}\n\n"
    yield f"data: {json.dumps({'type': 'complete'})}\n\n"
    return  # exit event_generator
```

storage.add_assistant_message extension — un kwarg adicional `stage4: Optional[Dict[str, Any]] = None` que añade `message["stage4"] = stage4` al dict cuando no es None. Plan 03-03 dejó el kwarg metadata; aquí añadimos stage4. NO afecta backwards compat — para fast/quality nadie pasa stage4.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear backend/research_strategy.py con CRITIC_RUBRIC + parse_critic_score + run() async generator</name>
  <files>backend/research_strategy.py</files>
  <read_first>
    - backend/council.py (post Plan 03-02) — para ver shape de stage1/2/3 helpers que vas a reusar (parse_ranking_from_text, calculate_aggregate_rankings).
    - backend/openrouter.py (post Plan 03-01) — para confirmar que `query_model` acepta `reasoning=True`.
    - backend/config.py — para ver el shape de PROFILES["quality_research"].
    - .planning/phases/03-quality-dial-pragmatic-deep-research/03-RESEARCH.md §"Critic prompt design" (líneas 286+) y §"reasoning_details renderable shapes" (líneas 187-281) y §"OpenRouter model availability" §"Suffix" (líneas 94-122).
    - .planning/phases/03-quality-dial-pragmatic-deep-research/03-CONTEXT.md §"Stage 4 refinement gating" (D-05 a D-09) y §specifics (Stage 4 refinement prompt + critic prompt template).
  </read_first>
  <action>
    Crea el archivo nuevo `backend/research_strategy.py` con la shape exacta del bloque `<interfaces>` arriba. Notas:

    1. **El módulo NO importa de council.py constantes ni model lists** — sólo importa **helpers profile-agnostic** (`parse_ranking_from_text`, `calculate_aggregate_rankings`). RSCH-04 isolation: el strategy es self-contained respecto a config QR-específica.

    2. **`reasoning=True` se pasa explícitamente** a `query_model` en Stage 1, Stage 2 (rankings — los reasoning models también razonan al rankear), Stage 3 (chairman synthesis), Stage 4 (chairman refinement). El critic NO lleva `reasoning=True` porque es un juicio determinista, no requiere chain-of-thought visible.

    3. **NO uses `query_models_parallel`** existente porque ese helper no acepta `reasoning` kwarg. En lugar usa `asyncio.gather` directo con `query_model(m, messages, reasoning=True)` per cada model. (Razón: extender `query_models_parallel` retroactivamente impactaría el flujo `fast`/`quality` que NO quiere reasoning. Mantener el helper viejo intacto y construir el gather inline aquí preserva isolation.)

    4. **El parser `parse_critic_score`** debe ser **defensivo**: usar `re.IGNORECASE`, anclar en `CRITIC SCORE:` (la última ocurrencia, por si el modelo echoes el header en su prosa), clamp a 1-10, devolver `(None, None)` cuando no haya match. Tomar el patrón VERBATIM de RESEARCH.md líneas 344-363.

    5. **Conservative skip on parse failure (RESEARCH.md líneas 366-368):** si `parse_critic_score` retorna `(None, None)`, NO disparar Stage 4. Es preferible ahorrar el coste de refinement cuando no podemos juzgar la calidad que ejecutar refinement ciegamente.

    6. **Graceful degradation cuando Stage 4 query falla:** `query_model` retorna None on failure. En ese caso `stage4_triggered = False` y NO emitas `stage4_complete` (el frontend no renderizará la sub-sección). Documenta este path explícitamente en un comment.

    7. **El event final `_final`** es un convention interna entre el strategy y main.py / council.py. Empezar el type con underscore señala que NO es un SSE event (main.py lo intercepta antes de yield SSE).

    8. **Annotations de OpenRouter web search NO se capturan en v1** (RESEARCH.md §"Citation extraction" — RSCH-V2-02 las usará). Añade un comment en el bloque de Stage 1 indicando que existe `annotations` en `data['choices'][0]['message']['annotations']` y que es deferred.

    9. **Pitfall registrado en comments:** RESEARCH.md §"Pitfall self-preference" — el critic = chairman family (Opus). Aceptado por D-06; añade un comment "# KNOWN RISK: critic and chairman are same model family. Recalibrate threshold (CD-04) if scores skew high."

    10. **Path de migración a server tool documentado en docstring** (RESEARCH.md §"`:online` plugin behavior" — `:online` deprecated, future v2 migrates to `tools=[{type:"openrouter:web_search"}]`).

    11. **Quirks de Opus 4.7 documentados en docstring** (RESEARCH.md líneas 116-122 — temperature/top_p/top_k ignorados; reasoning.max_tokens ignorado; verbosity:"xhigh" disponible). No afectan v1 single-shot pero quedan registrados.

    El archivo final debe ser ~200 líneas. Si supera 280 líneas, los blocks Stage 2 y Stage 3 prompt se pueden extraer a constantes module-level (`STAGE2_PROMPT_TEMPLATE`, `STAGE3_PROMPT_TEMPLATE`) — pero NO duplicar lógica de council.py más allá de los prompt strings.
  </action>
  <verify>
    <automated>
      python -c "import inspect; from backend import research_strategy; assert hasattr(research_strategy, 'CRITIC_RUBRIC'); assert hasattr(research_strategy, 'parse_critic_score'); assert hasattr(research_strategy, 'run'); assert inspect.isasyncgenfunction(research_strategy.run), 'run must be async generator'; s, c = research_strategy.parse_critic_score('CRITIC SCORE: 7\n...\nPRIMARY CONCERN: missing groundedness\n'); assert s == 7 and c == 'missing groundedness', (s, c); s2, c2 = research_strategy.parse_critic_score('garbage'); assert s2 is None and c2 is None; print('OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `backend/research_strategy.py` exists.
    - `grep -c "CRITIC_RUBRIC = " backend/research_strategy.py` == 1 (constante module-level).
    - `grep -c "STAGE4_PROMPT" backend/research_strategy.py` ≥ 1 (constante o prompt local).
    - `grep -c "async def run" backend/research_strategy.py` == 1.
    - `grep -c "def parse_critic_score" backend/research_strategy.py` == 1.
    - `grep -c 'reasoning=True' backend/research_strategy.py` ≥ 4 (al menos en Stage 1, Stage 2, Stage 3, Stage 4 — el critic NO lleva reasoning).
    - `grep -c '"type": "stage4_start"' backend/research_strategy.py` == 1.
    - `grep -c '"type": "critic_complete"' backend/research_strategy.py` == 1.
    - `grep -c '"type": "_final"' backend/research_strategy.py` == 1.
    - `grep -c "PROFILES\\[" backend/research_strategy.py` == 0 (el module recibe `profile_config` por param, no importa PROFILES — RSCH-04 isolation; el caller en main.py / council.py es quien pasa `PROFILES["quality_research"]`).
    - El test python inline imprime `OK`.
  </acceptance_criteria>
  <done>
    Module aislado, async generator funcional, critic + Stage 4 implementados con graceful degradation y conservative skip on parse failure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reemplazar placeholder en council.py con delegate single-line + extender storage.add_assistant_message para stage4</name>
  <files>backend/council.py, backend/storage.py</files>
  <read_first>
    - backend/council.py (post Plan 03-02) — específicamente el bloque `if profile == "quality_research"` que tiene el `raise NotImplementedError`.
    - backend/storage.py (post Plan 03-03) — específicamente `add_assistant_message` con kwarg metadata.
    - .planning/phases/03-quality-dial-pragmatic-deep-research/03-RESEARCH.md §"User Constraints" sección D-02 (council.py NO importa research config).
  </read_first>
  <action>
    **Cambios en `backend/council.py`:**

    1. **Añade el import al header** (justo después del import de openrouter):
       ```python
       from . import research_strategy
       ```

    2. **Reemplaza el bloque placeholder** (el `raise NotImplementedError` de Plan 03-02) con un delegate que consume el async generator del strategy y construye la tupla legacy para el endpoint no-streaming:
       ```python
       if profile == "quality_research":
           stage1_results, stage2_results = [], []
           stage3_result = {}
           message_metadata = {}
           stage4_result = None
           async for event in research_strategy.run(user_query, config):
               if event["type"] == "_final":
                   stage1_results = event["stage1"]
                   stage2_results = event["stage2"]
                   stage3_result = event["stage3"]
                   stage4_result = event.get("stage4")
                   message_metadata = event["message_metadata"]
           # Pack message_metadata + stage4 into the legacy "metadata" return slot so
           # main.py.send_message can persist them. The non-streaming endpoint loses
           # per-stage SSE granularity but preserves the final shape.
           combined_metadata = {
               "message_metadata": message_metadata,
               "stage4": stage4_result,
           }
           return stage1_results, stage2_results, stage3_result, combined_metadata
       ```

    3. Verifica via grep que:
       - `grep -c 'raise NotImplementedError' backend/council.py` == 0 (el placeholder fue reemplazado).
       - `grep -c 'PROFILES\\["quality_research"\\]' backend/council.py` == 0 (la config de QR se accede vía `config = PROFILES[profile]` que ya estaba ahí, NO directamente con string literal).
       - `grep -c "critic_model\|stage4_threshold\|CRITIC_RUBRIC" backend/council.py` == 0 (RSCH-04 isolation: ningún literal QR-specific en council.py).

    **Cambios en `backend/storage.py`:**

    4. **Extiende `add_assistant_message`** (la firma post Plan 03-03 ya tiene `metadata=None`; añade un kwarg más):
       ```python
       def add_assistant_message(
           conversation_id: str,
           stage1: List[Dict[str, Any]],
           stage2: List[Dict[str, Any]],
           stage3: Dict[str, Any],
           metadata: Optional[Dict[str, Any]] = None,
           stage4: Optional[Dict[str, Any]] = None,
       ):
           # ... existing body ...
           message = {
               "role": "assistant",
               "stage1": stage1,
               "stage2": stage2,
               "stage3": stage3,
           }
           if metadata is not None:
               message["metadata"] = metadata
           if stage4 is not None:
               message["stage4"] = stage4
           conversation["messages"].append(message)
           save_conversation(conversation)
       ```
       Documenta en el docstring que `stage4` solo se persiste para `quality_research` cuando el critic gatilló refinement.

    5. **NO modifiques** otras funciones del storage. NO migrar archivos existentes.
  </action>
  <verify>
    <automated>
      python -c "import inspect; from backend.storage import add_assistant_message; sig = inspect.signature(add_assistant_message); assert 'stage4' in sig.parameters; assert sig.parameters['stage4'].default is None; print('OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from . import research_strategy" backend/council.py` == 1.
    - `grep -c "raise NotImplementedError" backend/council.py` == 0 (placeholder eliminado).
    - `grep -c 'PROFILES\["quality_research"\]' backend/council.py` == 0 (RSCH-04: solo se accede a `PROFILES[profile]` con var, no con literal string).
    - `grep -c "critic_model" backend/council.py` == 0.
    - `grep -c "stage4_threshold" backend/council.py` == 0.
    - `grep -c "CRITIC_RUBRIC" backend/council.py` == 0.
    - `grep -c "if profile == \"quality_research\":" backend/council.py` == 1 (el único branch QR; es el delegate).
    - `grep -c "research_strategy.run" backend/council.py` == 1.
    - `grep -c "stage4: Optional\[Dict\[str, Any\]\] = None" backend/storage.py` == 1.
    - `grep -c 'message\["stage4"\] = stage4' backend/storage.py` == 1.
    - El test python inline imprime `OK`.
    - Funcional via REPL: `await run_full_council("test", "quality_research")` retorna una tupla 4-tuple sin errors (puede demorar ~10-30s por el pipeline real con 4× online).
  </acceptance_criteria>
  <done>
    Council.py delega QR vía single import + single async-for; placeholder eliminado; storage soporta stage4 opcional; RSCH-04 isolation verificado vía grep.
  </done>
</task>

<task type="auto">
  <name>Task 3: main.py event_generator consume research_strategy.run() y emite SSE events nuevos</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py (post Plan 03-03) — específicamente el `event_generator` y el placeholder `quality_research` que emite error.
    - backend/research_strategy.py (creado en Task 1) — para conocer el shape de events emitidos.
  </read_first>
  <action>
    Modifica el `event_generator` en `backend/main.py`. Cambios:

    1. **Añade import** al header:
       ```python
       from . import research_strategy
       ```

    2. **Reemplaza el bloque placeholder de quality_research** (el que emite `'error'` event con mensaje "lands in Plan 03-04") con el delegate al strategy:
       ```python
       if request.profile == "quality_research":
           # Strategy module owns the full QR pipeline (D-01 thick API).
           # We pass-through every event whose type does NOT start with '_';
           # the underscore-prefixed '_final' event carries the consolidated
           # tuple shape used to persist the message.
           final_stage1 = []
           final_stage2 = []
           final_stage3 = {}
           final_stage4 = None
           final_message_metadata = None

           async for event in research_strategy.run(request.content, PROFILES["quality_research"]):
               if event["type"] == "_final":
                   final_stage1 = event["stage1"]
                   final_stage2 = event["stage2"]
                   final_stage3 = event["stage3"]
                   final_stage4 = event["stage4"]
                   final_message_metadata = event["message_metadata"]
               else:
                   yield f"data: {json.dumps(event)}\n\n"

           # Title generation (parallel, started before the strategy ran)
           if title_task is not None:
               title = await title_task
               storage.update_conversation_title(conversation_id, title)
               yield f"data: {json.dumps({'type': 'title_complete', 'data': {'title': title}})}\n\n"

           # Persist
           storage.add_assistant_message(
               conversation_id,
               final_stage1, final_stage2, final_stage3,
               metadata=final_message_metadata,
               stage4=final_stage4,
           )

           # Emit message_metadata so the frontend hydrates the saved-message header
           yield f"data: {json.dumps({'type': 'message_metadata', 'data': final_message_metadata})}\n\n"
           yield f"data: {json.dumps({'type': 'complete'})}\n\n"
           return
       ```

    3. **NO toques** el branch fast/quality (Plan 03-03 ya lo tiene correcto). NO toques el manejo de excepciones (líneas 197-201).

    4. **Reordena title_task si es necesario:** el strategy run es largo (~10-30s), pero `title_task` se inicia ANTES del bloque QR, idéntico al flujo actual. Verifica que el `title_task` se inicia ANTES del `if request.profile == "quality_research"` y se await DESPUÉS de la última event yielded.

    5. **send_message (no-streaming)** ya delega a `run_full_council` que ahora soporta QR vía Plan 03-04 Task 2 (council.py delegate). Modifica el handler para extraer `stage4` del metadata combinado:
       ```python
       stage1_results, stage2_results, stage3_result, combined_metadata = await run_full_council(
           request.content, request.profile,
       )
       
       if request.profile == "quality_research":
           message_metadata = combined_metadata.get("message_metadata", {})
           stage4_data = combined_metadata.get("stage4")
       else:
           # fast/quality: combined_metadata es el legacy {label_to_model, aggregate_rankings}
           profile_config = PROFILES[request.profile]
           message_metadata = {
               "profile": request.profile,
               "models": profile_config["council_models"],
               "chairman": profile_config["chairman_model"],
           }
           stage4_data = None
       
       storage.add_assistant_message(
           conversation_id, stage1_results, stage2_results, stage3_result,
           metadata=message_metadata,
           stage4=stage4_data,
       )
       
       return {
           "stage1": stage1_results,
           "stage2": stage2_results,
           "stage3": stage3_result,
           "stage4": stage4_data,
           "metadata": combined_metadata if request.profile != "quality_research" else None,
           "message_metadata": message_metadata,
       }
       ```

    6. **NO emitas** `stage4_*` events para fast/quality (esos profiles nunca disparan Stage 4).

    7. **Documenta el ordering de events** en un comment al inicio del block QR:
       ```
       # SSE event order for quality_research:
       #   stage1_start → stage1_complete{data}
       #   stage2_start → stage2_complete{data, metadata}
       #   stage3_start → stage3_complete{data}
       #   critic_complete{data: {score, concern}}
       #   [optional] stage4_start → stage4_complete{data}
       #   [optional] title_complete{data}
       #   message_metadata{data}
       #   complete
       ```
  </action>
  <verify>
    <automated>
      grep -c "research_strategy.run" backend/main.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from . import research_strategy" backend/main.py` == 1.
    - `grep -c "research_strategy.run(request.content" backend/main.py` == 1.
    - `grep -c "lands in Plan 03-04" backend/main.py` == 0 (placeholder eliminado).
    - `grep -c '"type": "_final"' backend/main.py` ≥ 1 (la check para no emitir final como SSE).
    - `grep -c "stage4=final_stage4" backend/main.py` == 1 (persistencia stage4).
    - Funcional via curl SSE: `curl -N -X POST .../message/stream -d '{"content":"who won the latest f1 race?","profile":"quality_research"}'` produce stream con events en orden: `stage1_start, stage1_complete, stage2_start, stage2_complete, stage3_start, stage3_complete, critic_complete, [stage4_start, stage4_complete?], message_metadata, complete`.
    - Funcional: tras una QR query, el JSON guardado en disco contiene `messages[N].metadata = {"profile":"quality_research", "models":[...4 :online...], "chairman":"...", "critic":"...", "stage4_triggered": bool}` y, si stage4_triggered=true, `messages[N].stage4 = {response, critic_score, primary_concern, ...}`.
    - Funcional: la query QR no rompe los profiles fast/quality (regresión).
  </acceptance_criteria>
  <done>
    main.py orquesta el strategy via async-for limpio; persistencia consistente con el shape extendido; SSE events nuevos visibles al cliente.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → OpenRouter (web search) | Stage 1 invoca 4 modelos `:online`; OpenRouter dispatcheaa Exa/native search. La respuesta puede contener `annotations` (URLs externas). |
| Backend → Critic LLM | El critic recibe el chairman synthesis (que contiene contenido potencialmente generated por terceras fuentes via `:online`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Critic prompt input | accept | El user_query del usuario se inyecta tal cual en el critic prompt. Como ya se inyecta en Stage 1/2/3, no es nueva superficie. Prompt injection desde web search results es un riesgo conocido del modelo (no del transporte) — RSCH-V2-XX trabajará en sanitization si es necesario. |
| T-03-04-02 | Information Disclosure | `:online` web search results | accept | OpenRouter Exa search expone URLs públicas a los modelos. No salen secretos del backend. La query del usuario va a OpenRouter como ya iba. |
| T-03-04-03 | Denial of Service | QR pipeline timeouts | mitigate | `query_model` ya tiene `timeout=120s` por llamada. Pipeline completo puede tomar ~120s acumulados; aceptable per design (CONCERNS.md F-06 — loading 15-30s ya conocido, QR amplía a 30-60s). El frontend ya muestra spinner per-stage. |
| T-03-04-04 | Self-preference (LLM-as-judge) | Critic = chairman family (Opus) | accept | RESEARCH.md §"Pitfalls específicos al LLM-as-judge" — riesgo conocido. Mitigación: critic invocation es independiente con prompt distinto (D-06). Calibración manual post-deploy (CD-04) ajusta threshold si sesgo se manifiesta. |

Phase 3 no introduce nueva superficie de ataque novel respecto a las que ya existen para fast/quality (BYOK routing, prompt injection en LLM responses, single-user local). El web search via `:online` introduce contenido externo en el contexto de los modelos, pero el blast radius está contenido al pipeline mismo — no se persiste contenido externo bruto en el JSON de la conversación más allá del response markdown que ya se guardaba.
</threat_model>

<verification>
- `grep -L 'PROFILES\["quality_research"\]' backend/council.py` debe imprimir el path (significa NO match — RSCH-04 isolation).
- `grep -c "if profile == \"quality_research\":" backend/council.py` == 1 (delegate única).
- Pipeline E2E manual: lanzar UI → forzar `profile=quality_research` (POST manual con curl o Plan 03-05 toggle) → verificar SSE stream entrega los 7-9 events esperados → verificar JSON guardado tiene shape D-25 + stage4 cuando aplica.
- Test critic parser: `parse_critic_score("CRITIC SCORE: 9\nPRIMARY CONCERN: minor hedging\n")` → `(9, "minor hedging")`. `parse_critic_score("garbage")` → `(None, None)`.
</verification>

<success_criteria>
- `backend/research_strategy.py` exists con shape exacta del bloque `<interfaces>`.
- `backend/council.py` delegate single-line; cero referencias a config QR-específica.
- `backend/main.py` event_generator pasa-through events del strategy y persiste correctamente.
- QR pipeline produce respuestas reales con web search + reasoning + critic + Stage 4 condicional.
- Persistencia incluye `metadata.stage4_triggered` correcto + campo `stage4` cuando aplica.
- Fast / quality regression-free.
</success_criteria>

<output>
Crear `.planning/phases/03-quality-dial-pragmatic-deep-research/03-04-SUMMARY.md` documentando:
- Verificación grep de RSCH-04 isolation (council.py limpio).
- Sample de un message_metadata + stage4 persistido en JSON real.
- Cualquier desviación encontrada respecto a CONTEXT.md (e.g. CD-05 substitutions ya aplicadas en Plan 03-01).
- Calibración inicial sugerida del threshold tras inspección (CD-04 — opcional, basada en las primeras queries reales).
</output>
