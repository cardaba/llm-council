---
phase: 08-sticky-ia-polish-bundle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/BackToTopButton.jsx
  - frontend/src/components/BackToTopButton.css
autonomous: true
requirements:
  - A11Y-V2.1-01
must_haves:
  truths:
    - "User scrolling > 600px of .messages-container sees the back-to-top button fade in"
    - "User scrolling back to ≤ 600px sees the button fade out"
    - "The button's text colour passes WCAG AA contrast against its background in both light and dark themes"
    - "The button carries aria-label=\"Back to top\" (English, per D-10)"
    - "Under prefers-reduced-motion: reduce, the fade collapses to instant and the click scrolls instantly (no smooth-scroll)"
  artifacts:
    - path: "frontend/src/components/BackToTopButton.jsx"
      provides: "Threshold lowered from 800 to 600"
    - path: "frontend/src/components/BackToTopButton.css"
      provides: "Contrast bump via color token swap; optional background-token escalation if AA fails"
  key_links:
    - from: "el.scrollTop > 600"
      to: "setVisible(true)"
      via: "scroll handler in useEffect"
      pattern: "scrollTop > 600"
---

<objective>
Bring the existing scroll-to-top button to the A11Y-V2.1-01 baseline:
1. Lower the visibility threshold from `> 800` (BackToTopButton.jsx:18) to `> 600` (D-12).
2. Bump foreground colour from `var(--color-fg-secondary)` to `var(--color-fg-primary)` (D-11) so the symbol meets WCAG AA against `var(--color-bg-elevated)` in both themes; if that swap is still insufficient (verified in devtools), escalate by also moving background to `var(--color-bg-secondary)` — still NO new tokens.
3. Verify `aria-label="Back to top"` (already correct in code per D-10 — conscious EN override of REQUIREMENTS.md:39) is preserved.

The existing `prefers-reduced-motion` per-click handler (`BackToTopButton.jsx:29`) and the opacity-based fade (`BackToTopButton.css:26-31`) already satisfy the motion clause of A11Y-V2.1-01 and stay untouched.

Purpose: Close the last of the 8 phase-8 requirements with a minimal, isolated change set in two files.
Output: One file changed in JSX (threshold integer), one file changed in CSS (color value swap, optionally background swap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-sticky-ia-polish-bundle/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

# Source files
@frontend/src/components/BackToTopButton.jsx
@frontend/src/components/BackToTopButton.css

<interfaces>
<!-- Extracted from current code. -->

BackToTopButton.jsx:
- Line 18: `const onScroll = () => setVisible(el.scrollTop > 800);`
- Line 29: per-click prefers-reduced-motion check via `window.matchMedia('(prefers-reduced-motion: reduce)').matches`.
- Line 38: `aria-label="Back to top"` (already English — D-10 preserved verbatim).

BackToTopButton.css:
- Line 18: `background: var(--color-bg-elevated);`
- Line 21: `color: var(--color-fg-secondary);` — primary target of the D-11 swap.
- Line 26-31: opacity-based fade with motion-token transitions; honors reduced-motion via global CSS conventions.
- Line 38-41: `:hover { color: var(--color-fg-primary); background: var(--color-bg-secondary); }` — the hover destination is already the D-11 target; the swap merely promotes it to the default state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lower visibility threshold from 800 to 600 in BackToTopButton.jsx</name>
  <files>frontend/src/components/BackToTopButton.jsx</files>
  <read_first>
    - frontend/src/components/BackToTopButton.jsx (full 44 LOC — confirm only line 18 needs editing; lines 29 + 38 are already correct per D-10/D-12 carry-overs and MUST NOT be touched)
    - .planning/phases/08-sticky-ia-polish-bundle/08-CONTEXT.md (D-10, D-12)
  </read_first>
  <action>
    Open `frontend/src/components/BackToTopButton.jsx`. On line 18, change:
    ```js
    const onScroll = () => setVisible(el.scrollTop > 800);
    ```
    to:
    ```js
    const onScroll = () => setVisible(el.scrollTop > 600);
    ```
    Also update the JSDoc on line 6 that currently reads `Becomes visible when `.messages-container.scrollTop > 800`` to `Becomes visible when `.messages-container.scrollTop > 600` (A11Y-V2.1-01 / D-12).`. The comment update is mandatory — keep documentation accurate so future grep-driven archaeology matches reality.

    Do NOT change any other line. Specifically:
    - Do NOT touch line 29 (`prefers-reduced-motion` per-click check stays).
    - Do NOT touch line 38 (`aria-label="Back to top"` stays — D-10 conscious EN override).
    - Do NOT touch the `tabIndex={visible ? 0 : -1}` line 39 (a11y baseline keeps the button out of tab order while hidden).
  </action>
  <verify>
    <automated>cd frontend && grep -c "scrollTop > 600" src/components/BackToTopButton.jsx</automated>
    Expected: 1.
    Additionally: `cd frontend && grep -c "scrollTop > 800" src/components/BackToTopButton.jsx` MUST return 0 (the old threshold is fully removed, including from the JSDoc).
    Additionally: `cd frontend && grep -c 'aria-label="Back to top"' src/components/BackToTopButton.jsx` MUST return 1 (D-10 preserved).
    Additionally: `cd frontend && grep -c "prefers-reduced-motion" src/components/BackToTopButton.jsx` MUST return at least 1 (carry-over preserved).
    Then run `cd frontend && npm run lint` — zero errors.
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/BackToTopButton.jsx` contains the substring `scrollTop > 600`.
    - `frontend/src/components/BackToTopButton.jsx` does NOT contain the substring `> 800` (including the JSDoc reference).
    - `frontend/src/components/BackToTopButton.jsx` contains `aria-label="Back to top"` unchanged.
    - `frontend/src/components/BackToTopButton.jsx` contains `prefers-reduced-motion` unchanged in the per-click handler.
    - `cd frontend && npm run lint` returns exit code 0.
  </acceptance_criteria>
  <done>Threshold is 600; aria-label and reduced-motion handling are preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Contrast bump via token swap in BackToTopButton.css</name>
  <files>frontend/src/components/BackToTopButton.css</files>
  <read_first>
    - frontend/src/components/BackToTopButton.css (full 47 LOC — confirm current `color: var(--color-fg-secondary)` at line 21 and the hover destination `color: var(--color-fg-primary); background: var(--color-bg-secondary)` at lines 39-40)
    - .planning/phases/08-sticky-ia-polish-bundle/08-CONTEXT.md (D-11 — first try color-only swap; escalate to background swap ONLY if devtools confirms still <AA)
  </read_first>
  <action>
    Open `frontend/src/components/BackToTopButton.css`. Two sub-steps:

    1. **Primary swap (always apply):** Line 21 currently reads `color: var(--color-fg-secondary);`. Change to:
       ```css
       color: var(--color-fg-primary);
       ```
       This is the D-11 first move: bump the symbol colour to the most opaque foreground token. No other property in `.back-to-top` (lines 11-31) changes.

    2. **Escalation (apply ONLY if step 1 still fails WCAG AA in light or dark theme — verify with devtools during Task 3 smoke):**
       - If the primary swap does NOT achieve at least 4.5:1 contrast against the background in either theme, additionally change line 18 from `background: var(--color-bg-elevated);` to `background: var(--color-bg-secondary);`. This is one step deeper into the token chain per D-11.
       - The hover rule (`.back-to-top:hover`, lines 38-41) currently sets `color: var(--color-fg-primary); background: var(--color-bg-secondary);` — after escalation, the resting state matches the hover state's destination; this collapses the hover differentiation. If that is undesired (manual smoke flags it), tighten the hover by removing the now-redundant `background: var(--color-bg-secondary);` from the hover rule and let only `color` change (it would still match — at that point, leave the hover rule alone; the lack of a visible hover state on the back-to-top button is an acceptable Direction A "calmo" outcome).
       - Document the escalation in the Task 3 smoke notes if you take it.

    Do NOT introduce any new design tokens. Do NOT touch the `transition`, `box-shadow`, `border-radius`, or `:focus-visible` rules. Do NOT change the size (48×48), z-index (4), or positioning.
  </action>
  <verify>
    <automated>cd frontend && grep -c "color: var(--color-fg-primary)" src/components/BackToTopButton.css</automated>
    Expected: at least 1 in the resting `.back-to-top { … }` rule (the previous count was 1 — only in `:hover`; after this task it MUST be at least 2 if hover rule still keeps its own, or at least 1 if hover rule was simplified per escalation note).
    Additionally: `cd frontend && grep -c "color: var(--color-fg-secondary)" src/components/BackToTopButton.css` MUST return 0 (the demoted token is no longer used as the resting colour).
    Additionally: `cd frontend && grep -E "^[[:space:]]*--[a-z-]+:" src/components/BackToTopButton.css | wc -l` MUST be 0 (no new CSS custom properties).
    Then run `cd frontend && npm run build` — must succeed.
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/BackToTopButton.css` resting `.back-to-top { … }` block uses `color: var(--color-fg-primary);` (not `--color-fg-secondary`).
    - `frontend/src/components/BackToTopButton.css` does NOT contain `color: var(--color-fg-secondary)` (the previous resting colour is replaced everywhere).
    - `frontend/src/components/BackToTopButton.css` background is either `var(--color-bg-elevated)` (no escalation needed) or `var(--color-bg-secondary)` (escalation applied) — both are existing tokens.
    - `frontend/src/components/BackToTopButton.css` does NOT introduce any new `--*` custom properties.
    - `frontend/src/components/BackToTopButton.css` size, position, z-index, transition, border-radius, focus-ring properties are unchanged.
    - `cd frontend && npm run build` exits with code 0.
  </acceptance_criteria>
  <done>Resting symbol colour is `--color-fg-primary`; if escalation was needed, background is `--color-bg-secondary`. No new tokens.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke checkpoint — threshold, contrast, aria-label, reduced motion</name>
  <what-built>
    Tasks 1-2 lowered the back-to-top button's visibility threshold from 800 to 600 and bumped its resting symbol colour to `--color-fg-primary` (optionally escalating background to `--color-bg-secondary` if WCAG AA was still failing). aria-label="Back to top" and the prefers-reduced-motion per-click handling were not touched.
  </what-built>
  <how-to-verify>
    1. `cd frontend && npm run dev` and open `http://localhost:5173`.
    2. Open a conversation with a long Stage 3 deliberation. If none exists, send a fresh prompt with a verbose question and wait for Stage 3.
    3. **Threshold (D-12)** — scroll `.messages-container` slowly:
       - At `scrollTop ≤ 600px`, the back-to-top button is INVISIBLE (opacity 0, `tabIndex={-1}`, not focusable via Tab).
       - At `scrollTop > 600px`, the button fades in.
       - Scrolling back up to `≤ 600px` fades it out.
       - Confirm in devtools by inspecting the scroll container and watching `.is-visible` toggle on `.back-to-top`.
    4. **WCAG AA contrast (D-11)** — open Chromium devtools → Elements → select the `.back-to-top` button → Accessibility panel → Contrast ratio.
       - In **light theme**: contrast of the `↑` glyph against the button's background MUST be ≥ 4.5:1.
       - In **dark theme**: same check MUST be ≥ 4.5:1.
       - If either theme reads below 4.5:1, return to Task 2 step 2 and apply the background escalation (`--color-bg-elevated` → `--color-bg-secondary`); re-run the check. Note the escalation in the SUMMARY.
    5. **aria-label (D-10)** — in the devtools Accessibility panel, the button's accessible name MUST read exactly `Back to top` (English; this is the conscious override of REQUIREMENTS.md:39 per D-10, NOT "Volver al inicio").
    6. **Reduced motion** — toggle Chromium devtools → Rendering → "Emulate CSS media feature prefers-reduced-motion" → `reduce`.
       - Scroll to 700px so the button is visible. Click it. The container MUST jump to top instantly (no smooth-scroll). This confirms the per-click `matchMedia` check in `BackToTopButton.jsx:29` still works.
       - Disable the emulation and click again. Scroll MUST be smooth.
       - Note: the fade-in/out transitions are CSS opacity transitions; under reduced motion these would only collapse if a global `@media (prefers-reduced-motion: reduce)` rule existed. Confirm the existing global pattern still suffices; if the fade still animates noticeably under reduced motion, log it for Phase 10 follow-up (out of Phase 8 scope to add a new global rule).
    7. **Focus order** — Tab through the page with the button INVISIBLE (`scrollTop ≤ 600`). The button MUST NOT receive focus (`tabIndex={-1}`). Then scroll past 600px so the button is visible and Tab again — the button SHOULD be reachable.
    8. **No regression in click behavior** — click the visible button; the container scrolls to top. Repeat in both themes.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass and document whether the background escalation was needed, or describe specific failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none added) | UI-only changes; no new boundaries crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of service | Scroll handler firing more often at lower threshold | accept | The handler runs on every `scroll` event with `passive: true`; lowering the threshold from 800 to 600 does not change firing rate, only the comparison point. Negligible. |

UI-only changes: no new attack surface, no new input handling, no new persistence. Threat level: low.
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds.
- `cd frontend && npm run lint` returns zero errors on the two modified files.
- `cd frontend && npm test` returns green (Phase 8 adds no new tests).
- Manual smoke checkpoint (Task 3) passes all 7 checks across light + dark themes; WCAG AA verified in devtools.
</verification>

<success_criteria>
- The button becomes visible at `scrollTop > 600` (not 800).
- aria-label remains `"Back to top"` (English; D-10 override).
- The `↑` glyph achieves ≥ 4.5:1 contrast against the button background in both themes (with at most a background-token escalation if needed).
- `prefers-reduced-motion: reduce` still collapses the click-scroll to instant.
- No new design tokens introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-sticky-ia-polish-bundle/08-03-SUMMARY.md` capturing: A11Y-V2.1-01 closed, whether the background escalation was needed, the measured contrast ratios in both themes.
</output>
