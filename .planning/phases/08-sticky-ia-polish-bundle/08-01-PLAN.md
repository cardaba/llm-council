---
phase: 08-sticky-ia-polish-bundle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.jsx
  - frontend/src/components/ChatInterface.jsx
  - frontend/src/components/StageNavigationStrip.jsx
  - frontend/src/components/StageNavigationStrip.css
  - frontend/src/components/Stage1.jsx
  - frontend/src/components/Stage2.jsx
  - frontend/src/components/Stage3.jsx
  - frontend/src/components/Stage4.jsx
autonomous: true
requirements:
  - NAV-V2.1-01
  - NAV-V2.1-02
  - NAV-V2.1-03
  - IA-V2.1-01
must_haves:
  truths:
    - "User scrolling a long Stage response sees no strip of underlying content above the sticky stage-nav-strip (NAV-V2.1-01 floor preserved)"
    - "User scrolling past the assistant message header reads the conversation title inside the sticky stage-nav-strip as a breadcrumb line above the chip row"
    - "User looking at an active chip sees a 2px accent bottom border AND the strip's soft drop shadow coexisting (tab-connected-to-panel pattern, D-08)"
    - "User does NOT see a redundant 'Stage X: ...' H3 below the active chip in Stage 1/2/3/4"
    - "Screen readers announce each stage section via aria-labelledby pointing at the chip in the strip (no parallel sr-only headings)"
    - "Sticky bar does not jump in vertical size on stick/unstick — single sticky container with two lines"
  artifacts:
    - path: "frontend/src/components/StageNavigationStrip.jsx"
      provides: "Sticky chip strip extended with optional breadcrumb line + stable chip IDs"
      contains: "stage-nav-strip__breadcrumb"
    - path: "frontend/src/components/StageNavigationStrip.css"
      provides: "Two-line layout styles, breadcrumb microcopy class"
      contains: ".stage-nav-strip__breadcrumb"
    - path: "frontend/src/components/ChatInterface.jsx"
      provides: "Breadcrumb prop wiring + aria-labelledby on stage sections"
      contains: "conversationTitle"
    - path: "frontend/src/components/Stage1.jsx"
      provides: "Stage 1 panel without redundant h3.stage-title"
    - path: "frontend/src/components/Stage2.jsx"
      provides: "Stage 2 panel without redundant h3.stage-title"
    - path: "frontend/src/components/Stage3.jsx"
      provides: "Stage 3 panel without redundant h3.stage-title"
    - path: "frontend/src/components/Stage4.jsx"
      provides: "Stage 4 panel without redundant h3.stage-title"
  key_links:
    - from: "App.jsx"
      to: "ChatInterface (conversation prop already passed)"
      via: "currentConversation.title flows naturally via existing prop"
      pattern: "conversation={currentConversation}"
    - from: "ChatInterface.jsx"
      to: "StageNavigationStrip (new conversationTitle prop)"
      via: "JSX prop on the existing <StageNavigationStrip /> render site"
      pattern: "conversationTitle=\\{conversation\\.title"
    - from: "ChatInterface.jsx <section data-stage=...>"
      to: "StageNavigationStrip chip ids"
      via: "aria-labelledby='stage-nav-chip-stageN' resolving to the chip's id attribute"
      pattern: "aria-labelledby=\"stage-nav-chip-"
---

<objective>
Extend the existing sticky `.stage-nav-strip` into a two-line container: line 1 = conversation-title breadcrumb (microcopy, muted, single-line truncated), line 2 = existing chip row unchanged in behavior. Wire `currentConversation.title` from App.jsx → ChatInterface → StageNavigationStrip via a new `conversationTitle` prop. Drop the four redundant `<h3 className="stage-title">` headings from Stage1/2/3/4.jsx and replace their semantic role with `aria-labelledby` on each `<section data-stage="...">` pointing at a stable `id` on the matching chip button. Verify NAV-V2.1-01 layering still clean post-change (no re-implementation of 260511-l5w) and confirm NAV-V2.1-02 active chip border-bottom coexists with the strip box-shadow (D-08).

Purpose: Close 4 of the 8 phase-8 requirements (NAV-V2.1-01 verify, NAV-V2.1-02 verify, NAV-V2.1-03, IA-V2.1-01) on the sticky-strip surface in one coordinated pass, since all four interact with the same DOM and CSS.
Output: One enriched sticky bar (breadcrumb + chips, single sticky element, single shadow, single border), zero redundant H3s, screen-reader heading structure preserved via aria-labelledby.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-sticky-ia-polish-bundle/08-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md

# Source-of-truth files this plan modifies (read each before touching)
@frontend/src/components/StageNavigationStrip.jsx
@frontend/src/components/StageNavigationStrip.css
@frontend/src/components/ChatInterface.jsx
@frontend/src/components/Stage1.jsx
@frontend/src/components/Stage2.jsx
@frontend/src/components/Stage3.jsx
@frontend/src/components/Stage4.jsx
@frontend/src/App.jsx

<interfaces>
<!-- Extracted from existing code; executor should use these directly. -->

From StageNavigationStrip.jsx (current signature):
```
export default function StageNavigationStrip({ assistantMsg, scrollContainerRef })
```
Chips are produced by `buildChips(msg)` returning `[{ id: 'stage1'|'stage2'|'stage3'|'stage4', label: string }]`.
Chip rendering loop at lines 101-114 produces `<button className="stage-nav-strip__chip ...">`. The button currently has NO `id` attribute.

From ChatInterface.jsx (current StageNavigationStrip usage, ~line 252):
```
<StageNavigationStrip
  assistantMsg={msg}
  scrollContainerRef={messagesContainerRef}
/>
```
The four stage sections are rendered as `<section data-stage="stage1">…<Stage1 ... /></section>` (lines 264-311), and `<section data-stage="stage2">`, `<section data-stage="stage3">`. Stage4 sits inside Stage3's component tree (`Stage4.jsx:29` carries `data-stage="stage4"` on its OWN `<div>`).

From App.jsx (line 568-574): currentConversation is already passed via the `conversation` prop.

From Stage1.jsx:80, Stage2.jsx:15, Stage3.jsx:27, Stage4.jsx:30 — each contains exactly one `<h3 className="stage-title">…</h3>`. These are the four sites to remove.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `conversationTitle` prop + breadcrumb line + chip IDs to StageNavigationStrip</name>
  <files>frontend/src/components/StageNavigationStrip.jsx, frontend/src/components/StageNavigationStrip.css</files>
  <read_first>
    - .planning/phases/08-sticky-ia-polish-bundle/08-CONTEXT.md (D-01, D-02, D-03, D-08, D-09)
    - frontend/src/components/StageNavigationStrip.jsx (full file — see current chip-render loop and component signature)
    - frontend/src/components/StageNavigationStrip.css (full file — confirm box-shadow at line 24 STAYS, chip border-bottom at line 32 STAYS)
  </read_first>
  <action>
    In `StageNavigationStrip.jsx`:
    - Update the component signature to accept a third prop: `export default function StageNavigationStrip({ assistantMsg, scrollContainerRef, conversationTitle })`.
    - Inside the existing `<div className="stage-nav-strip" role="navigation" aria-label="Stage navigation">` container (line 100), insert a new child element as the FIRST child (before the chip mapping). The element MUST be:
      `{conversationTitle ? (<div className="stage-nav-strip__breadcrumb" title={conversationTitle}>{conversationTitle}</div>) : null}`
      Rationale per D-02: single sticky element, two-line layout. Per D-03: if `conversationTitle` is falsy, do not render the line (do NOT inject literal "New Conversation" here — the fallback string is the caller's responsibility per D-03; render nothing rather than empty whitespace).
    - On each chip `<button>` (in the `chips.map` block), add an `id` attribute: `id={`stage-nav-chip-${chip.id}`}`. The chip.id values are already `stage1` / `stage2` / `stage3` / `stage4` (see `buildChips`). Final chip id strings: `stage-nav-chip-stage1`, `stage-nav-chip-stage2`, `stage-nav-chip-stage3`, `stage-nav-chip-stage4`.
    - Keep ALL existing behavior: IntersectionObserver, prefers-reduced-motion handling, `aria-current`, `is-active` class, the early `if (chips.length === 0) return null;` guard.
    - Do NOT touch the existing box-shadow on `.stage-nav-strip` or the chip's `border-bottom: 2px solid transparent` / `is-active` border-bottom-color (D-08 — both stay).

    In `StageNavigationStrip.css`:
    - Adjust `.stage-nav-strip` to lay out children as a vertical stack at the breadcrumb level, while keeping chips horizontal: change `display: flex;` (line 16) to `display: flex; flex-direction: column;` and move the existing `gap: var(--space-2);` (line 17) to apply between children. Then wrap the chips into a horizontal flex by either (a) introducing an inner `.stage-nav-strip__chips` wrapper in the JSX OR (b) keeping the chips as direct children and using `gap` on a row child. Pick (a) — it is the cleaner option for D-02 (two-line layout) and avoids fighting flex-direction.
      - JSX update implied: wrap the `chips.map(...)` output in `<div className="stage-nav-strip__chips">…</div>`.
      - CSS rule: `.stage-nav-strip__chips { display: flex; gap: var(--space-2); overflow-x: auto; -webkit-overflow-scrolling: touch; }`.
      - Remove the `overflow-x: auto;` and `-webkit-overflow-scrolling: touch;` lines (21-22) from `.stage-nav-strip` itself (they now belong on the chips row). Keep `padding`, `background`, `border-bottom`, `box-shadow`, `position: sticky`, `top: 0`, `z-index: 3` on `.stage-nav-strip`.
    - Add a new CSS rule for the breadcrumb line:
      ```
      .stage-nav-strip__breadcrumb {
        font-family: var(--font-sans);
        font-size: var(--font-size-microcopy);
        color: var(--color-fg-muted);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        min-width: 0;
      }
      ```
      Place it directly under the `.stage-nav-strip` block.
    - Do NOT introduce new design tokens. Use only the existing `--font-sans`, `--font-size-microcopy`, `--color-fg-muted`, and existing space tokens.

    The combined effect: the sticky bar is now a single column-flex container; row 1 is the breadcrumb (when present), row 2 is the chips. The existing box-shadow + chip active-border coexist unchanged (D-08).
  </action>
  <verify>
    <automated>cd frontend && grep -n "stage-nav-strip__breadcrumb" src/components/StageNavigationStrip.jsx src/components/StageNavigationStrip.css | grep -v "^#" | grep -c "stage-nav-strip__breadcrumb"</automated>
    Expected: at least 3 matches (JSX usage + className + CSS rule).
    Additionally: `cd frontend && grep -c "id={`stage-nav-chip-" src/components/StageNavigationStrip.jsx` MUST return 1.
    Additionally: `cd frontend && grep -c "box-shadow: 0 4px 8px -4px" src/components/StageNavigationStrip.css` MUST return 1 (the 260511-l5w shadow is preserved).
    Additionally: `cd frontend && grep -c "border-bottom-color: var(--color-accent)" src/components/StageNavigationStrip.css` MUST return 1 (NAV-V2.1-02 active chip border preserved, D-08).
    Then run `cd frontend && npm run lint` — must return zero errors on the two modified files.
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/StageNavigationStrip.jsx` contains the substring `conversationTitle` in the component signature parameter list.
    - `frontend/src/components/StageNavigationStrip.jsx` contains the substring `className="stage-nav-strip__breadcrumb"`.
    - `frontend/src/components/StageNavigationStrip.jsx` contains the substring `id={`stage-nav-chip-${chip.id}`}` (or equivalent template literal that produces `stage-nav-chip-stage1` etc.) — verifiable by `grep "stage-nav-chip-" src/components/StageNavigationStrip.jsx`.
    - `frontend/src/components/StageNavigationStrip.jsx` contains the substring `className="stage-nav-strip__chips"`.
    - `frontend/src/components/StageNavigationStrip.css` contains a rule `.stage-nav-strip__breadcrumb { … text-overflow: ellipsis … }`.
    - `frontend/src/components/StageNavigationStrip.css` STILL contains `box-shadow: 0 4px 8px -4px rgba(0, 0, 0, 0.08)` (verifiable by grep).
    - `frontend/src/components/StageNavigationStrip.css` STILL contains `border-bottom: 2px solid transparent` on the chip and `border-bottom-color: var(--color-accent)` on `.is-active`.
    - `frontend/src/components/StageNavigationStrip.css` does NOT contain any newly added `--color-*` or `--space-*` variable definitions (no new tokens).
    - `cd frontend && npm run lint` returns exit code 0.
  </acceptance_criteria>
  <done>Sticky strip renders a breadcrumb line above the chip row when `conversationTitle` is non-empty; chip buttons carry stable `id="stage-nav-chip-stageN"`; the 260511-l5w shadow and the active-chip accent border both remain present in CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `conversationTitle` from App.jsx and add aria-labelledby on stage sections in ChatInterface.jsx</name>
  <files>frontend/src/components/ChatInterface.jsx</files>
  <read_first>
    - frontend/src/components/ChatInterface.jsx (full file — confirm StageNavigationStrip usage site ~line 252 and the four `<section data-stage="...">` sites at lines 265, 278, 303 + the Stage4 `<div data-stage="stage4">` inside Stage3's subtree)
    - .planning/phases/08-sticky-ia-polish-bundle/08-CONTEXT.md (D-01, D-03, D-09)
  </read_first>
  <action>
    In `frontend/src/components/ChatInterface.jsx`:
    - At the `<StageNavigationStrip>` render site (~line 252), add a third prop: `conversationTitle={conversation?.title || 'New Conversation'}`. This realises D-03: pull `title` from the `conversation` prop App.jsx already passes; fall back to the literal string `'New Conversation'` (matches `Sidebar.jsx:115`).
    - On `<section data-stage="stage1">` (~line 265), add `aria-labelledby="stage-nav-chip-stage1"`.
    - On `<section data-stage="stage2">` (~line 278), add `aria-labelledby="stage-nav-chip-stage2"`.
    - On `<section data-stage="stage3">` (~line 303), add `aria-labelledby="stage-nav-chip-stage3"`.
    - Do NOT touch the Stage4 `<div data-stage="stage4">` inside `Stage4.jsx` from this file — that one belongs to Task 3.
    - Do NOT modify any other prop or any other render branch.

    No changes are needed in App.jsx: the `conversation` prop already carries `.title` (line 569). Verify this is true; if for any reason the title is absent on the prop shape, fall back to `'New Conversation'` (already specified in the JSX above) — no extra plumbing needed.
  </action>
  <verify>
    <automated>cd frontend && grep -c "conversationTitle=" src/components/ChatInterface.jsx</automated>
    Expected: 1.
    Additionally: `cd frontend && grep -c "aria-labelledby=\"stage-nav-chip-" src/components/ChatInterface.jsx` MUST return 3.
    Then run `cd frontend && npm run lint` — zero errors on the modified file.
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ChatInterface.jsx` contains exactly one occurrence of `conversationTitle=` (passed to `<StageNavigationStrip>`).
    - `frontend/src/components/ChatInterface.jsx` contains exactly three occurrences of `aria-labelledby="stage-nav-chip-stage` (one each for stage1, stage2, stage3).
    - The fallback string `'New Conversation'` appears in the `conversationTitle` prop expression (verifiable by `grep "conversationTitle.*New Conversation" src/components/ChatInterface.jsx`).
    - `cd frontend && npm run lint` returns exit code 0.
  </acceptance_criteria>
  <done>ChatInterface wires the conversation title into the sticky strip and labels each stage section via aria-labelledby pointing at its corresponding chip id.</done>
</task>

<task type="auto">
  <name>Task 3: Remove redundant h3.stage-title from Stage1/2/3/4 and add aria-labelledby on Stage4 wrapper</name>
  <files>frontend/src/components/Stage1.jsx, frontend/src/components/Stage2.jsx, frontend/src/components/Stage3.jsx, frontend/src/components/Stage4.jsx</files>
  <read_first>
    - frontend/src/components/Stage1.jsx (line 80 — the h3 to remove)
    - frontend/src/components/Stage2.jsx (line 15 — the h3 to remove)
    - frontend/src/components/Stage3.jsx (line 27 — the h3 to remove; note this h3 is inside a `<div className="stage3-header">` flex container alongside a Download button — only remove the h3, leave the wrapping div + button intact)
    - frontend/src/components/Stage4.jsx (line 30 — the h3 to remove; line 29 is the `<div ... data-stage="stage4">` that needs aria-labelledby)
    - .planning/phases/08-sticky-ia-polish-bundle/08-CONTEXT.md (D-09)
  </read_first>
  <action>
    Make four edits, one per stage component:

    1. `frontend/src/components/Stage1.jsx:80` — delete the entire line `      <h3 className="stage-title">Stage 1: Individual Responses</h3>`.
    2. `frontend/src/components/Stage2.jsx:15` — delete the entire line `      <h3 className="stage-title">Stage 2: Peer Rankings</h3>`.
    3. `frontend/src/components/Stage3.jsx:27` — delete the entire line `        <h3 className="stage-title">Stage 3: Final Council Answer</h3>`. Leave the `<div className="stage3-header">` wrapper and the `<button className="download-btn" ...>` sibling unchanged. The wrapping `.stage3-header` becomes a single-child flex container — that is fine; do NOT remove the wrapper div in this plan (its CSS layout class may be reused elsewhere).
    4. `frontend/src/components/Stage4.jsx`:
       - On the existing wrapping `<div className="stage stage4" data-stage="stage4">` (line 29), add the attribute `aria-labelledby="stage-nav-chip-stage4"`. This is the labelling source for Stage 4 (the only stage panel labelled from within its own component tree, because Stage4 lives inside Stage3's DOM subtree and is not wrapped by a `<section>` in ChatInterface.jsx).
       - Delete the h3 line: `      <h3 className="stage-title">Stage 4: Refinement</h3>` (line 30).

    Do NOT touch any other line in any of the four files. Do NOT remove the `.stage-title` CSS rule (it may be defined in a shared CSS and removing it is out of scope; orphaned CSS is acceptable for this phase — Phase 10 VRT regen will surface any cosmetic effects).
  </action>
  <verify>
    <automated>cd frontend && grep -c "stage-title" src/components/Stage1.jsx src/components/Stage2.jsx src/components/Stage3.jsx src/components/Stage4.jsx</automated>
    Expected: each file returns 0 (no occurrences of the class string `stage-title` in any of the four files).
    <automated>cd frontend && grep -c "aria-labelledby=\"stage-nav-chip-stage4\"" src/components/Stage4.jsx</automated>
    Expected: 1.
    <automated>cd frontend && npm run build</automated>
    Expected: exit code 0 (compile error on a typo in JSX would surface here).
    <automated>cd frontend && npm run lint</automated>
    Expected: zero errors on the four modified files.
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/Stage1.jsx` does NOT contain the substring `stage-title` (verifiable by `grep -c "stage-title" src/components/Stage1.jsx` returning 0).
    - `frontend/src/components/Stage2.jsx` does NOT contain the substring `stage-title`.
    - `frontend/src/components/Stage3.jsx` does NOT contain the substring `stage-title`.
    - `frontend/src/components/Stage4.jsx` does NOT contain the substring `stage-title`.
    - `frontend/src/components/Stage4.jsx` contains exactly one occurrence of `aria-labelledby="stage-nav-chip-stage4"`.
    - `frontend/src/components/Stage3.jsx` still contains `<div className="stage3-header">` and `<button` with `className="download-btn"` (the download button must NOT be removed — only the H3 sibling).
    - `cd frontend && npm run build` exits with code 0.
  </acceptance_criteria>
  <done>The four redundant H3 stage titles are removed; Stage4's outer div carries aria-labelledby pointing at its chip; the build still compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual smoke checkpoint — sticky bar layering, breadcrumb, chip border, no redundant H3</name>
  <what-built>
    Tasks 1-3 produced: a two-line sticky strip (breadcrumb + chips) inside `.messages-container`, with the existing soft drop-shadow preserved and the active chip's 2px accent bottom border preserved (D-08). Conversation title now plumbed via prop. Four redundant H3s removed. Stage sections labelled via aria-labelledby pointing at chip ids.
  </what-built>
  <how-to-verify>
    1. `cd frontend && npm run dev` and open `http://localhost:5173`.
    2. Open an existing conversation that has Stage 3 (any v2.0 deliberation works). If no long deliberation exists, send a fresh prompt and wait for Stage 3 to complete.
    3. **Layering smoke (NAV-V2.1-01 close/verify)** — scroll the `.messages-container` slowly down through a long Stage 3 response. Confirm:
       - No strip of underlying content peeks above the sticky bar at any scroll position.
       - The sticky bar's drop shadow remains visible (it is the layering cue).
       - Repeat in light AND dark theme (toggle via the existing Header theme switch or Settings panel).
    4. **Breadcrumb (IA-V2.1-01)** — confirm:
       - At the top of the assistant message (sticky strip not yet "stuck"), the breadcrumb shows the conversation title in muted microcopy.
       - As you scroll past the message header and the strip becomes "stuck" to `top: 0` of the scroll container, the breadcrumb stays visible above the chip row. The strip does NOT jump in height on stick/unstick.
       - Hovering over the breadcrumb shows the full title as a native tooltip when the title is truncated (the `title={conversationTitle}` attribute on the breadcrumb).
       - If the conversation has no title, the breadcrumb shows `New Conversation` (fallback).
    5. **NAV-V2.1-02 chip border + shadow coexistence (D-08)** — confirm:
       - Click each chip in turn (Stage 1 / Stage 2 / Stage 3). The active chip carries a visible 2px accent-coloured bottom border. The strip's drop shadow remains visible at the same time.
    6. **NAV-V2.1-03 H3 removal** — confirm visually:
       - There is no "Stage 1: Individual Responses" / "Stage 2: Peer Rankings" / "Stage 3: Final Council Answer" / "Stage 4: Refinement" H3 line below the active chip. The chip itself now carries the stage identity.
    7. **A11y check** — using browser devtools accessibility inspector (Chromium → Elements → Accessibility):
       - Click into the Stage 1 section. The accessible name should resolve to the Stage 1 chip's text (e.g., "Stage 1 · 4 responses") via `aria-labelledby`.
       - Repeat for Stage 2, 3, 4. None should report "no accessible name".
    8. **Mobile** — toggle devtools to a 375×667 viewport. Confirm the sticky bar still renders as two lines (breadcrumb + chips), the chip row is still horizontally scrollable, and the layering is still clean.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass, or describe specific failures (which check, which theme, which viewport).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none added) | UI-only changes; no new boundaries crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information disclosure | StageNavigationStrip breadcrumb | accept | The breadcrumb only renders `conversation.title`, which is already user-supplied and visible in the Sidebar. No new data egress. |
| T-08-02 | Tampering | aria-labelledby chip ids | accept | IDs are static derivations of `chip.id` (`stage1`-`stage4`); no user input flows into the id string. No DOM-injection vector. |

UI-only changes: no new attack surface, no new input handling, no new persistence. Threat level: low. Mitigations: existing CORS + 127.0.0.1 bind.
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds.
- `cd frontend && npm run lint` returns zero errors on touched files.
- `cd frontend && npm test` returns green (no existing tests should regress — Phase 8 adds no new tests).
- Manual smoke checkpoint (Task 4) passes all 8 checks across light + dark themes and at least desktop + mobile-portrait viewports.
- VRT baselines are NOT regenerated in this plan (Phase 10 owns regen).
</verification>

<success_criteria>
- Sticky bar is a single two-line element: breadcrumb (microcopy, muted, truncated) on top, chip row on bottom.
- Breadcrumb plumbing: `conversation.title` (or `'New Conversation'` fallback) flows from App.jsx → ChatInterface → StageNavigationStrip without backend changes.
- Active chip carries 2px accent bottom border AND the strip retains its 260511-l5w drop shadow (D-08).
- No `h3.stage-title` strings remain in Stage1/2/3/4.jsx.
- Each stage section carries `aria-labelledby` pointing at its corresponding chip's `id` attribute.
- No new design tokens introduced.
- No re-implementation of the 260511-l5w padding-top drop (verify: `grep "padding-top" frontend/src/components/ChatInterface.css` returns no addition compared to current main).
</success_criteria>

<output>
After completion, create `.planning/phases/08-sticky-ia-polish-bundle/08-01-SUMMARY.md` capturing: files touched, the four req statuses (NAV-V2.1-01 closed/verified, NAV-V2.1-02 verified, NAV-V2.1-03 closed, IA-V2.1-01 closed), and any deviation from the breadcrumb/chip CSS layout strategy noted above.
</output>
