---
phase: 02-ux-research-design-brief
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - .planning/ux/04-mockups/wireframes.md
autonomous: true
requirements:
  - UXR-04
must_haves:
  truths:
    - "Existen wireframes ASCII/markdown bajo .planning/ux/04-mockups/ que cubren todas las pantallas y estados de la lista D-13."
    - "Los wireframes son neutros respecto a las 3 direcciones tonales — describen estructura/estado, no estética concreta."
    - "Cada wireframe está etiquetado con su nombre exacto (cold start, sidebar hover, three-dot menu open, search empty, search ≥3 chars no matches con affordance, inline rename activo, modal de delete abierto, input vacío, input con texto, input con attachments, Quality toggle Fast/Quality/Quality+Research, Stage 1 con tabs, Stage 1 con reasoning disclosure colapsado, Stage 1 con reasoning disclosure expandido, Stage 2, Stage 3, branded header, empty state, error state, loading state)."
  artifacts:
    - path: ".planning/ux/04-mockups/wireframes.md"
      provides: "Cobertura estructural completa en ASCII/markdown wireframes."
      contains:
        - "# Wireframes — LLM Council UI"
        - "## W01: Cold start"
        - "## W02: Sidebar — list"
        - "## W03: Sidebar — hover (three-dot visible)"
        - "## W04: Sidebar — three-dot menu open"
        - "## W05: Sidebar — search empty"
        - "## W06: Sidebar — search with title matches"
        - "## W07: Sidebar — search ≥3 chars no matches (content fallback affordance)"
        - "## W08: Sidebar — inline rename active"
        - "## W09: Sidebar — modal de delete open"
        - "## W10: Input — empty"
        - "## W11: Input — with text"
        - "## W12: Input — with attachments"
        - "## W13: Quality toggle — Fast"
        - "## W14: Quality toggle — Quality"
        - "## W15: Quality toggle — Quality+Research"
        - "## W16: Stage 1 — tabs + reasoning disclosure collapsed"
        - "## W17: Stage 1 — reasoning disclosure expanded"
        - "## W18: Stage 2 — de-anonymization + aggregate rankings"
        - "## W19: Stage 3 — synthesis + download"
        - "## W20: Branded header"
        - "## W21: Empty state — no conversations"
        - "## W22: Error state"
        - "## W23: Loading state — between stages"
  key_links:
    - from: ".planning/ux/04-mockups/wireframes.md"
      to: ".planning/ux/03-redesign-proposal.md"
      via: "Las anotaciones de cada wireframe referencian decisiones del redesign por dirección."
      pattern: "Direction [ABC]"
---

<objective>
Producir el set completo de wireframes ASCII/markdown bajo `.planning/ux/04-mockups/wireframes.md` que cubre la lista exacta de pantallas y estados de D-13. Los wireframes son **neutros respecto a las 3 direcciones tonales** — describen estructura, estados y comportamientos; la estética concreta vive en los HTML sketches de Plan 05. Esta neutralidad es deliberada: un único set de wireframes sirve como contrato estructural común para las 3 direcciones, evitando triplicar trabajo y facilitando comparación lado a lado.

Purpose: Es el contrato estructural que Phase 4 implementa. Mientras los HTML del Plan 05 deciden la estética, los wireframes deciden qué elementos hay y cómo se relacionan.

Output: `.planning/ux/04-mockups/wireframes.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ux-research-design-brief/02-CONTEXT.md
@.planning/phases/01-hardening-conversation-management/01-CONTEXT.md
@.planning/ux/01-cognitive-walkthrough.md
@.planning/ux/02-nielsen-audit.md
@.planning/ux/03-redesign-proposal.md
@frontend/src/components/Sidebar.jsx
@frontend/src/components/ChatInterface.jsx
@frontend/src/components/Stage1.jsx
@frontend/src/components/Stage2.jsx
@frontend/src/components/Stage3.jsx
@frontend/src/components/Modal.jsx
@frontend/src/components/Menu.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wireframes ASCII/markdown — cobertura completa de los 23 estados D-13</name>
  <files>.planning/ux/04-mockups/wireframes.md</files>
  <read_first>
    - .planning/phases/02-ux-research-design-brief/02-CONTEXT.md (D-12 ASCII/markdown wireframes para cobertura estructural; D-13 lista de estados; D-19 componentes Phase 1 a re-estilizar; D-20 single-shot input preservado).
    - .planning/REQUIREMENTS.md (UXR-04 mockups; QUAL-03 toggle 3-state Fast/Quality/Quality+Research visible junto al textarea; RSCH-05 reasoning_details disclosure colapsado dentro de cada Stage 1 tab).
    - .planning/phases/01-hardening-conversation-management/01-CONTEXT.md §Specifics (Modal mockup precedente aprobado en Phase 1; ChatGPT-pattern para three-dot; texto exacto del affordance "Search inside content (N conversations)").
    - .planning/ux/03-redesign-proposal.md (información architecture por dirección — los wireframes adoptan la IA común y anotan dónde hay variaciones por dirección).
    - frontend/src/components/Sidebar.jsx (estados reales del sidebar: search debounce, three-dot hover, intentRef rename pattern, modal de delete trigger, content-fallback affordance).
    - frontend/src/components/ChatInterface.jsx (input form, single-shot, attachments con chip list 500KB/file 2MB total, slot vacío para Quality toggle).
    - frontend/src/components/Stage1.jsx, Stage2.jsx, Stage3.jsx (tabs, de-anonimización bold, aggregate rankings, synthesis+download).
    - frontend/src/components/Modal.jsx, Menu.jsx (focus trap, hover-revealed three-dot, ESC/click-outside).
  </read_first>
  <action>
    Invocar la skill global `frontend-design` (o `impeccable` si está disponible) para producir un único documento `.planning/ux/04-mockups/wireframes.md` con:

    1. **Encabezado:**
       - Título: `# Wireframes — LLM Council UI`.
       - Sección `## Conventions` que explica: (a) los wireframes son ASCII boxes con anchos fijos a 80–100 cols; (b) el `▮` o `█` representa elementos sólidos / cursor / botón primario; (c) `[Label]` representa botones; (d) `< >` representa textos editables; (e) anotaciones laterales con `(*)` y notas a pie con `> note:`; (f) los wireframes son **neutros respecto a dirección tonal** — para estética concreta ir a los sketches HTML.
       - Sección `## Direction-specific notes` con 3 bullets cortos: "Para esta wireframe, Direction A/B/C aplica X" — patrón global, no repetir en cada wireframe.

    2. **23 wireframes con headings exactos** (críticos para grep):
       Cada uno con: heading `## WXX: <nombre>`, descripción de 1–2 líneas, ASCII box, anotaciones numeradas `(1)..(N)` referenciadas en notas debajo.

       - `## W01: Cold start` — primer arranque, sidebar vacío, main panel = welcome state con CTA "New Conversation". Header branded visible aunque vacío.
       - `## W02: Sidebar — list` — sidebar con 5–8 conversaciones con títulos truncados, una activa highlighted, scroll si supera viewport.
       - `## W03: Sidebar — hover (three-dot visible)` — mismo W02 pero con un item en hover state mostrando el `⋮` a la derecha.
       - `## W04: Sidebar — three-dot menu open` — menu popover abierto con items `Rename` y `Delete`, posicionado vía `position: fixed` con clamp al viewport.
       - `## W05: Sidebar — search empty` — input "Search conversations…" en top, lista completa visible debajo.
       - `## W06: Sidebar — search with title matches` — input con texto (ej. `"flux"`), lista filtrada con highlight en match (representado como `*flux*` o subrayado ASCII).
       - `## W07: Sidebar — search ≥3 chars no matches (content fallback affordance)` — input con `"async"` (≥3 chars), lista vacía, debajo el affordance literal `Search inside content (N conversations)` clicable. La N es el número de conversations totales.
       - `## W08: Sidebar — inline rename active` — un item con su título convertido en input editable, con cursor visible. Comentar Enter=commit, Esc=cancel, blur=commit.
       - `## W09: Sidebar — modal de delete open` — backdrop oscuro + modal centrado con el título de la conversación entre comillas, warning "This cannot be undone.", botones `[Cancel]` y `[Delete]` (destructive). El Modal.jsx Phase 1 mockup aprobado vive aquí.
       - `## W10: Input — empty` — main panel con input area visible (single-shot, antes del primer envío), placeholder, attachments slot vacío, slot del Quality toggle visible al lado del textarea.
       - `## W11: Input — with text` — input con texto multilínea de ejemplo, contador de caracteres si aplica.
       - `## W12: Input — with attachments` — chip list con 2 attachments (`paper.pdf · 320KB`, `notes.md · 12KB`), botón `[x]` por chip, contador `2 / 2MB`.
       - `## W13: Quality toggle — Fast` — toggle 3-state, "Fast" seleccionado con coste estimado pequeño visible (ej. `~$0.001`).
       - `## W14: Quality toggle — Quality` — "Quality" seleccionado con coste medio (ej. `~$0.05`).
       - `## W15: Quality toggle — Quality+Research` — "Quality+Research" seleccionado con coste alto destacado (ej. `~$0.45`) y nota "may take 5-10 min" o equivalente — el coste DEBE ser visible y prominente en este estado per PROJECT.md/Constraints.
       - `## W16: Stage 1 — tabs + reasoning disclosure collapsed` — header de stage, tabs por modelo (4 modelos típicos), contenido renderizado markdown del modelo activo, debajo del contenido un disclosure con label `▶ Show reasoning` colapsado.
       - `## W17: Stage 1 — reasoning disclosure expanded` — mismo W16 pero con el disclosure expandido `▼ Hide reasoning` mostrando 5–10 líneas de reasoning_details como texto monoespaciado / blockquote.
       - `## W18: Stage 2 — de-anonymization + aggregate rankings` — header de stage, tabs por evaluador, contenido de evaluación con etiquetas `Response A / B / C` y al lado la de-anonimización en bold (ej. `**openai/gpt-5.1**`), debajo o al lado un panel de aggregate rankings con avg position + vote count.
       - `## W19: Stage 3 — synthesis + download` — header de stage, contenido de la synthesis del chairman renderizado markdown, botón `[⇩ Download .md]` arriba o abajo, sin background verdoso `#f0fff0` (ese es Bootstrap-flavored y se elimina en Phase 4).
       - `## W20: Branded header` — header del shell con: nombre `LLM Council` + icono simple a la izquierda, indicador de conversation activa al centro, slot futuro para settings/profile a la derecha. Tono "personal tool", no logo corporativo.
       - `## W21: Empty state — no conversations` — sub-estado del W01, foco en sidebar empty + main panel CTA. Copia explícita de bienvenida.
       - `## W22: Error state` — qué ve el usuario cuando un stage falla (SSE error event con `kind: 'not_found'` u otro). Mensaje de error contextualizado, opción `[Retry]`.
       - `## W23: Loading state — between stages` — qué se muestra durante los 15-30s entre Stage 1 → Stage 2 → Stage 3 (per CONCERNS.md SSE per-stage). NO un spinner genérico; alguna forma de progress communication (timeline visual, "Stage 2: Models evaluating each other…" con tiempo transcurrido, etc.).

    3. **Por cada wireframe** incluir además:
       - **Anotaciones numeradas** apuntando a elementos clave del ASCII box.
       - **Direction notes** sub-bullet explicando si las direcciones A/B/C aplican variaciones a este wireframe (ej. en W15 Quality toggle, "Direction A: coste como nota al pie del label; Direction B: chip cromado; Direction C: subtitle sutil"). Esta anotación es CRÍTICA en W13/W14/W15 (toggle), W04 (menu), W08 (inline rename), W09 (modal), W18 (Stage 2 densidad), W23 (loading).

    4. **Sección final `## Coverage matrix`:**
       Tabla con columnas `Wireframe ID | D-13 item | Status` enumerando los 23 estados de D-13 y mostrando `✓` cuando están cubiertos. Esto sirve como check sheet para Phase 4 cuando implemente.

    Restricciones obligatorias:
    - **23 wireframes con la nomenclatura exacta `## WXX: <nombre>`** — verificable con grep.
    - **Cobertura completa de D-13** — todos los estados listados, ninguno omitido. La Coverage matrix lo evidencia.
    - **Wireframes neutros tonalmente** — no proponer paleta hex ni tipografía concreta dentro de cada wireframe; eso vive en Plan 05 (sketches HTML).
    - **W15 (Quality+Research) hace el coste prominente** — `~$0.XX` debe aparecer en el wireframe (no solo en la anotación). Esto es obligatorio per PROJECT.md/Constraints.
    - **W16 y W17 demuestran el disclosure de reasoning_details** colapsado y expandido — ambos estados, no solo uno. Resuelve RSCH-05 anticipatory finding.
    - **W09 sigue el modal mockup aprobado en Phase 1** (01-CONTEXT.md §Specifics).
    - **W07 contiene el copy literal** `Search inside content (N conversations)` (Phase 1 D-10).
    - **W23 NO usa spinner genérico** — describe alguna forma de progress communication específica (anotación obligatoria).
    - **Wireframes en ASCII/markdown puros** — sin SVG, sin imágenes binarias, sin HTML embebido. El doc se debe poder leer en un editor de texto.
    - **Idioma:** español para la prosa y anotaciones, headings en inglés (críticos para grep), copys en pantalla en inglés (los del UI real).
  </action>
  <verify>
    <automated>test -f .planning/ux/04-mockups/wireframes.md &amp;&amp; grep -cE "^## W[0-2][0-9]: " .planning/ux/04-mockups/wireframes.md | awk '{exit ($1 == 23 ? 0 : 1)}' &amp;&amp; grep -q "^## W23: Loading state" .planning/ux/04-mockups/wireframes.md &amp;&amp; grep -q "Search inside content (N conversations)" .planning/ux/04-mockups/wireframes.md &amp;&amp; grep -q "Show reasoning" .planning/ux/04-mockups/wireframes.md &amp;&amp; grep -q "Hide reasoning" .planning/ux/04-mockups/wireframes.md &amp;&amp; grep -qE "~\\\$0\\.[0-9]" .planning/ux/04-mockups/wireframes.md &amp;&amp; grep -q "^## Coverage matrix" .planning/ux/04-mockups/wireframes.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `.planning/ux/04-mockups/wireframes.md` existe.
    - Existen exactamente 23 wireframes con headings `## WXX: <nombre>` (verificable con `grep -cE "^## W[0-2][0-9]: "` → 23).
    - Los nombres exactos requeridos por D-13 aparecen como headings (lista en `must_haves.contains` arriba) — todos verificables con grep individual.
    - El copy literal `Search inside content (N conversations)` aparece en W07.
    - El doc menciona literal `Show reasoning` y `Hide reasoning` (W16 y W17 — colapsado/expandido).
    - Aparece al menos un valor de coste con formato `~$0.XX` (W13/W14/W15 — el toggle hace el coste visible).
    - Existe la sección `## Coverage matrix` con los 23 items marcados como cubiertos.
    - Existe la sección `## Conventions` explicando notación ASCII.
    - Las wireframes W04, W08, W09, W13, W14, W15 incluyen anotación `Direction` referenciando A/B/C.
    - El doc NO incluye paleta hex ni nombre de familia tipográfica como contenido del wireframe — esos están en Plan 03 (redesign) y Plan 05 (sketches).
    - W22 (error state) incluye un botón `[Retry]` o equivalente.
    - W23 (loading state) NO menciona la palabra "spinner" como elemento principal — debe describir progress communication (timeline / step indicator / texto contextual).
  </acceptance_criteria>
  <done>
    Documento `.planning/ux/04-mockups/wireframes.md` commiteado con los 23 wireframes ASCII completos, neutros tonalmente, anotados con direction notes donde aplica, y la coverage matrix marcando D-13 al 100%.
  </done>
</task>

</tasks>

<threat_model>
N/A — Plan 04 produce únicamente un documento markdown bajo `.planning/ux/04-mockups/`. Sin runtime, sin endpoints, sin código ejecutable. La política `security_enforcement` se satisface por exclusión documentada.
</threat_model>

<verification>
- 23 wireframes con headings exactos.
- Cobertura D-13 al 100% via Coverage matrix.
- Disclosure reasoning_details visible en W16/W17.
- Coste visible en Quality toggle W13/W14/W15.
- W22 con [Retry], W23 sin spinner genérico.
</verification>

<success_criteria>
Phase 4 puede tomar el wireframes.md como contrato estructural único y combinarlo con la dirección elegida en Plan 06 + el sketch HTML elegido para implementar sin volver a decidir layouts. ROADMAP success criterion #4 (parte estructural) satisfecho.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/02-ux-research-design-brief/02-04-SUMMARY.md` con:
- Conteo de wireframes producidos (debe ser 23).
- Lista de wireframes con direction-notes (qué elementos varían por dirección).
- Lista de estados D-13 cubiertos (Coverage matrix copy).
</output>
