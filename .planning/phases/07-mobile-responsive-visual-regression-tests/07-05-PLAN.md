---
phase: 07-mobile-responsive-visual-regression-tests
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
# Plan-checker BLOCKER fix (07-PLAN-CHECK §BLOCKER 1): 07-03 and 07-05 both
# modify frontend/package.json. 07-03 lands first (Playwright devDep +
# test:visual script), 07-05 appends vitest + RTL + scripts. Wave 2 shape:
# 07-04 (independent) ∥ (07-03 → 07-05). Trades ~5min serialization for
# zero package.json merge-conflict risk.
files_modified:
  - frontend/package.json
  - frontend/vite.config.js
  - frontend/src/test-setup.js
  - frontend/src/hooks/useTheme.test.js
  - frontend/src/hooks/useSettings.test.js
  - frontend/src/components/MessageHeader.test.jsx
  - frontend/src/components/QualityToggle.test.jsx
  - frontend/src/components/Stage2.test.jsx
  - frontend/src/utils/download.test.js
  - README.md
autonomous: true
requirements: [TEST-02, TEST-03]
must_haves:
  truths:
    - "`npm test --prefix frontend` runs the entire vitest suite once and exits with code 0"
    - "Every co-located `*.test.jsx` / `*.test.js` file lives next to its target module (frontend convention)"
    - "`window.matchMedia` is polyfilled in `test-setup.js` so `useTheme` tests run without errors (jsdom 29 does not implement matchMedia)"
    - "RTL v16 is used so `render` wraps in `act()` internally; no manual `act()` wrapping needed except for direct hook-result mutations"
    - "README has a `## Testing` section documenting all 3 test commands (pytest / vitest / Playwright — all native, no Docker per revised D-03)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Vitest + RTL v16 + jsdom devDependencies + `test` and `test:watch` scripts"
      contains: "vitest"
    - path: "frontend/vite.config.js"
      provides: "Test config block: jsdom env, globals: true, setupFiles: ['./src/test-setup.js'], css: false"
      contains: "jsdom"
    - path: "frontend/src/test-setup.js"
      provides: "Global afterEach cleanup + matchMedia polyfill"
      contains: "matchMedia"
    - path: "frontend/src/hooks/useTheme.test.js"
      provides: "Tests for default theme, toggle, localStorage round-trip, system-preference response, cleanup"
      contains: "useTheme"
    - path: "frontend/src/hooks/useSettings.test.js"
      provides: "Tests for default values, set* writers, localStorage validation, data-fontsize attribute side effect"
      contains: "useSettings"
    - path: "frontend/src/components/MessageHeader.test.jsx"
      provides: "Tests for legacy fallback (metadata.profile missing → 'Quality (legacy)'), profile labels, stage4 suffix, cost gating"
      contains: "Quality (legacy)"
    - path: "frontend/src/components/QualityToggle.test.jsx"
      provides: "Tests for option rendering, onChange wiring, disabled prop, value prop"
      contains: "onChange"
    - path: "frontend/src/components/Stage2.test.jsx"
      provides: "Tests for deAnonymizeText (response label replacement, multi-occurrence, no-mapping fallback, slash-less model id)"
      contains: "deAnonymizeText"
    - path: "frontend/src/utils/download.test.js"
      provides: "Tests for buildFinalAnswerMarkdown, buildFinalAnswerFilename, buildPromptWithAttachments composition"
      contains: "buildFinalAnswerMarkdown"
    - path: "README.md"
      provides: "## Testing section with the 3 test commands fenced as code blocks"
      contains: "## Testing"
  key_links:
    - from: "frontend/src/test-setup.js"
      to: "frontend/src/hooks/useTheme.test.js (and any other test that triggers matchMedia internally)"
      via: "global window.matchMedia mock installed before any test renders"
      pattern: "matchMedia"
    - from: "frontend/vite.config.js test block"
      to: "all *.test.jsx / *.test.js files in frontend/src/"
      via: "vitest discovery rooted at frontend/src/, environment: 'jsdom', globals: true"
      pattern: "environment: 'jsdom'"
    - from: "README.md ## Testing section"
      to: "the 3 test commands (uv run pytest backend/tests/ -v + npm test --prefix frontend + npx playwright test --config visual-tests/playwright.config.ts)"
      via: "fenced code blocks per TEST-03"
      pattern: "## Testing"
---

<objective>
Stand up the frontend vitest suite (TEST-02) AND document all 3 test commands in the README (TEST-03). The vitest suite covers the test targets from PATTERNS.md (`useTheme`, `useSettings`, `MessageHeader` legacy fallback, `QualityToggle` onChange wiring, `Stage2` de-anonymization, `download.js` pure helpers). README gets a single `## Testing` section with 3 fenced code blocks — minimal tone per Direction A "calmo", no separate `TESTING.md` (per RESEARCH Open Question 5 resolution).

Purpose: Lock the frontend critical paths against regression while staying within Direction A's restraint (no test framework bloat, no coverage targets, no fancy reporters). Per Phase 6 carry-forward STATE.md note: "Phase 7 planning include vitest spec for `handleStreamEvent` stale-event guard as part of TEST-01..03 to lock 06-08 against regression" — this plan adds that coverage via the `App.jsx` `handleStreamEvent` predicate (folded into the existing test-target list as an additional `App.test.jsx` if executor judges value, or noted in SUMMARY as deferred to v2.1 if the test-setup cost exceeds the value).

Output: `npm test --prefix frontend` passes cleanly; `README.md` has a single `## Testing` section with the 3 commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-CONTEXT.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-PATTERNS.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-03-SUMMARY.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-04-SUMMARY.md

# Direct test targets — read to identify exact behaviour to lock
@frontend/src/hooks/useTheme.js
@frontend/src/hooks/useSettings.js
@frontend/src/components/MessageHeader.jsx
@frontend/src/components/QualityToggle.jsx
@frontend/src/components/Stage2.jsx
@frontend/src/utils/download.js
@frontend/package.json
@frontend/vite.config.js
@README.md

<interfaces>
<!-- Vitest 4.1.5 + RTL v16.3.2 + jsdom 29.1.1 API surface — copy-paste-ready -->

From vitest (4.1.5) with globals: true:
- `describe`, `it`, `expect`, `beforeEach`, `afterEach`, `vi` — globally available, no import.
- `vi.fn()`, `vi.spyOn(obj, 'method')`, `vi.mock('module')`.

From @testing-library/react (16.3.2):
- `import { render, renderHook, act, cleanup, screen, fireEvent } from '@testing-library/react'`.
- `render(<Component />)` wraps internally in `act()` (RTL v16 changelog confirmed).
- `renderHook(() => useTheme())` returns `{ result, rerender, unmount }`; `result.current` is the hook's return value.
- `act(() => result.current.toggle())` — wrap direct hook-result mutations.

From @testing-library/user-event (^14.6):
- `import userEvent from '@testing-library/user-event'`.
- `const user = userEvent.setup(); await user.click(button)` — realistic event sequences.

From @testing-library/jest-dom (^6.6):
- `expect(element).toBeInTheDocument()`, `.toHaveTextContent(...)`, `.toHaveAttribute(...)`.

<!-- Existing hook contracts (verify by reading the actual files): -->
<!-- useTheme.js: returns { theme: 'light' | 'dark', toggle: () => void, setTheme: (t) => void } -->
<!--   - reads localStorage key 'theme' on mount -->
<!--   - if no stored value, reads window.matchMedia('(prefers-color-scheme: dark)').matches -->
<!--   - toggle flips current; setTheme sets explicit and stops responding to system -->
<!-- useSettings.js: returns { fontSize, density, stage4Threshold, setFontSize, setDensity, setStage4Threshold } -->
<!--   - keys: 'fontSize' (s|m|l), 'density' (compact|comfortable), 'stage4Threshold' (1-10, default 8) -->
<!--   - setFontSize writes localStorage AND sets data-fontsize on documentElement -->
<!--   - corrupt localStorage falls back to defaults; out-of-range writes are NOT rejected by setters -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vitest devDependencies + vite test config + test-setup.js</name>
  <read_first>
    - frontend/package.json (existing devDependencies block + scripts block)
    - frontend/vite.config.js (current 7-line config per PATTERNS.md — needs a `test` block appended)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md (Recipe 4 lines 270-350 — devDeps install command, vite.config.js extension, test-setup.js full body)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-PATTERNS.md (lines 516-630 — frontend test pattern assignments, RTL v16 act() discipline)
  </read_first>
  <files>frontend/package.json, frontend/vite.config.js, frontend/src/test-setup.js</files>
  <action>
In `frontend/package.json`:

- Add to `devDependencies` (per RESEARCH Recipe 4 line 286):
  - `"vitest": "^4.1.5"`
  - `"jsdom": "^29.1.1"`
  - `"@testing-library/react": "^16.3.2"`
  - `"@testing-library/jest-dom": "^6.6.0"`
  - `"@testing-library/user-event": "^14.6.0"`
- Add to `scripts`:
  - `"test": "vitest run"` (one-shot, exits when done — matches TEST-03 `npm test --prefix frontend` invocation)
  - `"test:watch": "vitest"` (developer watch mode; not used by the README docs)
- Do NOT touch existing scripts (`dev`, `build`, `preview`, `lint`, `test:visual` from 07-03).
- Do NOT add `@vitest/coverage-v8` — coverage is out of scope per CONTEXT "Deferred Ideas".
- Run `npm install --prefix frontend` from the host. vitest + jsdom + RTL are pure Node packages with no browser binaries. (D-03 revised — Playwright also runs natively now.)

In `frontend/vite.config.js`, EXTEND the existing config with a `test` block (per RESEARCH Recipe 4 lines 308-323):

    test: {
      environment: 'jsdom',
      globals: true,
      setupFiles: ['./src/test-setup.js'],
      css: false,
    }

`css: false` makes component CSS imports become no-ops during tests — this is critical because jsdom does not parse complex CSS and would either throw or balloon test runtime if `css: true`. Component visual styling is locked separately by 07-03 VRT.

Do NOT add `coverage` config. Do NOT add `pool` config. Do NOT add `reporters` config — the default reporter is calmo enough.

Create `frontend/src/test-setup.js` per RESEARCH Recipe 4 lines 327-350 verbatim. Sections:

1. `import '@testing-library/jest-dom/vitest';` — registers `.toBeInTheDocument()` etc. matchers.
2. `import { afterEach, vi } from 'vitest';` + `import { cleanup } from '@testing-library/react';`.
3. `afterEach(() => cleanup());` — defensive even though RTL v16 auto-cleans when `globals: true`.
4. `Object.defineProperty(window, 'matchMedia', { writable: true, value: vi.fn().mockImplementation(...) })` — the jsdom 29 polyfill. Default `matches: false` (light mode). Individual tests can override via `vi.spyOn(window, 'matchMedia')` per-test if they need dark-mode default.

Preserve the 7 properties on the matchMedia return: `matches`, `media`, `onchange`, `addEventListener`, `removeEventListener`, `addListener` (legacy), `removeListener` (legacy), `dispatchEvent`. All vi.fn() mocks.

2-space indent throughout. Use ESM (`import`/`export`) consistent with the rest of `frontend/src/`.
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; grep -n '"vitest"' frontend/package.json &amp;&amp; grep -n '"test": "vitest run"' frontend/package.json &amp;&amp; grep -n "environment: 'jsdom'" frontend/vite.config.js &amp;&amp; test -f frontend/src/test-setup.js &amp;&amp; grep -n "matchMedia" frontend/src/test-setup.js &amp;&amp; grep -n "@testing-library/jest-dom/vitest" frontend/src/test-setup.js</automated>
    <manual>None — installs and config validity are exercised by Task 3 (the test run).</manual>
  </verify>
  <done>
    - vitest, jsdom, RTL, jest-dom, user-event in `devDependencies` at the pinned versions.
    - `test` and `test:watch` scripts added; existing scripts intact.
    - `vite.config.js` has the test block with jsdom + globals + setupFiles + css:false.
    - `test-setup.js` exists with jest-dom registration + afterEach cleanup + matchMedia polyfill.
    - `npm install --prefix frontend` ran cleanly and updated `package-lock.json`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 6 co-located test files (hooks + components + utils)</name>
  <read_first>
    - frontend/src/hooks/useTheme.js (full file — to align tests with actual behaviour, especially around `followSystem` semantics and localStorage key name)
    - frontend/src/hooks/useSettings.js (full file — to confirm keys, defaults, the `data-fontsize` attribute side effect, and the corrupt-value fallback)
    - frontend/src/components/MessageHeader.jsx (lines 23-62 per PATTERNS.md — legacy fallback branch, profile labels, stage4 suffix, cost gating)
    - frontend/src/components/QualityToggle.jsx (full file — confirm exact prop names and option labels)
    - frontend/src/components/Stage2.jsx (lines 5-15 per PATTERNS.md — `deAnonymizeText` function)
    - frontend/src/utils/download.js (full file — confirm exact function signatures of `buildFinalAnswerMarkdown` / `buildFullDeliberationMarkdown` / `buildFinalAnswerFilename` / `buildDeliberationFilename` / `buildPromptWithAttachments` / `slugify`)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-PATTERNS.md (lines 518-579 — explicit test cases per file)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md (Recipe 4 sample test lines 374-399 — useTheme test pattern)
  </read_first>
  <files>frontend/src/hooks/useTheme.test.js, frontend/src/hooks/useSettings.test.js, frontend/src/components/MessageHeader.test.jsx, frontend/src/components/QualityToggle.test.jsx, frontend/src/components/Stage2.test.jsx, frontend/src/utils/download.test.js</files>
  <action>
Create 6 co-located test files. 2-space indent. Use `describe(...)` + `it(...)` blocks. Globals from vitest (no imports needed for `describe` / `it` / `expect` / `vi` because `globals: true` in vite.config.js).

**frontend/src/hooks/useTheme.test.js** — RESEARCH Recipe 4 sample lines 376-399 + PATTERNS.md test cases lines 524-530. Use `renderHook` from RTL. Wrap mutations (`result.current.toggle()`) in `act()`. Test cases:

- `it('defaults to light when no localStorage and matchMedia.matches=false')` — `localStorage.clear()`, render hook, assert `result.current.theme === 'light'`.
- `it('defaults to dark when matchMedia.matches=true')` — override the matchMedia mock to return `{ matches: true, ... }` via `vi.spyOn(window, 'matchMedia').mockImplementation(() => ({ matches: true, ... }))`, render hook, assert `'dark'`.
- `it('toggle flips theme and persists to localStorage')` — render, act+toggle, assert theme switched AND `localStorage.getItem('theme')` matches.
- `it('setTheme stops responding to system changes')` — set explicit theme; simulate matchMedia change event (call the registered `addEventListener` listener); assert theme did NOT change. Use the actual hook implementation as ground truth — if the hook's `followSystem` semantics differ from PATTERNS, write the test against the actual behaviour.
- `it('unmount removes the matchMedia listener')` — render, unmount, assert `removeEventListener` was called on the matchMedia mock.

Use `beforeEach(() => { localStorage.clear(); document.documentElement.removeAttribute('data-theme'); })` at the describe-block top.

**frontend/src/hooks/useSettings.test.js** — PATTERNS.md test cases lines 535-540. Same `renderHook` pattern. Test cases:

- `it('defaults: fontSize=m, density=comfortable, stage4Threshold=8')` — render, assert all three. The `8` matches `PROFILES["quality_research"]["stage4_threshold"]` in `backend/config.py`.
- `it('setFontSize writes localStorage and sets data-fontsize attr')` — act+setFontSize('l'); assert `localStorage.getItem('fontSize') === 'l'` AND `document.documentElement.getAttribute('data-fontsize') === 'l'`.
- `it('corrupt localStorage value falls back to default')` — `localStorage.setItem('fontSize', 'xyz')`, render hook, assert `fontSize === 'm'` (validation in the reader, per useSettings.js around lines 24-28 per PATTERNS).
- `it('setStage4Threshold accepts out-of-range without rejection')` — setter writes whatever value; reader is the validation gate. Document the behaviour. If actual code DOES reject in the setter, write the test against actual behaviour.

**frontend/src/components/MessageHeader.test.jsx** — PATTERNS.md test cases lines 545-550. Use `render` from RTL + `screen.getByText` / `screen.queryByText` assertions.

- `it('renders Quality (legacy) when metadata.profile is missing')` — render `<MessageHeader />` with `metadata={undefined}` (or `{}`), assert `screen.getByText(/quality \(legacy\)/i)` is in the document.
- `it('renders Fast label when profile=fast')` — assert `screen.getByText(/fast/i)`.
- `it('renders Stage 4 refinement suffix when stage4_triggered=true')` — assert the suffix text is present.
- `it('shows cost line when cost.total >= 0.001, hides below')` — render with `cost.total = 0.002` → cost line present; `cost.total = 0.0001` → not present. The 0.001 threshold is in `MessageHeader.jsx:39-41` per PATTERNS — confirm by reading.

**frontend/src/components/QualityToggle.test.jsx** — PATTERNS.md test cases lines 555-559. Use `@testing-library/user-event` for clicks.

- `it('renders 3 options (fast / quality / quality_research) with cost labels')` — render, assert all 3 labels present.
- `it('clicking Quality option fires onChange with quality')` — render with `onChange` spy (`vi.fn()`); `await user.click(qualityRadio)`; assert `onChange` was called exactly once with `'quality'`.
- `it('disabled prop disables all radios')` — render with `disabled=true`; assert all 3 radios have `disabled` attribute set.
- `it('value prop controls which radio is checked')` — render with `value='quality'`; assert `qualityRadio.checked === true` and the others are not.

**frontend/src/components/Stage2.test.jsx** — PATTERNS.md test cases lines 565-569. The target `deAnonymizeText` is a pure function exported from Stage2.jsx (lines 5-15 per PATTERNS). If it is exported, import and test directly. If not exported, the test renders `<Stage2 />` with mocked data and asserts the rendered output contains the de-anonymized model name in `<strong>`/bold formatting.

- `it('replaces Response A with bold model short name')` — `deAnonymizeText("Response A is great", { "Response A": "openai/gpt-5.5" })` → `"**gpt-5.5** is great"` (or whatever the exact bold-format syntax is).
- `it('replaces multiple occurrences')`.
- `it('returns text unchanged when mapping is empty')`.
- `it('handles model id with no slash')` — model id like `"some-model"` uses the full id as the short name.

If `deAnonymizeText` is NOT exported from Stage2.jsx, the executor has two options:
  (a) Export it (one-line addition to Stage2.jsx — `export function deAnonymizeText(...)`) and import in the test. This is the lowest-friction path.
  (b) Test indirectly via `render(<Stage2 />)` + DOM assertions on the bold output. More fragile.
Choose (a) unless the executor sees a reason to keep `deAnonymizeText` private (none expected per PATTERNS).

**frontend/src/utils/download.test.js** — PATTERNS.md test cases lines 575-579. All pure functions, no DOM needed for most.

- `it('buildFinalAnswerMarkdown without stage4 — no refined suffix')` — call with `stage4: null`; assert returned markdown contains the question and the response, no `(refined)` token.
- `it('buildFinalAnswerMarkdown with stage4.response — uses refined content')` — call with `stage4.response = "..."`; assert title contains `(refined)`, body contains stage4 content NOT finalResponse.response (per download.js:52-73 per PATTERNS).
- `it('buildFinalAnswerFilename slugifies and timestamps')` — assert lowercase, dashes, no special chars, ends with a timestamp pattern.
- `it('buildPromptWithAttachments composes prompt + file contents')` — call with prompt + array of `{ name, content }`; assert the returned string contains the prompt followed by file contents in attachment order.

For each test file, follow the convention:
- One `describe(<module-or-feature-name>, () => { ... })` per file (or per major exported function for `download.test.js` which has multiple).
- `it` blocks describe behaviour, not implementation.
- No comments unless WHY is non-obvious.

OPTIONAL: Per the STATE.md Phase 6 carry-forward ("vitest spec for `handleStreamEvent` stale-event guard as part of TEST-01..03 to lock 06-08 against regression"), the executor MAY add a 7th file `frontend/src/App.test.jsx` testing the `handleStreamEvent` `if (!lastMsg?.loading) return prev;` guard. If the cost is low (the predicate can be tested by simulating stale events against a mocked state via a wrapper), do it. If `App.jsx` is too tightly coupled to make this affordable, defer to v2.1 and note in SUMMARY. This is `auto` discretion, not a hard requirement.
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; ls frontend/src/hooks/*.test.js frontend/src/components/*.test.jsx frontend/src/utils/*.test.js 2&gt;/dev/null &amp;&amp; grep -c "it(" frontend/src/hooks/useTheme.test.js frontend/src/hooks/useSettings.test.js frontend/src/components/MessageHeader.test.jsx frontend/src/components/QualityToggle.test.jsx frontend/src/components/Stage2.test.jsx frontend/src/utils/download.test.js</automated>
    <manual>None — executed in Task 3.</manual>
  </verify>
  <done>
    - 6 (or 7 if App.test.jsx added) test files exist co-located with their targets.
    - Each file uses `describe` + `it` and globals from vitest (no per-file import of `describe`/`it`).
    - useTheme + useSettings tests use `renderHook` + `act` for mutation.
    - MessageHeader / QualityToggle / Stage2 tests use `render` + `screen` queries.
    - download tests are pure-function calls + assertions.
    - All tests describe ACTUAL behaviour discovered by reading source files, not aspirational behaviour from PATTERNS.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run vitest suite + document the 3 commands in README</name>
  <read_first>
    - README.md (full file — to identify the best insertion point for the `## Testing` section; default is after `## Setup` / `## Running locally` per RESEARCH Open Question 5 resolution)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-03-SUMMARY.md (for the exact native `npx playwright test` command)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-04-SUMMARY.md (for the pytest entry — though the canonical command is the locked `uv run pytest backend/tests/ -v` from TEST-03)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-CONTEXT.md (D-04b — README only, no CI; D-03 revised — README documents the native `npx playwright test` command)
  </read_first>
  <files>(none new) — modifies README.md and runs vitest</files>
  <action>
First, run from the project root:

    npm test --prefix frontend

Expected: all tests PASS, vitest exits 0. If a test fails:

1. **If the test was written against a wished-for behaviour** that doesn't match the real hook/component, correct the TEST (not the source code) to document reality.
2. **If the test reveals a real bug**, note it as an incidental finding in SUMMARY with severity classification, mark the test `it.skip(...)` or `it.todo(...)` if not critical, and continue. Do NOT fix source code in this plan (out of scope — quality lock, not bug-fix lap).
3. If `matchMedia` errors surface, double-check `test-setup.js` is wired via `setupFiles` in `vite.config.js` and that the polyfill is `defineProperty(window, 'matchMedia', ...)` (NOT `window.matchMedia = ...` which jsdom resists).
4. Re-run until green.

Second, edit `README.md` to add a `## Testing` section (per TEST-03 + RESEARCH Open Question 5 — append, NOT a separate `TESTING.md`).

Place the new section AFTER the existing "Setup" / "Running locally" section (whichever currently exists) and BEFORE any "Architecture" / "Contributing" sections. The section MUST be terse — one paragraph + 3 fenced code blocks, matching Direction A "calmo" tone (per CONTEXT D-04b + RESEARCH Open Question 5):

    ## Testing

    Three local test commands. No CI — that's intentional (single-user, local-only posture; v2.1 backlog).

    ### Backend (pytest)
    ```
    uv run pytest backend/tests/ -v
    ```

    ### Frontend (vitest)
    ```
    npm test --prefix frontend
    ```

    ### Visual regression (Playwright, native Windows)
    ```
    cd frontend && npx playwright test --config visual-tests/playwright.config.ts
    ```

    Playwright runs natively on Windows (D-03 revised 2026-05-11 — baselines committed as Windows PNGs; Docker variant `mcr.microsoft.com/playwright:v1.59.1-noble` is a v2.1+ contingency if CI or multi-OS contributors are introduced). Add `--update-snapshots` to regenerate baselines after intended visual changes.

Tone notes:
- One short paragraph before the commands. No bullet lists of what each suite covers (the test files self-document).
- No "What if X fails?" troubleshooting subsections (out of scope; Direction A "research notebook" tone).
- The "v2.1 backlog" framing for CI is explicit per D-04b — the README acknowledges the gap rather than hiding it.
- The `npx playwright test` command is verbatim from 07-03 SUMMARY for consistency.

Do NOT add a `## Continuous Integration` section. Do NOT add a `[![CI](https://...)]` badge. Do NOT touch any other README section.
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; npm test --prefix frontend 2&gt;&amp;1 | tail -10 &amp;&amp; grep -n "## Testing" README.md &amp;&amp; grep -c "uv run pytest" README.md &amp;&amp; grep -c "npm test --prefix frontend" README.md &amp;&amp; grep -c "mcr.microsoft.com/playwright:v1.59.1-noble" README.md</automated>
    <manual>
      1. Open README.md in a markdown preview. The Testing section reads in under 30 seconds. No extra fluff.
      2. Copy each fenced command into a terminal and run it. All three exit cleanly (Playwright first run downloads Chromium binary, ~2 min — subsequent runs are fast).
    </manual>
  </verify>
  <done>
    - `npm test --prefix frontend` exits 0; the vitest summary line shows all tests passing.
    - README.md has a `## Testing` section with the 3 commands fenced.
    - The section is short (≤15 lines including code fences) per Direction A "calmo" guidance.
    - The "v2.1 backlog" CI framing is explicit per D-04b.
    - Any incidental findings (skipped tests, real bugs surfaced) are documented in SUMMARY.
  </done>
</task>

</tasks>

<verification>
- `npm test --prefix frontend` from the project root passes with 0 failures.
- `grep -n "## Testing" README.md` returns exactly one line.
- The README Testing section contains all 3 commands (pytest / vitest / Playwright Docker).
- jsdom environment does not throw on `matchMedia` access (polyfill in test-setup.js is wired).
- No `.github/workflows/`, no `.gitlab-ci.yml`, no CI badge added (per D-04b).
- No `TESTING.md` file created (per RESEARCH Open Question 5 resolution — appended to README instead).
</verification>

<success_criteria>
- TEST-02 satisfied: vitest + RTL v16 + jsdom 29 configured; 6 co-located test files cover useTheme, useSettings, MessageHeader legacy fallback, QualityToggle onChange wiring, Stage2 de-anonymization, download.js pure helpers.
- TEST-03 satisfied: README `## Testing` section documents all 3 commands; no CI configuration.
- `npm test --prefix frontend` runs cleanly with 0 failures.
- jsdom 29's missing matchMedia is polyfilled before any test runs (structural via setupFiles).
- Direction A "calmo" tone respected in README — terse section, no bullet-list bloat.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mobile-responsive-visual-regression-tests/07-05-SUMMARY.md` summarising:
- The final vitest summary line (e.g., `Test Files  6 passed (6)  Tests  24 passed (24)`).
- Whether the optional `App.test.jsx` for `handleStreamEvent` regression coverage was added (or deferred to v2.1, with rationale).
- Whether `deAnonymizeText` was made a named export from Stage2.jsx (one-line addition) or tested indirectly via render+DOM assertions.
- Any incidental findings (skipped tests, source-code bugs surfaced but not fixed in this plan, with severity).
- Confirmation that all 10 Phase 7 requirements are now satisfied across the 5 plans (MOBL-01..04 from 07-01+02, VRT-01..03 from 07-03, TEST-01 from 07-04, TEST-02+03 from this plan).
- Phase 7 ready for `/gsd-verify-phase 7`.
</output>
