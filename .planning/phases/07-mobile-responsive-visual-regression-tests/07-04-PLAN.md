---
phase: 07-mobile-responsive-visual-regression-tests
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - pyproject.toml
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_storage.py
  - backend/tests/test_research_strategy.py
  - backend/tests/test_council_profile.py
  - backend/tests/fixtures/conversation_v1_sample.json
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "`uv run pytest backend/tests/ -v` runs cleanly from the project root with all tests passing"
    - "`pytest-asyncio` is configured in mode `strict` (NOT `auto`)"
    - "Every test runs in an isolated tmp `DATA_DIR` — no test touches the real `data/conversations/`"
    - "Storage UUID validation, path-traversal rejection, and the v1→v2 migration helper are covered"
    - "`add_assistant_message` round-trip is tested with metadata + stage4 + external_research variants"
    - "`research_strategy.parse_critic_score` is tested against the malformed-input matrix"
    - "Council profile routing (fast / quality / quality_research) is tested with `patch_query_model` fixture"
  artifacts:
    - path: "pyproject.toml"
      provides: "`[project.optional-dependencies] test = ['pytest>=9.0', 'pytest-asyncio>=1.3']` + `[tool.pytest.ini_options] asyncio_mode = 'strict'`"
      contains: "asyncio_mode = \"strict\""
    - path: "backend/tests/__init__.py"
      provides: "Empty package marker so `backend.tests` is importable"
      min_lines: 0
    - path: "backend/tests/conftest.py"
      provides: "`_tmp_data_dir` autouse fixture (double monkeypatch of DATA_DIR), `_dummy_api_key` autouse fixture, `patch_query_model` factory, `api_client` ASGI fixture, `make_conversation` / `make_assistant_message` factories"
      contains: "backend.storage.DATA_DIR"
    - path: "backend/tests/test_storage.py"
      provides: "UUID validation + path-traversal + round-trip + v1→v2 migration tests"
      contains: "def test_"
    - path: "backend/tests/test_research_strategy.py"
      provides: "parse_critic_score test matrix (valid / case-insensitive / clamping / last-occurrence / missing / empty)"
      contains: "parse_critic_score"
    - path: "backend/tests/test_council_profile.py"
      provides: "Profile routing tests for fast / quality / quality_research + stage4 threshold gating"
      contains: "PROFILES"
    - path: "backend/tests/fixtures/conversation_v1_sample.json"
      provides: "Real pre-v2 conversation shape (no schema_version, no mode) captured from disk for migration round-trip test"
      contains: "messages"
  key_links:
    - from: "backend/tests/conftest.py _tmp_data_dir fixture"
      to: "backend.config.DATA_DIR AND backend.storage.DATA_DIR"
      via: "monkeypatch.setattr on BOTH module attributes (single-patch is a silent no-op due to import-time capture at backend/storage.py:9)"
      pattern: "backend\\.storage\\.DATA_DIR"
    - from: "backend/tests/test_storage.py migration tests"
      to: "the v1→v2 migration helper inside `backend/storage.py` (`_migrate_conversation_if_needed` or equivalent — verify exact name in the file)"
      via: "direct import + invocation with v1-shaped dict"
      pattern: "schema_version"
    - from: "backend/tests/test_council_profile.py"
      to: "backend.openrouter.query_model"
      via: "patch_query_model factory fixture installing an async fake"
      pattern: "patch_query_model"
---

<objective>
Stand up the backend pytest suite per TEST-01. The suite uses `pytest-asyncio` in `strict` mode (NOT `auto`), a `conftest.py` with double-monkeypatch isolation of `DATA_DIR` (because `backend/storage.py:9` captures it at import time), factory functions for canonical-shape conversations and messages, an ASGI in-process `httpx.AsyncClient` fixture for FastAPI tests, and four test modules covering storage / research_strategy parser / council profile routing.

Purpose: First test suite in a previously test-free repo. Locks the critical paths against regression — UUID validation + path-traversal rejection (Phase 1 SEC-01 hardening), `add_assistant_message` metadata + stage4 + external_research persistence (Phase 5/6 contracts), v1→v2 migration (Phase 5 PERS-03 — "the test of greatest value of the milestone" per CONTEXT decisions), and the critic parser malformed-input tolerance (Phase 3 + 5 critic flow).

Output: `uv run pytest backend/tests/ -v` runs cleanly with N passing tests, 0 failing, 0 errors. No CI configuration (forbidden by D-04b).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-CONTEXT.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md
@.planning/phases/07-mobile-responsive-visual-regression-tests/07-PATTERNS.md

# Direct test targets — read to identify exact function signatures and line ranges
@backend/storage.py
@backend/council.py
@backend/research_strategy.py
@backend/config.py
@backend/openrouter.py
@backend/main.py
@pyproject.toml

<interfaces>
<!-- pytest-asyncio strict mode = every async test must be marked `@pytest.mark.asyncio`.
     `auto` would implicitly mark them; we use `strict` to keep test intent explicit. -->

<!-- backend.storage public surface (read backend/storage.py to confirm current signatures): -->
<!-- - get_conversation_path(conversation_id: str) → Path  (validates UUID + path-traversal) -->
<!-- - create_conversation(conversation_id: str, title: str = "") → dict -->
<!-- - get_conversation(conversation_id: str) → dict  (applies _migrate_conversation_if_needed lazily) -->
<!-- - save_conversation(conv: dict) → None -->
<!-- - add_user_message(conversation_id: str, content: str) → dict -->
<!-- - add_assistant_message(conversation_id: str, stage1, stage2, stage3, *, metadata=None, stage4=None, external_research=None) → dict -->
<!-- - DATA_DIR: str — module-level constant captured from backend.config at import time -->

<!-- backend.research_strategy public surface (read backend/research_strategy.py around line 112): -->
<!-- - parse_critic_score(text: str) → tuple[int | None, str | None]  -->
<!--   Parses the `CRITIC SCORE: N\nPRIMARY CONCERN: ...` block from a critic LLM response. -->

<!-- backend.config public surface: -->
<!-- - PROFILES: dict[str, dict]  — keyed by 'fast' / 'quality' / 'quality_research'; each has 'council_models', 'chairman_model', 'stage4_threshold' etc. -->
<!-- - DATA_DIR: str -->
<!-- - OPENROUTER_API_KEY: str -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure pytest in pyproject.toml + create test package skeleton</name>
  <read_first>
    - pyproject.toml (existing `[project]` block — to determine where to add `[project.optional-dependencies]` and `[tool.pytest.ini_options]`)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md (Recipe 3 lines 170-186 — pyproject config block verbatim)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-CONTEXT.md (RESEARCH Open Question 2 — resolved as `backend/tests/` with `__init__.py`)
  </read_first>
  <files>pyproject.toml, backend/tests/__init__.py</files>
  <action>
In `pyproject.toml`, append (per RESEARCH Recipe 3):

    [project.optional-dependencies]
    test = [
        "pytest>=9.0",
        "pytest-asyncio>=1.3",
    ]

    [tool.pytest.ini_options]
    asyncio_mode = "strict"
    testpaths = ["backend/tests"]
    python_files = ["test_*.py"]
    asyncio_default_fixture_loop_scope = "function"

The `asyncio_default_fixture_loop_scope = "function"` line silences the pytest-asyncio 1.x deprecation warning about an unset fixture loop scope (RESEARCH).

Run `uv lock` to refresh `uv.lock` with the test dependencies (per the existing uv-managed workflow). The executor agent may also run `uv sync --extra test` to install them into the local environment for the test run, but installation is optional at this stage — Task 4 (the test run) will install on demand if needed.

Create `backend/tests/__init__.py` as an EMPTY file (zero bytes, no docstring). The empty file makes `backend.tests` an importable package (Python convention) so `pytest` discovers tests under it without `sys.path` shenanigans (RESEARCH Open Question 2 resolution).

Do NOT add a `[tool.coverage.*]` section. Do NOT add a `pytest --cov` script. Coverage % targets are out of scope per CONTEXT "Deferred Ideas".
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; grep -n 'asyncio_mode = "strict"' pyproject.toml &amp;&amp; grep -n 'pytest>=9.0' pyproject.toml &amp;&amp; test -f backend/tests/__init__.py &amp;&amp; test ! -s backend/tests/__init__.py</automated>
    <manual>None.</manual>
  </verify>
  <done>
    - `pyproject.toml` has `[project.optional-dependencies].test` with `pytest>=9.0` and `pytest-asyncio>=1.3`.
    - `pyproject.toml` has `[tool.pytest.ini_options]` with `asyncio_mode = "strict"`, `testpaths = ["backend/tests"]`, `asyncio_default_fixture_loop_scope = "function"`.
    - `backend/tests/__init__.py` exists as a zero-byte file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create conftest.py with isolation fixtures + factories</name>
  <read_first>
    - backend/storage.py (line 9 specifically — confirm `from .config import DATA_DIR` import shape that mandates double monkeypatch; also lines 32-275 for the function signatures the tests will exercise)
    - backend/config.py (to confirm DATA_DIR + OPENROUTER_API_KEY module-level attributes)
    - backend/main.py (FastAPI app variable name — confirm it is `app` for the ASGI fixture)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md (Recipe 3 lines 188-264 — conftest.py block verbatim; Test Fixture Recommendation lines 638-664 — factory pattern)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-PATTERNS.md (lines 449-470 — conftest is the verbatim recipe)
  </read_first>
  <files>backend/tests/conftest.py</files>
  <action>
Create `backend/tests/conftest.py` per RESEARCH Recipe 3 lines 188-246, augmented with the factory functions from RESEARCH "Test Fixture Recommendation" lines 647-664. The file MUST be 4-space indented (backend convention per CLAUDE.md). Sections:

1. **Module docstring** — one line: `"""Shared fixtures for backend tests."""`
2. **Imports** — `os`, `pathlib.Path`, `typing.AsyncIterator` (Python 3.10+ has it stable in `typing`), `pytest`, `pytest_asyncio`, `from httpx import ASGITransport, AsyncClient`.
3. **`_tmp_data_dir` autouse fixture** — yields a per-test tmp directory and double-monkeypatches `backend.config.DATA_DIR` AND `backend.storage.DATA_DIR`. Include the docstring from RESEARCH Recipe 3 explaining WHY both attrs need patching (storage.py imports DATA_DIR at import time per backend/storage.py:9 — single patch is a silent no-op).
4. **`_dummy_api_key` autouse fixture** — sets `OPENROUTER_API_KEY` env var AND monkeypatches `backend.config.OPENROUTER_API_KEY`. Same double-attr reasoning.
5. **`patch_query_model` factory fixture** — returns a helper `_install(fake)` that monkeypatches `backend.openrouter.query_model` with an async wrapper around `fake(model, messages)`. Verbatim from RESEARCH Recipe 3 lines 224-237.
6. **`api_client` async fixture** — yields an `httpx.AsyncClient` with `ASGITransport(app=app)`. CRITICAL: import `backend.main.app` LATE (inside the fixture body) so the autouse monkeypatches have applied before the FastAPI app is constructed. Verbatim from RESEARCH Recipe 3 lines 239-246.
7. **`make_conversation` factory fixture** — returns a `_make(*, schema_version=2, mode='fresh', messages=None, title='test')` function building the canonical conversation dict. Default `id = '00000000-0000-0000-0000-000000000001'`, default `created_at = '2026-05-11T00:00:00Z'`. If `schema_version is None` → omit the key entirely (so callers can build pre-v2 shapes). If `mode is None` → omit `mode` (same reason). Verbatim from RESEARCH "Test Fixture Recommendation" pattern lines 648-664.
8. **`make_assistant_message` factory fixture** — returns a `_make(*, content='ok', stage1=None, stage2=None, stage3=None, metadata=None, stage4=None, external_research=None)` function returning a dict matching the canonical assistant message shape produced by `backend.storage.add_assistant_message`. Only includes keys whose value is non-None.

Use `@pytest.fixture` for synchronous fixtures, `@pytest_asyncio.fixture` for `api_client`. Place autouse fixtures with `autouse=True`.

Do NOT add `pytest.fixture(scope='session')` anywhere — function scope (the default) is required for test isolation; session scope would leak state between tests.
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; test -f backend/tests/conftest.py &amp;&amp; grep -n "backend.storage.DATA_DIR" backend/tests/conftest.py &amp;&amp; grep -n "backend.config.DATA_DIR" backend/tests/conftest.py &amp;&amp; grep -n "ASGITransport" backend/tests/conftest.py &amp;&amp; grep -n "make_conversation" backend/tests/conftest.py &amp;&amp; grep -n "make_assistant_message" backend/tests/conftest.py</automated>
    <manual>None — conftest correctness is exercised by Task 4 when the suite runs.</manual>
  </verify>
  <done>
    - `conftest.py` exists with 4-space indent.
    - `_tmp_data_dir` autouse fixture monkeypatches BOTH `backend.config.DATA_DIR` AND `backend.storage.DATA_DIR`.
    - `_dummy_api_key` autouse fixture monkeypatches both module-level attrs.
    - `patch_query_model` factory fixture returns an async wrapper around `fake(model, messages)`.
    - `api_client` fixture uses `ASGITransport(app=app)` with late `backend.main` import.
    - `make_conversation` factory supports `schema_version=None` for pre-v2 shapes.
    - `make_assistant_message` factory supports `metadata` / `stage4` / `external_research` extras.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write test_storage.py + test_research_strategy.py + test_council_profile.py + the v1 sample fixture</name>
  <read_first>
    - backend/storage.py (line ranges per PATTERNS.md: get_conversation_path at lines 32-51, _migrate_conversation_if_needed at lines 72-88, add_assistant_message at lines 215-275, the conversation shape construction at lines 91-123)
    - backend/research_strategy.py (parse_critic_score at line 112-144)
    - backend/council.py (profile routing logic — confirm the function that dispatches by profile, and that `quality_research` delegates to `research_strategy.run()`)
    - backend/config.py (PROFILES dict — lines 58-77 per PATTERNS.md)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-PATTERNS.md (lines 471-513 — explicit test cases per file)
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md (Recipe 3 sample async test lines 252-264)
    - data/conversations/ (look at one real v1 conversation if any exist; if all conversations are already v2, synthesize a v1 shape per backend/storage.py:91-123 minus schema_version and mode)
  </read_first>
  <files>backend/tests/test_storage.py, backend/tests/test_research_strategy.py, backend/tests/test_council_profile.py, backend/tests/fixtures/conversation_v1_sample.json</files>
  <action>
Create 3 test modules + 1 fixture JSON. 4-space indent throughout (backend convention). One-line docstrings on each test function explaining intent. Test names follow `test_<what>_<expected_behavior>` convention.

**backend/tests/test_storage.py** (per PATTERNS.md lines 471-490):

- `test_get_conversation_path_rejects_non_uuid` — `get_conversation_path("not-a-uuid")` raises `ValueError`.
- `test_get_conversation_path_rejects_path_traversal` — `get_conversation_path("../evil")` raises `ValueError` (same code path as invalid UUID; both must fail closed per Phase 1 SEC-01).
- `test_get_conversation_path_accepts_valid_uuid` — `get_conversation_path("00000000-0000-0000-0000-000000000001")` returns a `Path` rooted in the tmp `DATA_DIR`.
- `test_create_and_get_round_trip` — `create_conversation(id)` followed by `get_conversation(id)` returns a dict whose `id` matches.
- `test_add_assistant_message_persists_metadata` — call with `metadata={"profile": "fast", "label_to_model": {...}, "aggregate_rankings": [...]}`, then read back via `get_conversation` and assert the metadata dict is intact (per Phase 6 PERS-01/02 contract — metadata is an opaque dict, persisted verbatim).
- `test_add_assistant_message_persists_stage4_when_present` — when `stage4={...}` is passed, the resulting message has both `metadata` and `stage4` keys; when `stage4=None`, the `stage4` key is absent (NOT `null`).
- `test_add_assistant_message_persists_external_research_when_present` — when `external_research={...}`, the key is persisted; when `None`, the key is absent.
- `test_migration_adds_schema_version_2_to_v1_dict` — load `fixtures/conversation_v1_sample.json`, pass through the migration helper, assert returned dict has `schema_version == 2` and `mode == "fresh"` (per Phase 5 PERS-03 contract).
- `test_migration_is_idempotent` — pass an already-v2 dict; returned dict has the same `schema_version` and `mode` values (no double-stamp).
- `test_migration_adds_empty_metadata_to_legacy_assistant_messages` — assistant messages in the v1 sample without `metadata` key receive `metadata: {}` after migration.

The migration helper name is `_migrate_conversation_if_needed` per PATTERNS.md line 487, but the executor MUST verify this by reading `backend/storage.py` — if the helper has a different actual name (e.g. public `migrate_message_v1_to_v2` per the STATE.md "Carryover" reference), use the ACTUAL name. Same applies to whether migration is invoked automatically inside `get_conversation` (most likely yes per Phase 5 design); the test exercises whichever entry point is canonical.

**backend/tests/test_research_strategy.py** (per PATTERNS.md lines 492-503):

- `test_parse_critic_score_valid_input` — `"CRITIC SCORE: 7\nPRIMARY CONCERN: needs more detail"` → `(7, "needs more detail")`.
- `test_parse_critic_score_case_insensitive` — `"critic score: 5"` → `(5, None)` (lowercase header still parses).
- `test_parse_critic_score_clamps_high` — `"CRITIC SCORE: 99"` → score capped at 10.
- `test_parse_critic_score_clamps_low` — `"CRITIC SCORE: -3"` → score floored at 1.
- `test_parse_critic_score_last_occurrence_wins` — `"CRITIC SCORE: 3 ... actually CRITIC SCORE: 8"` → `(8, ...)` (defensive against critics who hedge mid-response).
- `test_parse_critic_score_missing_returns_none` — `"no score here"` → `(None, None)`.
- `test_parse_critic_score_empty_returns_none` — `""` → `(None, None)`.

If the parser's actual behaviour for clamping or last-occurrence differs from what PATTERNS expects, the executor MUST: (a) verify via direct call before writing the assertion; (b) write the test against the ACTUAL behaviour (not the wished-for behaviour); (c) note the discrepancy in the plan SUMMARY. Tests document reality, not aspiration.

**backend/tests/test_council_profile.py** (per PATTERNS.md lines 505-513):

Use `@pytest.mark.asyncio` (strict mode requires explicit marker) for any test that calls async council functions.

- `test_fast_profile_routes_to_fast_council_models` — install `patch_query_model` to record which `model` ids are queried; trigger a council run with profile `'fast'`; assert the called model ids match `PROFILES['fast']['council_models']` exactly (per backend/config.py:58-63 per PATTERNS).
- `test_quality_profile_routes_to_quality_council_models` — same shape, for `PROFILES['quality']`.
- `test_quality_research_delegates_to_research_strategy` — install `patch_query_model`; trigger profile `'quality_research'`; assert that `research_strategy.run()` was called (use `monkeypatch.setattr` on `backend.research_strategy.run` to install a recorder) — council.py never hits its own Stage 1/2/3 for this profile (per CLAUDE.md / STATE.md carry-over invariant).
- `test_stage4_threshold_triggers_refinement_below_cutoff` — install a critic-score patch that returns a low score (e.g., 5) below threshold 8; assert Stage 4 was invoked (verify by recording call to whatever function gates Stage 4 — likely a refinement coroutine inside `research_strategy.py`).
- `test_stage4_threshold_skipped_at_or_above_cutoff` — critic-score returns 8 (== threshold); Stage 4 is NOT invoked.

For the last 2 tests, the executor MUST inspect `backend/research_strategy.py` to identify the actual gating call; the assertion target is "the Stage 4 refinement function was/wasn't called", not "the conversation has/lacks a stage4 field" (the latter is the persisted outcome; the test is about the in-process routing decision).

**backend/tests/fixtures/conversation_v1_sample.json**:

Synthesize a real pre-v2 conversation shape per `backend/storage.py:91-123` minus the `schema_version` and `mode` fields. Include:

- `id`: `"00000000-0000-0000-0000-000000000001"`
- `title`: a realistic short title (e.g., `"v1 sample for migration test"`)
- `created_at`: `"2026-05-01T00:00:00Z"` (predates v2 deployment)
- `messages`: a 2-message conversation — one user, one assistant. The assistant message has `stage1`, `stage2`, `stage3` but NO `metadata` key (per Phase 5 PERS-03 — the migration backfills `metadata: {}` for legacy assistant messages).
- NO `schema_version` key.
- NO `mode` key.

If a real pre-v2 conversation exists in `data/conversations/` (unlikely since Phase 5 already migrated them lazily, but check), copy that one VERBATIM with PII redacted. Otherwise synthesise per shape.

Pretty-print with 2-space indent (JSON convention) for diff readability.
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; test -f backend/tests/test_storage.py &amp;&amp; test -f backend/tests/test_research_strategy.py &amp;&amp; test -f backend/tests/test_council_profile.py &amp;&amp; test -f backend/tests/fixtures/conversation_v1_sample.json &amp;&amp; grep -c "def test_" backend/tests/test_storage.py &amp;&amp; grep -c "def test_" backend/tests/test_research_strategy.py &amp;&amp; grep -c "def test_" backend/tests/test_council_profile.py</automated>
    <manual>None — the suite is run end-to-end in Task 4.</manual>
  </verify>
  <done>
    - 4 files exist with the test bodies + fixture.
    - Every test has a one-line docstring stating intent.
    - Every async test is decorated `@pytest.mark.asyncio` (strict mode requirement).
    - Migration tests use the ACTUAL migration helper name (verified by reading `backend/storage.py`, not assumed from PATTERNS.md).
    - Parser tests document ACTUAL parser behaviour (clamping / last-occurrence / case sensitivity verified by direct call before assertions hardcode).
    - Profile-routing tests intercept `query_model` via `patch_query_model` rather than mocking HTTP.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run the full pytest suite and ensure it passes cleanly</name>
  <read_first>
    - .planning/phases/07-mobile-responsive-visual-regression-tests/07-RESEARCH.md (Recipe 3 — `uv run pytest backend/tests/ -v` is the canonical invocation)
  </read_first>
  <files>(none — execution only)</files>
  <action>
Run from the project root:

    uv sync --extra test
    uv run pytest backend/tests/ -v

Expected outcome: all tests PASS, 0 fail, 0 error. If a test fails:

1. **Read the failure carefully.** If the assertion contradicts ACTUAL backend behaviour (i.e., the test was written against a wished-for behaviour from PATTERNS.md that doesn't match the real code), correct the TEST to document reality — NOT the code to match the wish. If the failure reveals a real bug in `backend/storage.py` etc., NOTE IT and proceed; do NOT fix backend code in this plan (out of scope for Phase 7 — the suite is a quality lock, not a bug-fix lap).
2. **If a test reveals a pre-existing bug** (e.g., the migration helper doesn't actually backfill `metadata: {}` despite the contract), document the gap in SUMMARY as an "incidental finding" with severity (critical / moderate / cosmetic) and the test EITHER (a) is marked `@pytest.mark.xfail(reason="incidental — track in v2.1")` if the bug is moderate/cosmetic, OR (b) prompts an immediate halt + plan revision if the bug is critical.
3. **Re-run the suite** until it is green.

Verify no deprecation warnings about asyncio mode (`asyncio_default_fixture_loop_scope = "function"` should silence the only known one).

Commit the entire suite (`backend/tests/` + `pyproject.toml` changes + any `uv.lock` update) as one commit per the existing `docs($PHASE): create phase plan` convention from the workflow.
  </action>
  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; uv run pytest backend/tests/ -v 2&gt;&amp;1 | tail -20</automated>
    <manual>
      Read the final pytest summary line. Required: `N passed in M.MMs` with 0 failed and 0 errors. If `warnings`, inspect them — none should be from `pytest-asyncio` config (deprecation warnings indicate the `asyncio_default_fixture_loop_scope` config didn't apply).
    </manual>
  </verify>
  <done>
    - `uv run pytest backend/tests/ -v` exits 0 with all tests passing.
    - No pytest-asyncio deprecation warnings.
    - Any incidental findings are documented in SUMMARY (with severity classification), not silently swept.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest backend/tests/ -v` from the project root passes with 0 failures.
- No test touches the real `data/conversations/` directory (verified by `find data/conversations -newer pyproject.toml` returning no files modified during the test run).
- `pytest --co -q` (collection only) shows all tests under the `backend/tests/` testpath.
- The v1→v2 migration test is the most-touched test in the suite (it's the test of greatest value per STATE.md "PITFALLS.md §CRIT-3 — the test that asserts v1 JSON sin metadata hidrata en v2 sin lanzar es el test de mayor valor del milestone").
</verification>

<success_criteria>
- TEST-01 satisfied: pytest configured strict, conftest isolates DATA_DIR + API key, factories produce canonical shapes, ASGI client works, and the 5 test areas (storage UUID/path-traversal, add_assistant_message round-trip, v1→v2 migration, parser tolerance, council profile routing) are covered.
- Suite runs cleanly from the project root with `uv run pytest backend/tests/ -v`.
- No CI configuration (per D-04b).
- pyproject.toml has `[tool.pytest.ini_options] asyncio_mode = "strict"`.
- Single fixture JSON file for the v1 conversation sample; everything else is factory-built per RESEARCH "Test Fixture Recommendation".
</success_criteria>

<output>
After completion, create `.planning/phases/07-mobile-responsive-visual-regression-tests/07-04-SUMMARY.md` summarising:
- The actual migration helper name discovered in `backend/storage.py` (`_migrate_conversation_if_needed` per PATTERNS.md, or whatever the real name is).
- Exact parser behaviour confirmed by direct call (clamp range, case sensitivity, last-occurrence handling).
- Any incidental findings (bugs surfaced by tests but not fixed in this plan, with severity classification).
- The final pytest summary line (e.g., `15 passed in 0.42s`).
- Confirmation that no Phase 7 plan dependency was introduced (this plan touches only `pyproject.toml` + `backend/tests/**` + `uv.lock` — zero overlap with 07-03 VRT or 07-05 vitest).
</output>
