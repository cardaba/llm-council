---
phase: 01-hardening-conversation-management
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - backend/main.py
  - frontend/src/api.js
  - frontend/src/App.jsx
  - frontend/src/components/Sidebar.jsx
  - frontend/src/components/Sidebar.css
autonomous: true
requirements:
  - CONV-02
tags:
  - conversation-management
  - rename
  - inline-edit
  - keyboard
  - patch-endpoint

must_haves:
  truths:
    - "El item Rename aparece en el Menu (encima de Delete) en cada conversación del sidebar."
    - "Picking Rename convierte la fila en un input controlado con el texto seleccionado y el foco activo."
    - "Enter commitea el nuevo título; Escape cancela; blur commitea (excepto si vino de Escape)."
    - "El backend acepta PATCH /api/conversations/{id} con body {title: str} (longitud 1..200) y devuelve 200 con la metadata actualizada."
    - "El nuevo título persiste tras refrescar la página y aparece en futuros listados."
    - "Un title vacío o sin cambio NO dispara PATCH (cancela silenciosamente)."
  artifacts:
    - path: "backend/main.py"
      provides: "Endpoint PATCH /api/conversations/{id} + UpdateConversationRequest model con Field(min_length=1, max_length=200)."
      contains: "@app.patch"
    - path: "frontend/src/api.js"
      provides: "api.renameConversation(id, title) usando fetch PATCH JSON."
      contains: "renameConversation"
    - path: "frontend/src/App.jsx"
      provides: "handleRenameConversation que llama api.renameConversation y refresca lista."
      contains: "handleRenameConversation"
    - path: "frontend/src/components/Sidebar.jsx"
      provides: "Estado editingId, handleKeyDown con flag intentRef, handleBlur, item Rename en el Menu."
      contains: "intentRef"
  key_links:
    - from: "Menu Rename item"
      to: "Sidebar setEditingId"
      via: "items prop callback"
      pattern: "setEditingId"
    - from: "input onKeyDown/onBlur"
      to: "App.handleRenameConversation"
      via: "intentRef + onCommitRename callback"
      pattern: "onCommitRename"
    - from: "App.handleRenameConversation"
      to: "api.renameConversation"
      via: "fetch PATCH"
      pattern: "PATCH"
    - from: "api.renameConversation"
      to: "backend PATCH endpoint"
      via: "/api/conversations/{id}"
      pattern: "method: 'PATCH'"
    - from: "backend PATCH endpoint"
      to: "storage.update_conversation_title"
      via: "función ya existente"
      pattern: "storage.update_conversation_title"
---

<objective>
Slice vertical end-to-end de **CONV-02: rename inline de conversaciones**. Reutiliza el `Menu` introducido en Plan 02 añadiendo un item `Rename` arriba de `Delete`. Implementa el mode de edición inline con resolución correcta de la carrera Enter/Blur/Escape mediante un `intentRef` (RESEARCH §Pattern 4). Backend expone `PATCH /api/conversations/{id}` que valida title con `Pydantic Field(min_length=1, max_length=200)` y reusa `storage.update_conversation_title` (función existente).

**Por qué este es un plan separado del 02 a pesar de tocar Sidebar**: el rename inline tiene su propia complejidad (la carrera de eventos Enter/Blur/Escape) y aislarlo evita que el ejecutor mezcle dos features simultáneos en una sola pasada. Las dependencias hacia plan 02 son: Menu existe (para añadir el item Rename) y el patrón de validación UUID 400 está desplegado.

**Purpose:** Cumplir CONV-02 sin retrofitear los archivos del sidebar.
**Output:** PATCH endpoint, api.renameConversation, item Rename en menu, modo edit inline con resolución de race condition.
**Requirement coverage:** CONV-02 (per D-04, D-05, D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-hardening-conversation-management/01-CONTEXT.md
@.planning/phases/01-hardening-conversation-management/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@.planning/phases/01-hardening-conversation-management/01-01-SUMMARY.md
@.planning/phases/01-hardening-conversation-management/01-02-SUMMARY.md
@backend/main.py
@backend/storage.py
@frontend/src/App.jsx
@frontend/src/api.js
@frontend/src/components/Sidebar.jsx
@frontend/src/components/Sidebar.css

<interfaces>
<!-- storage helper que ya existe (sin tocar) -->
```python
# backend/storage.py:159-172 — preexistente
def update_conversation_title(conversation_id: str, title: str):
    conversation = get_conversation(conversation_id)  # raises ValueError tras Plan 01
    if conversation is None:
        raise ValueError(f"Conversation {conversation_id} not found")
    conversation["title"] = title
    save_conversation(conversation)
```

<!-- Menu items shape (introducido en Plan 02) -->
```jsx
// items prop del componente Menu
[
  { label: 'Rename', onClick: () => {...} },
  { label: 'Delete', destructive: true, onClick: () => {...} },
]
```

<!-- Patrón inline-edit a copiar literalmente desde RESEARCH §Pattern 4 -->
- Estado: `editingId` (id que se está renombrando) y `draftTitle` (string controlado)
- Refs: `intentRef` ('commit' | 'cancel' | null) y `inputRef`
- onKeyDown: setear intentRef y disparar `e.target.blur()` para que onBlur tome el camino único
- onBlur: leer intentRef, decidir commit vs cancel; trim + check de "no change"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 3.1: PATCH endpoint backend + api.renameConversation + handleRenameConversation</name>
  <files>backend/main.py, frontend/src/api.js, frontend/src/App.jsx</files>

  <read_first>
    - backend/main.py (estado actual tras Planes 01 y 02)
    - backend/storage.py (verifica que `update_conversation_title` existe sin cambios)
    - frontend/src/api.js (estado tras Plan 02 con `deleteConversation` ya añadido)
    - frontend/src/App.jsx (estado tras Plan 02 con `handleDeleteConversation` ya añadido)
    - .planning/phases/01-hardening-conversation-management/01-RESEARCH.md secciones "PATCH and DELETE handler pattern" (líneas 769-811), "API client extensions" (815-841).
    - .planning/phases/01-hardening-conversation-management/01-CONTEXT.md decisión D-15 (PATCH también heredará validación UUID).
  </read_first>

  <action>
    **Parte A — Backend `backend/main.py`**: añadir el modelo Pydantic `UpdateConversationRequest` y el endpoint PATCH.

    Insertar el modelo cerca de los modelos existentes (después de `class Conversation` línea 45-50, antes de la primera ruta):

    ```python
    class UpdateConversationRequest(BaseModel):
        """Request to update conversation metadata. Only `title` is editable in v1."""
        title: str = Field(..., min_length=1, max_length=200)
    ```

    Y al inicio del archivo, asegurar que `Field` está importado: cambiar `from pydantic import BaseModel` por `from pydantic import BaseModel, Field`.

    Añadir el handler PATCH justo antes del handler DELETE (creado en Plan 02), o al final junto con él:

    ```python
    @app.patch("/api/conversations/{conversation_id}", response_model=ConversationMetadata)
    async def patch_conversation(conversation_id: str, request: UpdateConversationRequest):
        """
        Update a conversation's metadata. Only `title` is supported in v1.

        Returns 200 with updated metadata.
        Returns 400 if conversation_id is not a valid UUID.
        Returns 404 if the conversation does not exist.
        Returns 422 if the body fails Pydantic validation (empty title, > 200 chars, etc.).
        """
        try:
            existing = storage.get_conversation(conversation_id)
        except ValueError:
            raise HTTPException(status_code=400, detail="Invalid conversation ID")
        if existing is None:
            raise HTTPException(status_code=404, detail="Conversation not found")

        storage.update_conversation_title(conversation_id, request.title)

        return {
            "id": conversation_id,
            "created_at": existing["created_at"],
            "title": request.title,
            "message_count": len(existing["messages"]),
        }
    ```

    NOTA: `storage.update_conversation_title` ya valida el UUID a través de `get_conversation_path` (Plan 01) y maneja el caso `None` lanzando ValueError, pero como ya hicimos `get_conversation` antes y verificamos None, el flujo dentro de `update_conversation_title` no encuentra `None`. No se necesita un segundo try/except.

    **Parte B — `frontend/src/api.js`**: añadir `renameConversation`.

    Insertar dentro del objeto `api` (junto al `deleteConversation` añadido en Plan 02):

    ```js
    /**
     * Rename a conversation. Persists across reloads.
     */
    async renameConversation(conversationId, title) {
      const response = await fetch(
        `${API_BASE}/api/conversations/${conversationId}`,
        {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ title }),
        }
      );
      if (!response.ok) {
        throw new Error('Failed to rename conversation');
      }
      return response.json();
    },
    ```

    **Parte C — `frontend/src/App.jsx`**: añadir `handleRenameConversation` y propagar al Sidebar.

    Añadir tras `handleDeleteConversation` (que ya existe tras Plan 02):

    ```jsx
    const handleRenameConversation = async (id, newTitle) => {
      try {
        await api.renameConversation(id, newTitle);
        await loadConversations();
      } catch (error) {
        console.error('Failed to rename conversation:', error);
      }
    };
    ```

    Modificar el render del Sidebar para pasar la prop:

    ```jsx
    <Sidebar
      conversations={conversations}
      currentConversationId={currentConversationId}
      onSelectConversation={handleSelectConversation}
      onNewConversation={handleNewConversation}
      onDeleteConversation={handleDeleteConversation}
      onRenameConversation={handleRenameConversation}
    />
    ```

    NOTA crítica per RESEARCH §Anti-Patterns línea 592: si el rename es de la conversación actualmente abierta, NO llamar también `loadConversation(currentConversationId)`. El título visible en el sidebar se actualiza vía `loadConversations()`; el contenido del chat no depende del título.
  </action>

  <verify>
    <automated>cd C:/GIT/llm-council &amp;&amp; uv run python -c "from backend.main import app, UpdateConversationRequest; from pydantic import ValidationError;
# Verifica que el modelo rechaza title vacío
ok = False
try:
    UpdateConversationRequest(title='')
except ValidationError:
    ok = True
assert ok, 'empty title should be rejected'
# Verifica que rechaza > 200 chars
ok = False
try:
    UpdateConversationRequest(title='x'*201)
except ValidationError:
    ok = True
assert ok, 'over 200 chars should be rejected'
# Verifica route registrada
methods_paths = [(r.methods, r.path) for r in app.routes if hasattr(r, 'methods')]
assert any('PATCH' in m and '/api/conversations/{conversation_id}' == p for m, p in methods_paths), methods_paths
print('PATCH route + Pydantic limits: OK')" &amp;&amp; cd frontend &amp;&amp; npx eslint src/api.js src/App.jsx --max-warnings 0</automated>
  </verify>

  <acceptance_criteria>
    - Backend route: `grep -c "@app.patch(\"/api/conversations/{conversation_id}\"" backend/main.py` == 1
    - Backend model: `grep -c "class UpdateConversationRequest" backend/main.py` == 1
    - Backend Field constraints: `grep -c "min_length=1" backend/main.py` >= 1 y `grep -c "max_length=200" backend/main.py` >= 1
    - Backend reusa storage.update_conversation_title: `grep -c "storage.update_conversation_title" backend/main.py` >= 1
    - Backend pydantic Field import: `grep -c "from pydantic import BaseModel, Field" backend/main.py` == 1 (o equivalente con Field importado)
    - Frontend api: `grep -c "renameConversation" frontend/src/api.js` >= 1
    - Frontend api method: `grep -c "method: 'PATCH'" frontend/src/api.js` >= 1
    - App handler: `grep -c "handleRenameConversation" frontend/src/App.jsx` >= 2
    - Prop wired: `grep -c "onRenameConversation={handleRenameConversation}" frontend/src/App.jsx` == 1
    - Comportamiento (servidor corriendo):
      - `NEW=$(curl -s -X POST http://127.0.0.1:8001/api/conversations -H "Content-Type: application/json" -d '{}' | python -c "import sys,json; print(json.load(sys.stdin)['id'])")`
      - `curl -i -X PATCH "http://127.0.0.1:8001/api/conversations/$NEW" -H "Content-Type: application/json" -d '{"title":"My new title"}'` → HTTP 200, body con `"title":"My new title"`.
      - `curl -i "http://127.0.0.1:8001/api/conversations/$NEW"` → muestra el nuevo título (persistencia).
      - `curl -i -X PATCH "http://127.0.0.1:8001/api/conversations/$NEW" -H "Content-Type: application/json" -d '{"title":""}'` → HTTP 422 (Pydantic min_length).
      - `curl -i -X PATCH http://127.0.0.1:8001/api/conversations/not-a-uuid -H "Content-Type: application/json" -d '{"title":"x"}'` → HTTP 400.
      - `curl -i -X PATCH http://127.0.0.1:8001/api/conversations/00000000-0000-4000-8000-000000000000 -H "Content-Type: application/json" -d '{"title":"x"}'` → HTTP 404.
  </acceptance_criteria>

  <done>
    Backend expone `PATCH /api/conversations/{id}` con validación de body Pydantic (title 1..200 chars), retornando 200/400/404/422 según el caso. Frontend tiene `api.renameConversation` y `handleRenameConversation` propagado al Sidebar.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.2: Inline rename UX en Sidebar (intentRef pattern + Menu Rename item)</name>
  <files>frontend/src/components/Sidebar.jsx, frontend/src/components/Sidebar.css</files>

  <read_first>
    - frontend/src/components/Sidebar.jsx (estado tras Plan 02 con Menu y Modal cableados)
    - .planning/phases/01-hardening-conversation-management/01-RESEARCH.md sección "Pattern 4: Inline Rename — Resolving Enter / Escape / Blur Race" (líneas 433-509) — copiar literalmente.
    - .planning/phases/01-hardening-conversation-management/01-RESEARCH.md "Anti-Patterns" línea 591-592 (mantener controlled-input pattern, no `defaultValue`).
    - .planning/phases/01-hardening-conversation-management/01-CONTEXT.md decisión D-06 (Enter commits, Escape cancels, blur commits, single-line) y CD-05 (test ad-hoc aceptable para keyboard handling).
  </read_first>

  <action>
    Refactorizar `Sidebar.jsx` para soportar el modo edit inline. **NO romper el cableado de Plan 02** (Menu, Modal, trigger ⋮, handleDeleteConversation prop).

    Cambios necesarios:

    1. Aceptar prop nueva `onRenameConversation` en la firma del componente.

    2. Añadir estado para el rename:
    ```jsx
    const [editingId, setEditingId] = useState(null);
    ```

    3. Extraer la fila a un sub-componente `ConversationItem` dentro del mismo archivo (CD-04 lo permite, y ayuda a aislar refs por fila):

    ```jsx
    function ConversationItem({
      conv,
      isActive,
      isEditing,
      onSelect,
      onContextMenu,
      onMenuTriggerClick,
      isMenuOpen,
      onCommitRename,
      onCancelRename,
    }) {
      const [draftTitle, setDraftTitle] = useState(conv.title || '');
      const intentRef = useRef(null);  // 'commit' | 'cancel' | null
      const inputRef = useRef(null);

      // Reset draft + intent when entering edit mode (handles rapid menu clicks).
      useEffect(() => {
        if (isEditing) {
          setDraftTitle(conv.title || '');
          intentRef.current = null;
        }
      }, [isEditing, conv.title]);

      // Autofocus + select-all when entering edit mode.
      useEffect(() => {
        if (isEditing && inputRef.current) {
          inputRef.current.focus();
          inputRef.current.select();
        }
      }, [isEditing]);

      const handleKeyDown = (e) => {
        if (e.key === 'Enter') {
          e.preventDefault();
          intentRef.current = 'commit';
          e.target.blur();  // single exit path → handleBlur
        } else if (e.key === 'Escape') {
          e.preventDefault();
          intentRef.current = 'cancel';
          e.target.blur();
        }
      };

      const handleBlur = () => {
        if (intentRef.current === 'cancel') {
          onCancelRename();
        } else {
          // commit (Enter or default blur). Trim + reject empty/unchanged silently.
          const trimmed = draftTitle.trim();
          if (trimmed && trimmed !== (conv.title || '')) {
            onCommitRename(conv.id, trimmed);
          } else {
            onCancelRename();
          }
        }
        intentRef.current = null;
      };

      return (
        <div
          className={`conversation-item ${isActive ? 'active' : ''}`}
          onClick={isEditing ? undefined : onSelect}
          onContextMenu={isEditing ? undefined : onContextMenu}
        >
          <div className="conversation-content">
            {isEditing ? (
              <input
                ref={inputRef}
                className="conversation-title-input"
                type="text"
                value={draftTitle}
                onChange={(e) => setDraftTitle(e.target.value)}
                onKeyDown={handleKeyDown}
                onBlur={handleBlur}
                onClick={(e) => e.stopPropagation()}
                maxLength={200}
              />
            ) : (
              <>
                <div className="conversation-title">
                  {conv.title || 'New Conversation'}
                </div>
                <div className="conversation-meta">
                  {conv.message_count} messages
                </div>
              </>
            )}
          </div>
          {!isEditing && (
            <button
              type="button"
              className="menu-trigger"
              aria-label={`Actions for ${conv.title || 'conversation'}`}
              aria-haspopup="menu"
              aria-expanded={isMenuOpen}
              onClick={onMenuTriggerClick}
            >
              ⋮
            </button>
          )}
        </div>
      );
    }
    ```

    4. En el componente Sidebar principal, mapear sobre conversations usando `ConversationItem` y proporcionar callbacks. El item `Rename` del Menu llama a `setEditingId(conv.id)` + cerrar menu. El item `Delete` sigue funcionando como en Plan 02:

    ```jsx
    {openMenuFor && (
      <Menu
        x={openMenuFor.x}
        y={openMenuFor.y}
        onClose={() => setOpenMenuFor(null)}
        items={[
          {
            label: 'Rename',
            onClick: () => {
              setEditingId(openMenuFor.id);
            },
          },
          {
            label: 'Delete',
            destructive: true,
            onClick: () => {
              const conv = conversations.find((c) => c.id === openMenuFor.id);
              if (conv) requestDelete(conv);
            },
          },
        ]}
      />
    )}
    ```

    5. Pasar callbacks al `ConversationItem`:

    ```jsx
    {conversations.map((conv) => (
      <ConversationItem
        key={conv.id}
        conv={conv}
        isActive={conv.id === currentConversationId}
        isEditing={conv.id === editingId}
        isMenuOpen={openMenuFor?.id === conv.id}
        onSelect={() => onSelectConversation(conv.id)}
        onContextMenu={(e) => {
          e.preventDefault();
          e.stopPropagation();
          setOpenMenuFor({ id: conv.id, x: e.clientX, y: e.clientY });
        }}
        onMenuTriggerClick={(e) => {
          e.stopPropagation();
          const rect = e.currentTarget.getBoundingClientRect();
          setOpenMenuFor({ id: conv.id, x: rect.right, y: rect.bottom + 4 });
        }}
        onCommitRename={(id, newTitle) => {
          setEditingId(null);
          onRenameConversation(id, newTitle);
        }}
        onCancelRename={() => setEditingId(null)}
      />
    ))}
    ```

    6. Imports al inicio del archivo, añadir `useEffect` y `useRef`:
    ```jsx
    import { useState, useEffect, useRef } from 'react';
    ```

    7. Añadir CSS para el input inline en `Sidebar.css` (al final del archivo):

    ```css
    .conversation-item .conversation-title-input {
      width: 100%;
      box-sizing: border-box;
      padding: 4px 6px;
      font-size: inherit;
      font-family: inherit;
      color: #222;
      background: #fff;
      border: 1px solid #4a90e2;
      border-radius: 3px;
      outline: none;
    }
    .conversation-item .conversation-title-input:focus {
      outline: 2px solid #4a90e2;
      outline-offset: 0;
    }
    ```

    **Anti-patterns a evitar (RESEARCH §Anti-Patterns):**
    - NO usar `defaultValue` + `inputRef.current.value`. El input es controlled (consistente con `ChatInterface.jsx:255-263`).
    - NO ejecutar `loadConversation(currentConversationId)` después del rename — sólo `loadConversations()` (que ya hace App.handleRenameConversation).
    - NO commit prematuro: el handler debe respetar `intentRef`. Si no se establece flag y el user hace blur "natural" (clic fuera), eso ES un commit (D-06 dice blur commits).
  </action>

  <verify>
    <automated>cd C:/GIT/llm-council/frontend &amp;&amp; npx eslint src/components/Sidebar.jsx --max-warnings 0 &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Imports: `grep -c "useState, useEffect, useRef" frontend/src/components/Sidebar.jsx` == 1 (o useEffect, useRef presentes en algún import desde 'react').
    - Editing state: `grep -c "editingId" frontend/src/components/Sidebar.jsx` >= 3 (declare + setter calls + check)
    - intentRef pattern: `grep -c "intentRef" frontend/src/components/Sidebar.jsx` >= 4 (RESEARCH §Pattern 4)
    - Race condition keys: `grep -c "'commit'" frontend/src/components/Sidebar.jsx` >= 1 y `grep -c "'cancel'" frontend/src/components/Sidebar.jsx` >= 1
    - Controlled input: `grep -c "value={draftTitle}" frontend/src/components/Sidebar.jsx` == 1 (no `defaultValue`)
    - Anti-pattern check: `grep -c "defaultValue" frontend/src/components/Sidebar.jsx` == 0
    - Menu Rename item presente: `grep -c "label: 'Rename'" frontend/src/components/Sidebar.jsx` == 1
    - Item order: en el array de items del Menu, Rename aparece ANTES que Delete (verificar con awk: `awk "/label: 'Rename'/{r=NR} /label: 'Delete'/{print (r > 0 \&\& NR > r) ? \"OK\" : \"FAIL: rename must come before delete\"}" frontend/src/components/Sidebar.jsx`).
    - Input maxLength sincronizado con backend: `grep -c "maxLength={200}" frontend/src/components/Sidebar.jsx` == 1
    - CSS: `grep -c "conversation-title-input" frontend/src/components/Sidebar.css` >= 1
    - Build: `cd frontend && npm run build` exit 0.
    - **Manual smoke** (servidor + frontend):
      1. Click en `⋮` en una conversación → menú muestra `Rename` y `Delete` (en ese orden).
      2. Click en `Rename` → la fila se reemplaza por un input con el título seleccionado y foco.
      3. Tipear "Test Rename" + Enter → input desaparece, sidebar muestra el nuevo título, GET `/api/conversations` confirma persistencia.
      4. Refrescar el browser → el nuevo título sigue ahí.
      5. Activar rename de nuevo, modificar texto, Escape → vuelve al título original sin PATCH (verificar que NO hay request en network tab).
      6. Activar rename de nuevo, modificar texto, click en otra fila → blur dispara commit (D-06).
      7. Activar rename, dejar vacío, Enter → cancela silenciosamente, título queda como antes (no PATCH, no error UI).
      8. Right-click en una fila distinta mientras se edita otra → el menú se abre (no rompe el edit).
  </acceptance_criteria>

  <done>
    Sidebar.jsx contiene el sub-componente `ConversationItem` con manejo correcto de la race condition Enter/Escape/Blur via `intentRef`. El Menu del Plan 02 ahora expone `Rename` antes de `Delete`. El input es controlled con `maxLength={200}` (alineado con backend). CONV-02 satisfecho end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI loopback | PATCH body `{title}` es input no confiable; `Field(min_length=1, max_length=200)` lo restringe a string corto. Path `{id}` heredada validación UUID de Plan 01. |
| Render del título en sidebar / chat header | Texto del usuario aparece como child de un nodo React → escapado automáticamente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | PATCH con id manipulado | mitigate | Heredado de Plan 01: ValueError → 400 antes de tocar filesystem. |
| T-03-02 | Tampering | PATCH con title vacío o gigante | mitigate | Pydantic `Field(min_length=1, max_length=200)` rechaza con 422 sin escribir storage. |
| T-03-03 | Information Disclosure | Title con caracteres de control rompe parsing JSON | accept | Pydantic acepta cualquier string Unicode válido; `json.dump` en storage maneja escaping. No se almacena nada que no se pueda leer. |
| T-03-04 | XSS via title | Title se renderiza en sidebar | mitigate | React escapa string children por defecto; el componente NO interpreta HTML del título. |
| T-03-05 | DoS por flood de PATCH | Sin rate limit | accept | Single-user local, mismo modelo de amenaza que CONV-01 (T-02-03). |
| T-03-06 | Repudiation | No hay auditoría de cambios de título | accept | Single-user local; sin requirement de log. |
</threat_model>

<verification>
**Tests manuales backend:**
```bash
NEW=$(curl -s -X POST http://127.0.0.1:8001/api/conversations -H "Content-Type: application/json" -d '{}' | python -c "import sys,json; print(json.load(sys.stdin)['id'])")

# Happy path
curl -i -X PATCH "http://127.0.0.1:8001/api/conversations/$NEW" \
  -H "Content-Type: application/json" -d '{"title":"My deliberation about X"}'
# Expected: 200, body shows new title

# Persistence
curl -s "http://127.0.0.1:8001/api/conversations/$NEW" | grep "My deliberation"

# Empty title rejected
curl -i -X PATCH "http://127.0.0.1:8001/api/conversations/$NEW" \
  -H "Content-Type: application/json" -d '{"title":""}'
# Expected: 422

# Over-long title rejected
curl -i -X PATCH "http://127.0.0.1:8001/api/conversations/$NEW" \
  -H "Content-Type: application/json" -d '{"title":"'"$(python -c 'print("x"*201)')"'"}'
# Expected: 422

# Bad UUID
curl -i -X PATCH http://127.0.0.1:8001/api/conversations/not-a-uuid \
  -H "Content-Type: application/json" -d '{"title":"x"}'
# Expected: 400

# Not found
curl -i -X PATCH http://127.0.0.1:8001/api/conversations/00000000-0000-4000-8000-000000000000 \
  -H "Content-Type: application/json" -d '{"title":"x"}'
# Expected: 404
```

**Tests manuales UI:** ver acceptance_criteria smoke 1-8.
</verification>

<success_criteria>
1. **CONV-02 satisfecho**: el usuario puede editar el título inline desde el menú Rename y el cambio persiste tras refrescar.
2. **Race condition resuelta**: Enter, Escape y blur producen el resultado correcto sin commits dobles ni inconsistencias entre el state del input y el state del store.
3. **Backend valida tamaño**: títulos vacíos o >200 chars devuelven 422 sin escribir archivo.
4. **Sin regresiones**: el menú Delete sigue funcionando igual que tras Plan 02; el modal de delete sigue ahí; el flujo post-delete welcome state intacto.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hardening-conversation-management/01-03-SUMMARY.md` describing:
- PATCH endpoint signature, status codes y body shape.
- Cómo el `intentRef` resuelve la carrera Enter/Escape/Blur (con ejemplo de los tres caminos).
- ConversationItem extracted como sub-componente, justificación CD-04.
- Smoke checklist UI (los 8 items del acceptance manual).
</output>
